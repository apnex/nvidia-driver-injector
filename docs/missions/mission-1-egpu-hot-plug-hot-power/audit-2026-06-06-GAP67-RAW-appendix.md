# GAP-6/7 audit — RAW per-auditor streams (appendix)
[{"area":"GAP-6 engine-dispatch audit of timeoutCondWait (C7-e2 insertion point) in /root/open-gpu-kernel-modules/src/nvidia/src/kernel/gpu/gpu_timeout.c + inc/kernel/gpu/gpu_timeout.h, with full caller sweep and GB202/GSP-RM/eGPU reachability via generated NVOC funcTables","callers":["src/kernel/gpu/arch/blackwell/kern_gpu_gb100.c:1578 | _gpuMnocMboxSendReceiverCond | returned directly by _gpuMnocMboxSendReceiverReady_GB100 -> gpuMnocMboxSend_GB100 propagates | no (generic) | no (REG_RD32 BAR0 IoAperture) | NO: gpuMnocMboxSend GB202-binding=stub _395e98 (g_gpu_nvoc.c:1789 mask=GB100/GB102/GB110/GB112) AND PDB_PROP_KFSP_USE_MNOC_CPU set only via regkey NV_REG_STR_RM_FSP_USE_MNOC_CPU (kern_fsp.c:168) | SAFE","src/kernel/gpu/arch/blackwell/kern_gpu_gb100.c:1620 | _gpuMnocMboxSendCreditCond | same propagate-only | no | no (MMIO) | NO (same double gate) | SAFE","src/kernel/gpu/arch/blackwell/kern_gpu_gb100.c:1690 | _gpuMnocMboxPollCond | same propagate-only | no | no (MMIO) | NO (same double gate) | SAFE","src/kernel/gpu/falcon/arch/ampere/kernel_falcon_ga102.c:264 | _kflcnWaitForScrubbingToFinish | returned directly by kflcnWaitForResetToFinish_GA102; upstream NV_ASSERT_OK propagate | no | no (kflcnRegRead HWCFG2) | YES: GB202 binds _GA102 (g_kernel_falcon_nvoc.c:578 else-branch); runs on every falcon/GSP reset | SAFE (note: function already early-returns NV_ERR_GPU_IN_FULLCHIP_RESET on IN_RESET — precedent for early-error)","src/kernel/gpu/falcon/arch/turing/kernel_falcon_tu102.c:323 | _kflcnMemScrubbingFinished | returned directly | no | no (MMIO) | NO: _TU102 bound only TU102-GA100 | SAFE","src/kernel/gpu/falcon/arch/turing/kernel_falcon_tu102.c:549 | s_riscvIsIcdNotBusy | 'if (gpuTimeoutCondWait(...) != NV_OK) return NV_ERR_TIMEOUT;' then 5 upstream consumers at tu102.c:606,661,707,747,785 test '(kflcnRiscvIcdWaitForIdle_HAL(...) != NV_ERR_TIMEOUT)' | YES — but the TIMEOUT branch is the BAIL-OUT: 'else { return NV_ERR_INVALID_STATE; }' i.e. on TIMEOUT it SKIPS issuing the ICD MMIO command; kflcnRiscvIcdHalt:826 retry loop is bounded (tries=9) | no (ICD regs MMIO) | YES: kflcnRiscvIcdWaitForIdle GB202→_TU102 (g_kernel_falcon_nvoc.c:718, GB202 in mask); used in RISCV crash-forensic dumps | SAFE — C7-e2 makes the dead-bus case fail in ~0 instead of 125ms x N and skips dead MMIO writes","src/kernel/gpu/gsp/arch/hopper/kernel_gsp_gh100.c:133 | _kgspWaitForAsserted (GPU_TIMEOUT_FLAGS_TMR) | 'if (status != NV_OK) { NV_PRINTF; DBG_BREAKPOINT(); }' then return status; caller _kgspBootstrapGspFmc_GH100:726 NV_ASSERT_OK_OR_RETURN | no | no (GPU_FLD_TEST_DRF_DEF RESET_STATUS) | YES: kgspResetHw GB202→_GB100 (g_kernel_gsp_nvoc.c:1158) which tail-calls _GH100; every GSP boot/teardown | SAFE","src/kernel/gpu/gsp/arch/hopper/kernel_gsp_gh100.c:144 | _kgspWaitForDeasserted (TMR) | same print+breakpoint+return | no | no (MMIO) | YES (same path) | SAFE","src/kernel/gpu/gsp/arch/hopper/kernel_gsp_gh100.c:620 | _kgspSpdmBootedOrFmcError | '!= NV_OK → print, goto exit' propagate | no | no (kflcnRegRead mailbox) | NO on config: _kgspEstablishSpdmSession gated at kernel_gsp_gh100.c:1026 'pSpdm != NULL && PDB_PROP_SPDM_ENABLED' — no CC/SPDM on consumer GB202 | SAFE","src/kernel/gpu/gsp/arch/hopper/kernel_gsp_gh100.c:662 | _kgspFalconMailbox0Cleared | '!= NV_OK → print, goto exit' | no | no (MMIO) | NO on config (same SPDM gate) | SAFE","src/kernel/gpu/gsp/arch/hopper/kernel_gsp_gh100.c:1050 | _kgspLockdownReleasedOrFmcError | '!= NV_OK → print + kgspFmcReportErrorCode + kfspDumpDebugState + 2 mailbox reads + return status' (kgspBootstrap_GH100 propagates to RmInitAdapter) | no (generic) | no (kflcnRegRead lockdown/mailbox) | YES — THE #292 wedge site; kgspBootstrap GB202→_GH100 (g_kernel_gsp_nvoc.c:803) | SAFE + PRIMARY TARGET: failure path is prints + dead-MMIO reads (os_pci-short-circuited); C7-e2 converts unbounded spin/accidental-0xFFFFFFFF-NV_OK into prompt clean NV_ERR_TIMEOUT","src/kernel/gpu/gsp/arch/hopper/kernel_gsp_gh100.c:1271 | _kgspHasCcCleanupFinished | '!= NV_OK → print, goto exit'; exit unconditionally writes MAILBOX1 ack (posted write, harmless dead-bus) | no | no (MMIO) | NO on config: caller kgspUnloadRm_IMPL:5187 gated gpuIsCCFeatureEnabled | SAFE","src/kernel/gpu/sec2/arch/blackwell/kernel_sec2_gb20b.c:577 | _ksec2WaitBootCond_GB20B | return status | no | no (MMIO) | NO: ksec2WaitForSecureBoot GB202→stub _395e98 (g_kernel_sec2_nvoc.c:885, GB20B/GB20C only) | SAFE","src/kernel/gpu/fsp/kern_fsp.c:364 | _kfspWaitForCanSend | print + return; consumer kfspSendPacket_GH100 (kern_fsp_gh100.c:700) remaps '!= NV_OK → return NV_ERR_INSUFFICIENT_RESOURCES' | no | no (kfspCanSendPacket reads queue head/tail MMIO) | YES: kfspPollForCanSend_IMPL, every FSP command on GB202 (kfspSendPacket GB202→_GB100→falls through to _GH100 without MNOC regkey) | SAFE","src/kernel/gpu/gsp/arch/turing/kernel_gsp_falcon_tu102.c:473 | _kgspIsReloadCompleted | 'if ((status != NV_OK) || (secMailbox0 != NV_OK)) return NV_ERR_TIMEOUT' | no (generic-OR, output remapped to TIMEOUT) | no (GPU_REG_RD32 BSI scratch) | RARE on GB202: kgspExecuteCoreResume GB202→_TU102 (g_kernel_gsp_nvoc.c:2002) but only GB202-reachable caller is GSP-RM RPC load_exec_hs_binary → kgspLoadAndExecuteHsBinary_GA102:401 (kernel_gsp.c:458 RPC handler); kgspBootstrap_TU102 path not bound | SAFE","src/kernel/gpu/sec2/arch/blackwell/kernel_sec2_gb10b.c:308 | _ksec2IsCmdhandlingCompleted_GB10B | return status | no | no (MMIO) | NO: GB10B SoC only | SAFE","src/kernel/gpu/sec2/arch/blackwell/kernel_sec2_gb10b.c:376 | _ksec2IsGspTargetMaskReleased | return status; kgspBootstrap consumer prints + propagates | no | no (GPU_REG_RD32 HWCFG2) | NO: ksec2WaitForGspTargetMaskReleased GB202→stub (g_kernel_sec2_nvoc.c:840, GB10B/GB20B/GB20C) and bootstrap SEC2 branch gated PDB_PROP_KSEC2_BOOT_GSPFMC | SAFE","src/kernel/gpu/gsp/arch/blackwell/kernel_gsp_gb100.c:83 | _kgspWaitForResetAccess (GPU_TIMEOUT_FLAGS_TMR) | 'print + DBG_BREAKPOINT' then CONTINUES into kgspResetHw_GH100 regardless | no | no (PLM reg MMIO) | YES: kgspResetHw GB202→_GB100; every GSP reset | SAFE (continuation = posted writes + two further bounded cond-waits; note TMR-flag waits on dead bus already self-terminate fast since tmrGetCurrentTime reads 0xFFFFFFFF.. ≥ deadline — C7-e2 makes it deterministic)","src/kernel/gpu/bif/arch/ada/kernel_bif_ad102.c:73 | _kbifPreOsCheckErotGrantAllowed_AD102 | print + return status | no | no (MMIO) | NO: kbifPreOsGlobalErotGrantRequest GB202→stub _ac1694 (g_kernel_bif_nvoc.c:1057, AD102-AD107 only) | SAFE","src/kernel/gpu/fsp/arch/blackwell/kern_fsp_gb202.c:80 | _kfspWaitBootCond_GB202 (OSTIMER|BYPASS_THREAD_STATE, 5s) | '!= NV_OK → gpuMarkDeviceForReset (SW-state only: _gpuSetResetRequiredState) + NV_ERROR_LOG + 5 reg reads + kfspDumpDebugState + return status'; consumers (kgspWaitForGfwBootOk_GH100:257, kern_fsp_gh100.c:1329, kernel_bif_gh100.c:1059 FLR) all generic '!= NV_OK' propagate | no | no (THERM scratch MMIO) | YES: GB202's own FSP secure-boot wait — the A6/A12 cold-init path, every RmInitAdapter | SAFE — C7-e2 bounds the 5s cold-init poll on dead bus; meshes with A12 funnel","src/kernel/gpu/gsp/arch/ampere/kernel_gsp_falcon_ga102.c:92 | s_dmaPollCondFunc | 'print + DBG_BREAKPOINT + return status'; upstream NV_ASSERT_OK_OR_RETURN in s_dmaTransfer_GA102; result becomes RPC status back to GSP | no | no (DMATRFCMD MMIO; the DMA itself is HW-side, the cond is a reg poll) | RARE on GB202: via kgspLoadAndExecuteHsBinary_GA102 (GB202-bound, g_kernel_gsp_nvoc.c:2061) triggered only by GSP RPC load_exec_hs_binary | SAFE","src/kernel/gpu/fsp/arch/hopper/kern_fsp_gh100.c:828 | _kfspWaitBootCond_GH100 | gpuMarkDeviceForReset + log + return | no | no (MMIO) | NO: kfspWaitForSecureBoot GB202→_GB202 (g_kern_fsp_nvoc.c:494) | SAFE","src/kernel/gpu/fsp/arch/hopper/kern_fsp_gh100.c:1158 | _kfspIsGspTargetMaskReleased | return status; consumer kgspBootstrap_GH100 prints 'Timeout waiting for GSP target mask release' + kgspFmcReportErrorCode + kfspDumpDebugState + return status | no | no (GPU_REG_RD32_UNCHECKED HWCFG2 — deliberately bypasses 0xbadf check, returns 0xFFFFFFFF=released on dead bus → accidental-true exit class) | YES: kfspWaitForGspTargetMaskReleased GB202→_GH100 (g_kern_fsp_nvoc.c:631); every GSP boot, immediately precedes the lockdown wait | SAFE — C7-e2 replaces accidental-0xFFFFFFFF 'released' with clean TIMEOUT","src/kernel/gpu/fsp/arch/blackwell/kern_fsp_gb100.c:95 | _kfspWaitBootCond_GB100 | gpuMarkDeviceForReset + log + kfspDumpDebugState + return | no | no (MMIO) | NO: GB202 uses _GB202 variant | SAFE","src/kernel/gpu/gsp/arch/turing/kernel_gsp_tu102.c:1155 | _kgspIsProcessorSuspended | return value IGNORED at kgspUnloadRm_IMPL (kernel_gsp.c:5194 — bare call, teardown proceeds); generic 'print mailbox + return status' at kgspLoadAndExecuteHsBinary_GA102:325 | no | no (mailbox MMIO) | YES: kgspWaitForProcessorSuspend GB202→_TU102 (g_kernel_gsp_nvoc.c:1549); every driver unload / suspend — the A7/F40 shutdown arm | SAFE — C7-e2 bounds a wait whose status is discarded anyway; dead-bus unload today burns the full default timeout here"],"engine_findings":["DISPATCH: gpuTimeoutCondWait is a macro, gpu_timeout.h:147 '#define gpuTimeoutCondWait(g,a,b,t) timeoutCondWait(&(g)->timeoutData, t, a, b, __LINE__)' — direct call, no vtable, no HAL halification (timeoutCondWait/timeoutCheck/timeoutSet are plain non-NVOC functions). TIMEOUT_DATA has no function pointers (gpu_timeout.h:76-92).","SINGLE ENGINE BODY: tree-wide grep (src/nvidia + src/common + kernel-open, case-insensitive 'condwait') finds NO second cond-wait engine — no tmrApiCondWait, no timeoutCondWaitTmr. GPU_TIMEOUT_FLAGS_TMR/_TMRDELAY/_OSTIMER/_OSDELAY are branches inside the static _checkTimeout (gpu_timeout.c:367/380/413/435) called from timeoutCheck inside the ONE timeoutCondWait loop. ONE C7-e2 edit in the loop (gpu_timeout.c:591-605) intercepts ALL gpuTimeoutCondWait paths, all 25 call sites, all flag variants. VERIFIED.","SINGLE INSTANCE: TIMEOUT_DATA exists only as pGpu->timeoutData (g_gpu_nvoc.h:1295); initialized at gpuPostConstruct (gpu.c:458) and refreshed on compute-mode refcount flips (gpu.c:322/340). No free-standing TIMEOUT_DATA anywhere.","API_GPU_IN_RESET_SANITY_CHECK (g_gpu_nvoc.h:5873): (pGpu==NULL) || PDB_PROP_GPU_IN_FULLCHIP_RESET || _IN_SECONDARY_BUS_RESET || _IN_GC6_RESET || _SECONDARY_BUS_RESET_PENDING → timeoutCheck:483 returns NV_ERR_TIMEOUT immediately. On our config during #292 none of these are set (RM never initiates the SBR; tb_egpu recover is external) → no early-out → confirms the observed spin. It does NOT test PDB_PROP_GPU_IS_LOST — the precise gap C7-e2 fills.","PRECEDENT FOR C7-e2: the engine ALREADY fabricates early NV_ERR_TIMEOUT in two places — timeoutCheck:483 (GPU in reset) and _checkTimeout:416/438 (API_GPU_ATTACHED_SANITY_CHECK fails → 'return NV_ERR_TIMEOUT'). Every caller must therefore already tolerate spurious/early TIMEOUT. C7-e2 extends an existing engine convention; strong upstreamability argument.","RESCUE-RECHECK SEMANTICS: timeoutCondWait:598-602 'if ((status == NV_ERR_TIMEOUT) && pCondFunc(...)) status = NV_OK' is the engine's own TIMEOUT-specific branch. C7-e2's 'break' before timeoutCheck intentionally bypasses it — on a dead bus this rescue IS the accidental-0xFFFFFFFF-evaluates-true hazard. Bypassing it is the design intent, not a regression.","RESIDUAL (by design): C7-e2 cannot sanitize the case where the cond is accidentally-true on the FIRST while() evaluation (dead bus 0xFFFFFFFF making pCondFunc return NV_TRUE before the loop body ever runs → NV_OK returned). Identical to today's behavior; bounded, not a wedge; downstream mailbox/status reads catch garbage (e.g. kgspBootstrap's mailbox0 != 0 check at gh100:1075).","NULL-SAFETY at insertion point: pTD->pGpu is non-NULL by hard invariant — (1) all 25 call sites are engine HAL code running at/after gpuStateInit, after gpu.c:458; (2) timeoutCondWait:591 already passes pGpu unguarded into pCondFunc which derefs it; (3) the pTimeout==NULL default path calls timeoutSet which derefs pGpu at gpu_timeout.c:285 (IS_GSP_CLIENT) on the GSP-client OSTIMER default. A '(pGpu != NULL) &&' guard mirroring timeoutCheck:483 is free belt-and-braces.","HAZARD (a) RESULT: only NV_ERR_TIMEOUT-specific consumers anywhere downstream of timeoutCondWait are the 5 ICD sites (kernel_falcon_tu102.c:606/661/707/747/785) where '!= NV_ERR_TIMEOUT' GATES issuing the next ICD MMIO command — the TIMEOUT branch returns NV_ERR_INVALID_STATE and touches nothing. Early TIMEOUT on dead bus = skip dead MMIO = beneficial. All other '== NV_ERR_TIMEOUT' hits in gsp/fsp/sec2/bif/falcon (kfspWaitForResponse kern_fsp.c:418, sec2:220/625, kflcnWaitForHalt tu102:353, ga102:294, kernel_gsp.c:426/2866/4004, bif sites) consume hand-rolled gpuCheckTimeout/threadState statuses or GspMsgQueue statuses — NOT timeoutCondWait outputs; unaffected by C7-e2.","HAZARD (b) RESULT: ZERO non-MMIO-completing conds among all 25 timeoutCondWait users — every cond function reads BAR0 (GPU_REG_RD32 / GPU_REG_RD32_UNCHECKED / kflcnRegRead_HAL / REG_RD32-IoAperture / GPU_FLD_TEST_DRF_DEF). No sysmem, no doorbell, no host-CPU-written-flag conds. The break-changes-completes-to-fails hazard is empty for C7-e2.","VERDICT ON DISPATCH QUESTION: ONE edit in timeoutCondWait (gpu_timeout.c, top of the while body at ~line 593, before osSpinLoop) intercepts ALL gpuTimeoutCondWait paths. No TMR/PTIMER second body exists; no additional engine edit needed for C7-e2."],"verdict":"all-safe","needs_handling":["ADVISORY (non-blocking, spec hardening): write C7-e2 as 'if ((pGpu != NULL) && osIsGpuBusLost(pGpu)) { status = NV_ERR_TIMEOUT; break; }' — mirrors timeoutCheck:483's NULL guard; analysis shows pGpu is non-NULL by invariant, so this is defense-in-depth only.","ADVISORY (non-blocking, scope note for C7-e5..e7 hand-rolled-loop edits, NOT e2): the ONE sysmem-completing poll in the GSP path — kgspIssueNotifyOp_GH100's lockless-notify seq poll 'while (seqValue + 1 != *pSeqAddr)' at kernel_gsp_gh100.c:1238 (gpuCheckTimeout loop, reads GSP-DMA-written sysmem) — is the only place where a bus-lost early-break could theoretically convert completes→fails; in practice a dead PCIe link kills GSP's DMA writes too. If this loop is among C7's 3 hand-rolled edit targets, prefer NV_ERR_TIMEOUT (its existing error path) and note the sysmem semantics; if it is not a target, no action.","ADVISORY (test-plan note): C7-e2 measurably changes dead-bus shutdown timing at kgspWaitForProcessorSuspend (kernel_gsp.c:5194, status ignored) and cold-init timing at kfspWaitForSecureBoot_GB202 (5s poll) — both become near-instant; A7 (1200ms) and A6/A12 (3000/2000) budget interactions are improvements, but the apnex.32 live-test assertions on shutdown/init latency should expect the new faster bounds."],"surprises":["timeoutCheck ALREADY fabricates early NV_ERR_TIMEOUT (IN_RESET sanity check + detached check in _checkTimeout TMR branches) — C7-e2 is an extension of an existing engine convention, not a novel behavior class. Strongest upstreamability argument found.","kgspWaitForProcessorSuspend's return value is silently DISCARDED at kgspUnloadRm_IMPL (kernel_gsp.c:5194 — bare call) — today every dead-bus unload burns the full default timeout there before teardown proceeds; C7-e2 bounds it for free (F40/A7 shutdown-arm benefit).","_kfspIsGspTargetMaskReleased deliberately uses GPU_REG_RD32_UNCHECKED to bypass 0xbadf sanity-checking — on a dead bus 0xFFFFFFFF reads as 'mask released', a textbook instance of the accidental-true exit class C7-e2 closes (this wait runs immediately before the #292 lockdown wait every boot).","Turing-named code is live on Blackwell via RPC: kgspLoadAndExecuteHsBinary_GA102 (with kgspWaitForProcessorSuspend + s_dmaPoll cond-waits + kgspExecuteCoreResume_TU102's _kgspIsReloadCompleted wait) is GB202-BOUND and reachable whenever GSP-RM sends rpc load_exec_hs_binary — 3 'legacy' cond-wait sites are conditionally live on our chip.","Dead-but-bound combination: kfspSendPacket_GB100 (GB202-bound) contains an MNOC branch, but gpuMnocMboxSend on GB202 resolves to a stub AND the branch needs a regkey (NV_REG_STR_RM_FSP_USE_MNOC_CPU) — the 3 MNOC cond-wait sites are double-gated off on GB202.","kfspWaitForSecureBoot failure marks the device for reset via gpuMarkDeviceForReset → _gpuSetResetRequiredState (SW-state only, no HW touch) — benign on dead bus, and a useful pre-existing 'reset required' breadcrumb the C7 funnels could later key on.","GAP-7 spot-scan (scan-level confidence, not the full GAP-7 audit): every getProperty(PDB_PROP_GPU_IS_LOST) consumer found in src/ (journal.c:565/1089/2929, nv_debug_dump.c:282, kernel_gsp.c:315/2814, kern_fsp_gh100.c:638, mem_mgr_pwr_mgmt.c:257, gpu.c:7427, gpu_access.c:838, kernel_nvlink.c:1159/1213) is a PROTECTIVE SKIP (skip dump/RPC/HW-touch when TRUE). PDB-unset-but-os_pci-dead degrades to 'attempts the access, which the osIsGpuBusDead MMIO layer short-circuits' — no site found that frees state or avoids a hang ONLY when PDB==TRUE. Supports C7's claim that the PDB-never-set end-state is tolerable, pending the dedicated GAP-7 pass.","The 'TIMEOUT + recheck cond → NV_OK' rescue pattern exists in THREE more hand-rolled copies (kfspWaitForResponse kern_fsp.c:418, ksec2 kernel_sec2.c:220/625) — same accidental-0xFFFFFFFF rescue hazard class as timeoutCondWait:598, relevant if C7's hand-rolled-loop list is ever extended (FSP/SEC2 response polls are MMIO-read conds, so on dead bus the rescue can fire on garbage).","This checkout (/root/open-gpu-kernel-modules) already carries some fork-side patches (e.g. journal.c:2929 'Crash-safety guard' IS_LOST short-circuit comment) but osIsGpuBusLost/os_pci_is_disconnected do NOT exist in it yet — C7-e1 must land before e2 compiles; reachability/dispatch conclusions are unaffected (gpu_timeout.c and all 25 call sites are vanilla)."]},{"area":"GAP-6 caller audit — GSP family (5 files, 10 gpuTimeoutCondWait sites) + engine-dispatch verification + GAP-7 PDB-reader sweep, against C7 spec §3 of design-2026-06-06-292-redesign-C7-A13prime-A14.md. Tree: /root/open-gpu-kernel-modules/src/nvidia (composed tree — C5/nv-gpu-lost.h project patches already present; the 10 cond-wait sites themselves are unmodified vendor code).","callers":["src/kernel/gpu/gsp/arch/hopper/kernel_gsp_gh100.c:133 | _kgspWaitForAsserted (BAR0 MMIO: NV_PGSP_FALCON_ENGINE RESET_STATUS) | `if (status != NV_OK) { NV_PRINTF(\"Timed out waiting for GSP falcon reset to assert\"); DBG_BREAKPOINT(); }` then falls through; kgspResetHw_GH100 returns final status; external consumers all NV_ASSERT_OK_OR_RETURN (gh100:726 _kgspBootstrapGspFmc — not taken on GB202 since FSP present; kflcnReset chain from falcon_ga102.c:334 / falcon_tu102.c:465) | NO — generic !=NV_OK at every level | NO — pure MMIO | YES (via kflcnReset_HAL on GSP falcon: PM/HS-binary resume paths; NORMAL boot resets via FSP) | SAFE","src/kernel/gpu/gsp/arch/hopper/kernel_gsp_gh100.c:144 | _kgspWaitForDeasserted (same MMIO reg, inverse polarity) | identical pattern to :133 (`!= NV_OK` print + DBG_BREAKPOINT, status returned) | NO | NO — MMIO | YES (same chain as :133) | SAFE","src/kernel/gpu/gsp/arch/hopper/kernel_gsp_gh100.c:620 | _kgspSpdmBootedOrFmcError (MMIO MAILBOX0/MAILBOX1) | `if (status != NV_OK) { NV_PRINTF(\"Timeout waiting for SPDM Responder...\"); goto exit; }`; exit dumps FSP debug state, returns; caller gh100:1029 NV_ASSERT_OK_OR_RETURN | NO | NO — MMIO | NO — gated `pSpdm != NULL && PDB_PROP_SPDM_ENABLED` (gh100:1026); CC/SPDM off on this host | SAFE (config-dead here; C7-e2 still closes its ==0-adjacent trap for other configs)","src/kernel/gpu/gsp/arch/hopper/kernel_gsp_gh100.c:662 | _kgspFalconMailbox0Cleared (MMIO MAILBOX0 == 0 — the polarity-trap site: dead-value 0xFFFFFFFF never completes) | `if (status != NV_OK) { NV_PRINTF; goto exit; }` same exit as :620 | NO | NO — MMIO | NO — same SPDM gate | SAFE","src/kernel/gpu/gsp/arch/hopper/kernel_gsp_gh100.c:1050 | _kgspLockdownReleasedOrFmcError (MMIO MAILBOX0/MAILBOX1/HWCFG2; memdescGetPhysAddr is CPU-side) | `if (status != NV_OK) { prints + kgspFmcReportErrorCode_HAL + kfspDumpDebugState_HAL (dead reads short-circuit, bounded) + mailbox prints; return status; }` -> kgspBootstrap_GH100 -> kernel_gsp.c:4798 `if (status != NV_OK ...)` generic: bootAttempts++ + bounded retry (`while (bRetry && bootAttempts < maxGspBootAttempts)` kernel_gsp.c:5077/242) or gpu_suspend.c:250 generic store+goto done | NO — generic at every level (the 1068-1078 NV_ERR_NOT_READY branches are FMC-error paths, also generic upstream) | NO — MMIO | YES — the #292 netcon2 site itself (NORMAL boot + resume; GB202 dispatches kgspBootstrap_GH100) | SAFE — confirms the design's §S claim; dead-bus retry loop is bounded and C7-e2 makes each retry fast","src/kernel/gpu/gsp/arch/hopper/kernel_gsp_gh100.c:1271 | _kgspHasCcCleanupFinished (MMIO MAILBOX1 == SUCCESS||FAILURE sentinel) | `if (status != NV_OK) { NV_PRINTF(\"CC secret cleanup failed due to timeout!\"); goto exit; }`; exit posts ack write (harmless posted write on dead bus); caller kernel_gsp.c:5190 DISCARDS return, gated `gpuIsCCFeatureEnabled(pGpu)` | NO | NO — MMIO | NO — CC off on this host | SAFE","src/kernel/gpu/gsp/arch/blackwell/kernel_gsp_gb100.c:83 | _kgspWaitForResetAccess (MMIO RESET_PRIV_LEVEL_MASK; explicit RMTIMEOUT with GPU_TIMEOUT_FLAGS_TMR) | `if (status != NV_OK) { NV_PRINTF; DBG_BREAKPOINT(); }` then status DISCARDED — unconditionally `return kgspResetHw_GH100(...)` | NO | NO — MMIO | YES — GB202 wires kgspResetHw_GB100 (g_kernel_gsp_nvoc.c:1158 else-branch covers all GB10x/GB20x) | SAFE — timeout already deliberately non-fatal; C7-e2 only shortens the TMR-dead-PTIMER edge","src/kernel/gpu/gsp/arch/ampere/kernel_gsp_falcon_ga102.c:92 | s_dmaPollCondFunc (MMIO DMATRFCMD masked compare) | `if (status != NV_OK) { NV_PRINTF; DBG_BREAKPOINT(); return status; }` -> s_dmaTransfer_GA102 -> NV_ASSERT_OK_OR_RETURN / NV_CHECK_OK_OR_RETURN(LEVEL_SILENT) in kgspLoadAndExecuteHsBinary_GA102; RPC-event dispatcher (kernel_gsp.c:1596,1613-1623) prints-and-soldiers-on | NO | NO — MMIO | PARTIAL — kgspExecuteHsFalcon on GB202 = _5baef9 stub (GA102 mask is GA102..AD107 only); reachable only via kgspLoadAndExecuteHsBinary_GA102 (wired on GB202, GSP RPC GSP_LOAD_EXEC_HS_BINARY, PM/suspend path) | SAFE","src/kernel/gpu/gsp/arch/turing/kernel_gsp_tu102.c:1155 | _kgspIsProcessorSuspended (MMIO MAILBOX0 & 0x80000000 — dead-value 0xFFFFFFFF evaluates TRUE, so on a dead bus this completes NV_OK on first cond eval BEFORE C7-e2 runs) | direct `return gpuTimeoutCondWait(...)`; consumers: kernel_gsp.c:5194 kgspUnloadRm_IMPL — return DISCARDED, teardown proceeds; falcon_ga102.c:325-331 + falcon_tu102.c:531-537 `if (status != NV_OK) { read mailbox0 (dead-safe) + print; return status; }` generic | NO | NO — MMIO | YES — kernel_gsp.c:5194 runs on EVERY unload (shutdown/F40 arm + suspend) on GB202 (TU102 impl wired); GenericBootloader consumer NOT wired on GB202 | SAFE (both with and without C7: accidental-TRUE completion or marker break, teardown continues either way)","src/kernel/gpu/gsp/arch/turing/kernel_gsp_falcon_tu102.c:473 | _kgspIsReloadCompleted (MMIO GPU_REG_RD32 NV_PGC6_BSI_SECURE_SCRATCH_14 handoff field) | `status = gpuTimeoutCondWait(...)`; then reads SEC2 MAILBOX0 (dead-safe, pre-branch); `if ((status != NV_OK) || (secMailbox0 != NV_OK)) { NV_PRINTF; return NV_ERR_TIMEOUT; }` — PRODUCES NV_ERR_TIMEOUT itself, consumes generically; callers: falcon_ga102.c:401 `return kgspExecuteCoreResume_HAL` -> RPC-event print-and-continue; falcon_tu102.c:565 NV_ASSERT_OK_OR_RETURN (GenericBootloader, not wired GB202) | NO (generic; the NV_ERR_TIMEOUT here is production not consumption) | NO — MMIO | YES on GB202 (kgspExecuteCoreResume -> TU102 impl, via HS-binary PM path) | SAFE"],"engine_findings":["SINGLE ENGINE CONFIRMED: gpuTimeoutCondWait is a plain macro (inc/kernel/gpu/gpu_timeout.h:147 `#define gpuTimeoutCondWait(g,a,b,t) timeoutCondWait(&(g)->timeoutData, t, a, b, __LINE__)`) — static dispatch, NOT an NVOC/HAL vtable; TIMEOUT_DATA has no function pointers. timeoutCondWait (src/kernel/gpu/gpu_timeout.c:572-608) is the ONLY cond-wait body: case-insensitive grep for 'condwait' across src/nvidia, src/common, kernel-open finds NO tmrApiCondWait and no second engine. All 4 RMTIMEOUT flag classes (OSTIMER/OSDELAY/TMR/TMRDELAY) flow through the SAME `while (!pCondFunc(...))` loop — flags only select the clock inside _checkTimeout. C7-e2's single edit therefore intercepts ALL 25 gpuTimeoutCondWait call sites tree-wide, including the TMR-flagged ones (gh100:133/144, gb100:83).","ENGINE HAS NO EXISTING BUS-LOST ESCAPE: timeoutCheck (gpu_timeout.c:483) fast-returns NV_ERR_TIMEOUT on API_GPU_IN_RESET_SANITY_CHECK, but that macro (generated/g_gpu_nvoc.h:5873-5878) tests only FULLCHIP/SECONDARY_BUS/GC6/SBR-pending — NOT PDB_PROP_GPU_IS_LOST, NOT os_pci. Also precedent: the engine already returns NV_ERR_TIMEOUT on GPU-state (non-clock) grounds, supporting C7-e2's choice of NV_ERR_TIMEOUT.","LOOP-ENTRY NUANCE: the cond is evaluated BEFORE C7-e2's check (`while (!pCondFunc(...))`), so conds that read dead-value-TRUE (0xFFFFFFFF) — _kgspIsProcessorSuspended, _kgspLockdownReleasedOrFmcError (mailbox0!=0), _kgspSpdmBootedOrFmcError, possibly _kgspWaitForAsserted — return NV_OK on a dead bus without C7-e2 ever firing. Audited all 10: every such caller fails closed afterwards via post-wait dead-value-safe error checks (e.g. gh100:1068-1078 mailbox!=0 -> NV_ERR_NOT_READY). No cond fn can HANG: all are MMIO reads, short-circuited on os_pci alone (os.c osIsGpuBusDead).","C7-e2's break correctly skips the engine's one-extra-cond-recheck courtesy (gpu_timeout.c:598-602) — that recheck exists for OS-preemption races, meaningless on a dead bus.","TIMEOUT-SPECIFIC BRANCHES, FULL SWEEP: tree-wide grep of ==/!=/case NV_ERR_TIMEOUT found NONE downstream of the 10 audited sites. The only GSP-family TIMEOUT-specific consumers are kernel_gsp.c:426 (_kgspRpcSendMessage: increments timeout count + rate-limits prints on TIMEOUT||BUSY_RETRY from GspMsgQueueSendCommand — benign/beneficial for C7-e6's NV_ERR_TIMEOUT) and kernel_gsp.c:2866 (inside _kgspRpcRecvPoll's classification — C7-e3 returns pre-loop, never reaches it on a lost bus). kern_fsp.c:418 and kernel_sec2.c:220/625 are the standard timeout-vs-cond-met race recheck (status==TIMEOUT && cond -> NV_OK) in OTHER families' own loops, not consumers of GSP statuses.","STATUS-CODE SURFACE vs C5 MACROS: NV_ASSERT_OR_GPU_LOST (inc/kernel/gpu/nv-gpu-lost.h:74) tolerates only NV_OK/NV_ERR_GPU_IN_FULLCHIP_RESET/NV_ERR_GPU_IS_LOST — NOT NV_ERR_TIMEOUT. The only co-occurrence with a cond-wait-derived status is kernel_gsp_tu102.c:640 (kgspTeardown_TU102 kflcnReset) — pre-Hopper Booter world, NOT dispatched on GB202 (teardown -> GH100). And pre-C7 a dead bus produced the same NV_ERR_TIMEOUT there via natural timeout, so C7 changes latency only. Cosmetic, other-arch, no action needed.","GB202 HAL WIRING (generated/g_kernel_gsp_nvoc.c): kgspBootstrap->GH100 (789-803), kgspResetHw->GB100 (1140-1158), kgspWaitForProcessorSuspend->TU102 (1539-1549), kgspCheckGspRmCcCleanup->GH100 but CC-gated at call site (1979-1991, kernel_gsp.c:5188), kgspExecuteCoreResume->TU102 (1998-2010), kgspExecuteHsFalcon->_5baef9 stub (1518-1532), kgspLoadAndExecuteGenericBootloader->_5baef9 stub (2032-2043), kgspLoadAndExecuteHsBinary->GA102 (2050-2062).","NOT INTERCEPTED BY C7-e2 (hand-rolled gpuCheckTimeout loops in the GSP family BEYOND the design's 3 in C7-e6): (1) kgspIssueNotifyOp_GH100, kernel_gsp_gh100.c:1238 — SYSMEM seq-counter poll after VF doorbell write (a genuine non-MMIO-completing cond, but in a hand-rolled loop, not timeoutCondWait); flags=0 -> OSTIMER on GSP client -> CPU-clock bounded (~4s default), callers kern_gmmu_gh100.c:583/613 return status generically. (2) RM-state-monitor dump loop, kernel_gsp.c:~3990-4010 — sysmem seq poll, OSTIMER defaultus/10 + BYPASS_THREAD_STATE, prints + returns void on timeout. Both are bounded-latency on a dead bus (clock is CPU-side), no wedge — they merely burn their full budget instead of breaking early. Optional C7-e6 extensions only; NOT required for #292.","GAP-7 SWEEP (direct PDB_PROP_GPU_IS_LOST getProperty readers, os_pci-set-but-PDB-unset end-state): no teardown/cleanup/re-open code depends on PDB==TRUE for CORRECTNESS on this config. kernel_gsp.c:315 + 2814 are exactly C7-e3/e4's OR-in anchors (verified present with the bPollingForRpcResponse clear). vgpu/rpc.c:1854/2097/11530, journal.c:565/1089/2934/3133, nv_debug_dump.c:282, gpu_protobuf.c:85, gpu.c:7427, kernel_rc_watchdog_callback.c:155 are all skip-guards whose miss yields bounded dead-value reads or fast-failing RPCs (C7-e3/e4/e6 cover the waits) — latency/noise only, no leak, no hang, no must-skip HW touch. gpu_access.c:838 (_regCheckReadFailure) runs gpuSanityCheck classification on dead reads — bounded prints. kernel_nvlink.c — no NVLink here. TWO residuals, neither in the #292 window: (a) kern_fsp_gh100.c:631-644 — the project's v4-G9 EMEM guard requires dead-value AND PDB==TRUE; with PDB unset it won't fire and the downstream arithmetic-invariant NV_ASSERT_OR_RETURN fires instead (noisy assert + generic error unwind — fails closed, no hang); (b) mem_mgr_pwr_mgmt.c:257 — FBSR DESTROY-vs-RESTORE bifurcation keys on PDB; PDB-unset dead-bus PM-resume would attempt RESTORE and fail via generic NV_CHECK_OK_OR_GOTO (PM path only, and resume already aborts earlier at gpu_suspend.c:250 when kgspBootstrap fails)."],"surprises":["Only 3 of the 6 gh100 sites are live on this host: 133/144 (reset FSM) and 1050 (lockdown — the #292 site). 620/662 are SPDM-gated (PDB_PROP_SPDM_ENABLED, CC-only) and 1271 is gpuIsCCFeatureEnabled-gated — config-dead but correct to cover via the shared C7-e2 chokepoint anyway.","_kgspIsProcessorSuspended evaluates ACCIDENTALLY TRUE on a dead bus (0xFFFFFFFF & 0x80000000), so the every-unload wait at kernel_gsp.c:5194 never spins on a dead bus even pre-C7, and its return is discarded outright — explains why the F40 shutdown arm never wedged at this site.","kgspResetHw_GB100 (gb100:83) deliberately DISCARDS its cond-wait status (print + DBG_BREAKPOINT, then proceeds to GH100 reset regardless) — C7-e2's early NV_ERR_TIMEOUT there is behaviorally invisible downstream.","kgspExecuteCoreResume_TU102 normalizes ANY failure (including secMailbox0 dead-read garbage) to NV_ERR_TIMEOUT at falcon_tu102.c:478-482 — vendor precedent for exactly the status code C7-e2 chose.","A genuine non-MMIO-completing cond DOES exist in the GSP family — kgspIssueNotifyOp_GH100's sysmem seq poll (gh100:1238) — but it is a hand-rolled gpuCheckTimeout loop, NOT a timeoutCondWait caller, is OSTIMER-bounded (~4s), and is absent from the design's C7-e6 list of 3. Hazard (b) is therefore nil for the actual gpuTimeoutCondWait population: all 10 audited conds are pure MMIO.","OPTIONAL (non-blocking) follow-ups surfaced, for the C7 backlog not the apnex.32 gate: (1) one-line widening of the kern_fsp_gh100.c:638 G9 guard (and any sibling `dead-value && getProperty(PDB_PROP_GPU_IS_LOST)` guards) to osIsGpuBusLost(pGpu) once C7-decl lands — kills the assert-noise path in the PDB-unset end-state; (2) C7-e6-style marker breaks in kgspIssueNotifyOp_GH100 and the kernel_gsp.c RM-state-dump loop — latency-only wins.","The working tree at /root/open-gpu-kernel-modules is already partially composed (nv-gpu-lost.h, C5 NV_ASSERT_OR_GPU_LOST guards, and the C7-e3 anchor comment block at kernel_gsp.c:2805-2822 are project text) — the audit's anchors therefore match the design doc's source-verified line numbers, but diff-vs-vendor should be kept in mind when porting C7-e2 line ~591 (verified: insertion point `while (!pCondFunc(...))` at gpu_timeout.c:591 is intact vendor code)."],"verdict":"all-safe","needs_handling":[]},{"area":"GAP-6 caller audit — FSP (kern_fsp.c, kern_fsp_gh100.c x2, kern_fsp_gb202.c, kern_fsp_gb100.c) + BIF (kernel_bif_ad102.c); engine-dispatch verification for C7-e2; in-scope GAP-7 sweep of the same files. Tree audited: /root/open-gpu-kernel-modules (NOTE: tree is already apnex-patched — carries v4 guards + inc/kernel/gpu/nv-gpu-lost.h).","callers":["/root/open-gpu-kernel-modules/src/nvidia/src/kernel/gpu/fsp/kern_fsp.c:364 | _kfspWaitForCanSend -> kfspCanSendPacket_HAL = CanSendPacket_GB100 -> _GH100 (cmdq/msgq HEAD==TAIL MMIO reads, kern_fsp_gh100.c:243-259) | kern_fsp.c:365 `if (status != NV_OK) { NV_PRINTF(...\"Timed out waiting for FSP queues to be empty\"); } return status;` then sole consumer kfspSendPacket_GH100 (kern_fsp_gh100.c:701) `if (status != NV_OK) return NV_ERR_INSUFFICIENT_RESOURCES;` | TIMEOUT-specific: NO (status discarded, replaced wholesale) | non-MMIO cond: NO (FSP writes the regs but RM consumes via BAR0 read) | reachable: YES on 5090 — GB202 wires kfspSendPacket_GB100 which falls back to the GH100 EMEM path because PDB_PROP_KFSP_USE_MNOC_CPU defaults FALSE (regkey-gated, kern_fsp.c:166-172); runs every cold-init COT send | VERDICT: SAFE. Dead-bus nuance: all-Fs reads make HEAD==TAIL -> cond accidentally TRUE in the while-condition BEFORE C7-e2's in-loop check -> returns NV_OK exactly as today; no behavior change, recovery falls to the next covered poll (kfspWaitForResponse kern_fsp.c:411 = C7-e6).","/root/open-gpu-kernel-modules/src/nvidia/src/kernel/gpu/fsp/arch/hopper/kern_fsp_gh100.c:828 | _kfspWaitBootCond_GH100 (THERM_I2CS_SCRATCH 31:0 == 0xFF, MMIO) | kern_fsp_gh100.c:830 `if (status != NV_OK) { NV_ASSERT_OK(gpuMarkDeviceForReset(pGpu)); NV_ERROR_LOG(... 5x GPU_REG_RD32 ...); } return status;` consumers kfspPrepareBootCommands_GH100:1332 (`statusBoot != NV_OK -> goto failed`) and kbifDoFunctionLevelReset_GH100 (`!= NV_OK -> DBG_BREAKPOINT + goto exit`) | TIMEOUT-specific: NO (all generic !=NV_OK) | non-MMIO cond: NO | reachable: NO on 5090 — HAL wired ChipHal GH100 only (g_kern_fsp_nvoc.c:488-490); GB202 gets kfspWaitForSecureBoot_GB202 | VERDICT: SAFE (dead code here; dead-bus all-Fs != 0xFF so e2 would fast-fail it; failure path is bounded scratch-RMW + reg-read logging).","/root/open-gpu-kernel-modules/src/nvidia/src/kernel/gpu/fsp/arch/hopper/kern_fsp_gh100.c:1158 | _kfspIsGspTargetMaskReleased (GPU_REG_RD32_UNCHECKED of NV_PGSP+FALCON_HWCFG2; TRUE iff reg!=0 && (reg&0xFFFFFF00)!=0xBADF4100; pTimeout=NULL -> GPU_TIMEOUT_DEFAULT) | returns status directly; sole consumer kgspBootstrap_GH100 (kernel_gsp_gh100.c:986-1000) `if (status != NV_OK) { NV_PRINTF(...); kgspFmcReportErrorCode_HAL(...); kfspDumpDebugState_HAL(...); return status; }` | TIMEOUT-specific: NO | non-MMIO cond: NO | reachable: YES on 5090 — wired GH100+GB1xx+GB202..GB207 (g_kern_fsp_nvoc.c:630-636) and kgspBootstrap_GH100 is GB202's bootstrap (g_kernel_gsp_nvoc.c:801-804); runs EVERY GSP boot, inside the #292 RmInitAdapter path | VERDICT: SAFE. Dead-bus: 0xFFFFFFFF -> cond accidentally TRUE before e2 can fire -> NV_OK, identical to today; the immediately-following lockdown/RPC polls are C7-covered and fail fast.","/root/open-gpu-kernel-modules/src/nvidia/src/kernel/gpu/fsp/arch/blackwell/kern_fsp_gb202.c:80 | _kfspWaitBootCond_GB202 (THERM_I2CS_SCRATCH field 31:0 == 0x000000FF SUCCESS, dev_therm_addendum.h:28-29; OSTIMER|BYPASS_THREAD_STATE, 5s scaled) | gb202.c:82 `if (status != NV_OK) { NV_ASSERT_OK(gpuMarkDeviceForReset(pGpu)); NV_ERROR_LOG(...status + 5x GPU_REG_RD32...); kfspDumpDebugState_HAL(...= _GB100: ~48 indirect CMS2 WR/RD pairs + mailbox reads, all bounded); } return status;` -> kfspPrepareBootCommands_GH100:1330-1337 `statusBoot != NV_OK -> \"FSP secure boot partition timed out\" -> status=statusBoot; goto failed` -> RmInitAdapter fails | TIMEOUT-specific: NO | non-MMIO cond: NO | reachable: YES — THE hot site, every cold-init GFW boot wait on this 5090 (g_kern_fsp_nvoc.c:492-494) | VERDICT: SAFE, and C7-e2 is a strict improvement: dead-bus all-Fs (31:0 field = 0xFFFFFFFF != 0xFF) means cond never accidentally completes -> today spins the full 5s budget; e2 converts to prompt NV_ERR_TIMEOUT. Failure path fully bounded on a dead bus: gpuMarkDeviceForReset = scratch-bit read(all-Fs -> prevState TRUE -> state-changed callback skipped)+dropped write; all logging reads short-circuit.","/root/open-gpu-kernel-modules/src/nvidia/src/kernel/gpu/fsp/arch/blackwell/kern_fsp_gb100.c:95 | _kfspWaitBootCond_GB100 (same 31:0==0xFF THERM scratch pattern, gb100 addendum identical) | gb100.c:97 same generic `if (status != NV_OK) { gpuMarkDeviceForReset + NV_ERROR_LOG reg reads + kfspDumpDebugState_HAL }` THEN falls through to FUSE_ERROR_CHECK block (more bounded reg reads; may overwrite status with NV_ERR_SECURE_BOOT_FAILED if PDB_PROP_KFSP_FSP_FUSE_ERROR_CHECK_ENABLED — still generic error-out); return status | TIMEOUT-specific: NO | non-MMIO cond: NO | reachable: NO on 5090 — wired GB100|GB102|GB110|GB112 only (g_kern_fsp_nvoc.c:496-499) | VERDICT: SAFE (other-arch dead code; same bounded-fail shape as GB202).","/root/open-gpu-kernel-modules/src/nvidia/src/kernel/gpu/bif/arch/ada/kernel_bif_ad102.c:73 | _kbifPreOsCheckErotGrantAllowed_AD102 (GPU_REG_RD32 NV_PBUS_SW_SCRATCH(0), tests _ALLOW bit 5:5 == 1; pTimeout=NULL -> default) | ad102.c:74 `if (status != NV_OK) { NV_PRINTF(...\"Timed out waiting for preOs to grant access to EEPROM\"); } return status;` sole consumer kgspExecuteVbiosRead path (kernel_gsp_vbios_tu102.c:482-489) `if (status != NV_OK) { NV_PRINTF(...); goto out; }` | TIMEOUT-specific: NO | non-MMIO cond: NO (ERoT/preOs firmware writes the scratch reg, but completion is observed via MMIO read — bus dead = no legitimate completion) | reachable: NO on 5090 — kbifPreOsGlobalErotGrantRequest wired AD102-AD107 only; GB202 gets the _ac1694 inline stub returning NV_OK (g_kernel_bif_nvoc.c:1056-1064, g_kernel_bif_nvoc.h:1746-1748) | VERDICT: SAFE. Dead-bus (on Ada): function early-returns NV_OK at the line-65 pre-check (all-Fs has ALLOW=1) before the cond-wait even runs — e2 unreached, unchanged."],"engine_findings":["DISPATCH CONFIRMED SINGLE-BODIED: gpuTimeoutCondWait is a plain macro `#define gpuTimeoutCondWait(g,a,b,t) timeoutCondWait(&(g)->timeoutData, t, a, b, __LINE__)` (gpu_timeout.h:147). There is NO TIMEOUT_DATA vtable/pFn (struct at gpu_timeout.h:76-92 holds only scalars + pGpu), NO tmrApiCondWait anywhere in the tree (tree-wide grep: zero hits), and no second cond-wait body — all 22 tree-wide gpuTimeoutCondWait call sites funnel through the one loop at gpu_timeout.c:572-608. C7-e2's single edit intercepts ALL cond-waits.","ALL GPU_TIMEOUT_FLAGS_* TIME-SOURCE VARIANTS COVERED: OSTIMER/OSDELAY/TMR/TMRDELAY/thread-state dispatch happens INSIDE timeoutCheck/_checkTimeout (gpu_timeout.c:330-465, 471-552), which the cond-wait loop calls at :595. The TMR-based variant is a flags branch of _checkTimeout, not a separate engine. pTimeout==NULL callers (sites gh100:1158, ad102:73) get GPU_TIMEOUT_DEFAULT built at :585-589 — same loop, covered.","PRECEDENT FOR e2's STATUS CHOICE: timeoutCheck:483-484 already returns NV_ERR_TIMEOUT unconditionally when API_GPU_IN_RESET_SANITY_CHECK(pGpu) — every cond-wait caller must already tolerate a spontaneous early NV_ERR_TIMEOUT unrelated to elapsed time. e2 matches existing engine semantics; no audited caller branches on NV_ERR_TIMEOUT specifically (all six consume `!= NV_OK` generically; kfspSendPacket_GH100 even discards it for NV_ERR_INSUFFICIENT_RESOURCES).","ORDER-OF-EVALUATION CAVEAT (placement ~591): pCondFunc is evaluated in the while-CONDITION before the loop body, so conds whose all-Fs dead-bus read evaluates TRUE bypass e2 entirely and return NV_OK. In the audited set that is 3 of 6 sites (_kfspWaitForCanSend HEAD==TAIL equality; _kfspIsGspTargetMaskReleased !=0&&!=0xBADF41xx; _kbifPreOsCheckErotGrantAllowed bit5=1). This is IDENTICAL to pre-C7 behavior (these sites never spin on a dead bus), so no regression — but e2 should not be advertised as converting every cond-wait to prompt-fail; accidental-completers still exit NV_OK and rely on the next covered poll. The conservative placement is correct: hoisting the bus-lost check above the cond evaluation is what would create hazard-(b) completes->fails flips.","e2's `break` with status=NV_ERR_TIMEOUT skips the engine's last-chance rescue recheck (gpu_timeout.c:598-602, TIMEOUT + cond-now-true -> NV_OK). Irrelevant for all audited sites: every FSP/BIF cond is a BAR0 MMIO read; none can legitimately complete via sysmem/doorbell after bus loss. No hazard-(b) instance exists in the FSP/BIF area.","GB202 FSP sends use the GH100 EMEM path on this rig: kfspSendPacket_GB100 (kern_fsp_gb100.c:284-294) only takes the MNOC branch under PDB_PROP_KFSP_USE_MNOC_CPU, which is FALSE absent the NV_REG_STR_RM_FSP_USE_MNOC regkey. Consequence: the MNOC cond-wait sites at kern_gpu_gb100.c:1578/1620/1690 (gpuMnocMboxSend/Recv) are NOT reachable on the 5090 — but they are gpuTimeoutCondWait callers outside this audit's file list; flag to the orchestrator if no other agent owns kern_gpu_gb100.c."],"verdict":"needs-handling","needs_handling":["[GAP-7, in audited file, REACHABLE on 5090] v4 guard G9 at /root/open-gpu-kernel-modules/src/nvidia/src/kernel/gpu/fsp/arch/hopper/kern_fsp_gh100.c:637-643 is defeated by the C7 freed-worker end-state (os_pci marker set, PDB_PROP_GPU_IS_LOST never set): its predicate is `if (reg32 == NV_GPU_BUS_DEAD_VALUE_U32 && pGpu->getProperty(pGpu, PDB_PROP_GPU_IS_LOST))` — an AND on the PDB property. With PDB unset the guard falls through to junk DRF math; the dropped writes then make `NV_ASSERT_OR_RETURN((ememOffsetEnd - ememOffsetStart) == wordsWritten, NV_ERR_INVALID_STATE)` (kern_fsp_gh100.c:672) fire mid-HW-touch (assert noise / DBG breakpoint on debug builds) — and kfspSendPacket_GH100:713 IGNORES _kfspWriteToEmem's return value entirely, so the dead-bus send still 'succeeds' and recovery defers to the response-timeout loop (C7-e6). Bounded either way (no hang), but G9's clean NV_ERR_GPU_IS_LOST classification — its whole purpose — is lost. CONCRETE DELTA: as part of C7, change G9's second conjunct from the PDB getProperty to the new `osIsGpuBusLost(pGpu)` predicate (os_pci OR PDB), i.e. `if (reg32 == NV_GPU_BUS_DEAD_VALUE_U32 && osIsGpuBusLost(pGpu))`. One line, file is already apnex-owned. Audit the same `&& PDB_PROP_GPU_IS_LOST` pattern at the other v4-guard files (thread_state.c, kernel_gsp.c, rpc.c — outside this audit's scope) for the identical defeat.","All SIX gpuTimeoutCondWait call sites themselves: SAFE as-is — zero NV_ERR_TIMEOUT-specific branches, zero non-MMIO-completing conds, all failure paths bounded on a dead bus. No per-caller C7 deltas required."],"surprises":["The working tree /root/open-gpu-kernel-modules is NOT vanilla — it already carries apnex v4 guards (grep 'v4 guard': thread_state.c, kern_fsp_gh100.c, kernel_gsp.c, rpc.c) and inc/kernel/gpu/nv-gpu-lost.h (NV_GPU_BUS_DEAD_VALUE_U32=0xFFFFFFFF, NV_GPU_LOST_LOG_ONCE). Verdicts above account for the patched code, and the G9 finding only exists because of it.","kern_fsp.c:364 IS reachable on the 5090 despite GB202 nominally using the GB100 send HAL: the MNOC path is regkey-gated (PDB_PROP_KFSP_USE_MNOC_CPU default FALSE) so GB202 falls back to kfspSendPacket_GH100 -> kfspPollForCanSend. Matches design-doc row 3's 'YES (FSP send)'.","kfspWaitForGspTargetMaskReleased_GH100 lives in the hopper/ file but is HAL-wired for ALL Blackwell including GB202 (g_kern_fsp_nvoc.c:630-636) — file location misleads; it runs every GSP boot on this 5090, inside the exact #292 kgspBootstrap path.","3 of the 6 conds accidentally evaluate TRUE on dead-bus all-Fs reads and therefore bypass C7-e2 (cond runs in the while-condition before the in-loop check). Not a regression — identical to today — but 'every poll-site short-circuited' should be stated as 'every SPINNING poll-site': accidental-completers exit NV_OK and the next covered poll provides the fast-fail.","kfspSendPacket_GH100:713 discards _kfspWriteToEmem_GH100's return status (pre-existing upstream sloppiness): even when G9 or the EMEM sanity assert fails, the packet send reports NV_OK; first detection moves to kfspWaitForResponse (kern_fsp.c:411, C7-e6 territory).","The audited BIF AD102 EROT site is double-protected against dead-bus spin by accident: on all-Fs reads the function early-returns NV_OK at the ALLOW pre-check (line 65) before ever reaching its cond-wait — and it is Ada-only anyway (GB202 gets an inline NV_OK stub)."]},null,{"area":"GAP-7 teardown dependency trace (os_pci-dead, PDB_PROP_GPU_IS_LOST unset) + GAP-6 engine-dispatch/caller audit for C7 (apnex.32 pre-build)","callers":["[PDB-reader] os.c:1895 | osIsGpuBusDead | PDB OR'd with os_pci_is_disconnected | n/a | n/a | yes (every MMIO read) | SAFE — os_pci alone short-circuits; PDB-unset irrelevant","[PDB-reader] os.c:1935 | cleanupGpuLostStateAtomic idempotence gate | PDB unset means primitive WOULD run if invoked | n/a | n/a | yes | SAFE — correct semantics","[PDB-reader] os.c:2085 | osDevReadReg032 post-read DETECTOR_MMIO_DEAD gate | never reached: early return at os.c:2050 fires first when os_pci set | n/a | n/a | no (pre-empted) | SAFE/moot — this pre-emption IS why PDB stays unset (confirmed premise)","[PDB-reader] osapi.c:425 | RmLogGpuCrash G6 gate (os_pci AND PDB) | PDB unset → gate doesn't fire → rcdbAddRmGpuDump proceeds | requires BOTH markers | dump RPCs bounded by C7-e3/e4 (µs each); journal.c:1089 bGpuAccessible self-falsifies via dead PMC_BOOT_0 read | NOT reachable in failed-init unwind (no XID/RC path runs) | SAFE — bounded+noisy at worst, unreachable in sequence","[PDB-reader] osapi.c:441-451 | RmLogGpuCrash EEH re-eval | PDB unset + IS_CONNECTED true → re-eval block SKIPPED (no raw PMC_BOOT_0 read) | n/a | n/a | same as above | SAFE","[PDB-reader] gpu.c:7427 | gpuLogOobXidMessage_KERNEL | PDB unset → that clause false, but !pGpu->gspRmInitialized (true in failed-init window) returns early anyway; post-init losses bounded by C7 | one of 9 OR'd skip conditions | RPC bounded by C7-e3/e4 | no in window | SAFE","[PDB-reader] gpu_access.c:838 | _regCheckReadFailure | PDB unset → runs gpuSanityCheck (extra cfg-space+MMIO reads, all bounded) → classifies BAD_READ_GPU_OFF_BUS (correct) | no | reads short-circuited | rare (regCheckRead032 verif path) | SAFE","[PDB-reader] mem_mgr_pwr_mgmt.c:257 | memmgrRestorePowerMgmtState_KERNEL | PDB TRUE→FBSR_OP_DESTROY, unset→FBSR_OP_RESTORE attempted; fbsr ops fail on dead bus, done: label still frees FBSR memory (memmgrFreeFbsrMemory_HAL unconditional) | yes — explicit bIsGpuLost branch | no | NOT reachable (no suspend occurred in sequence; PM-resume only) | SAFE here — no leak even out-of-sequence; flag for PM-resume-after-loss family","[PDB-reader] kernel_gsp.c:2813-2814 | _kgspRpcRecvPoll pre-loop G8 | C7-e3 ORs osIsGpuBusLost into this exact site → NV_ERR_RESET_REQUIRED, bPollingForRpcResponse cleared | n/a | sysmem RPC queue — marker-sink accepted by design | yes (the netcon3 wedge site) | SAFE post-C7","[PDB-reader] kernel_gsp.c:314-315 | _kgspRpcSanityCheck | C7-e4 ORs osIsGpuBusLost → NV_ERR_GPU_IS_LOST frees mid-loop worker | n/a | n/a | yes | SAFE post-C7","[PDB-reader] rpc.c:1854 (G2 _issueRpcAndWait) + rpc.c:2097 (G3 chunked) | PDB unset → guards don't fire → RPC issued → bounded by C7-e3/e4 + C7-e6 (msgq tx loop) | no | send=sysmem+posted doorbell | yes post-loss | SAFE — latency µs-ms per call instead of instant skip","[PDB-reader] rpc.c:11530 | rpcRmApiFree_GSP NV_OK-conversion | THE one genuine PDB-only CORRECTNESS branch: PDB unset → free RPC returns NV_ERR_RESET_REQUIRED → resserv free-path NV_ASSERTs fire (log noise; host-side bookkeeping still completes — NV_ASSERT non-fatal in release) | yes — returns NV_OK only on PDB | n/a | NOT reachable in traced sequence (zero GSP-backed objects exist pre-gpuStateInit; RmShutdownAdapter skips gpuStateUnload/Destroy via unset NV_INIT_FLAG_GPU_STATE*) | SAFE in sequence; optional hardening for post-init-loss family (see surprises)","[PDB-reader] kern_fsp_gh100.c:638 | _kfspWriteToEmem G9 (dead-value AND PDB) | PDB unset → guard inert → junk offsets → trailing NV_ASSERT_OR_RETURN fires → clean error return, no hang | yes — conjunct | no | NOT reachable (re-open dies earlier at GFW-boot cond-wait via C7-e2 before any FSP EMEM send) | SAFE — assert-noise worst case","[PDB-reader] journal.c:565 (state mask), 1089 (bGpuAccessible), 2934 (rcdbAddRmGpuDump), 3133 (rcdProbeGpuPresent) | telemetry/guards | unset → mask lacks IS_LOST bit (cosmetic); bGpuAccessible self-falsifies via dead BOOT_0 read; dump proceeds bounded; probe reads short-circuited dead value → concludes lost correctly | journal.c:2934 is PDB-only skip | reads short-circuited | crash-dump paths only, not in failed-init unwind | SAFE","[PDB-reader] nv_debug_dump.c:282 | nvdDumpAllEngines loop-break | PDB unset → break doesn't fire, but each engine callback's RPCs bounded by C7; bGpuAccessible upstream limits | PDB||INACCESSIBLE | n/a | crash-dump only | SAFE-bounded","[PDB-reader] kernel_nvlink.c:1159,1213 | nvlink shutdown gates | no NVLink on GB202 consumer | n/a | n/a | DEAD CODE here | SAFE","[PDB-reader] kernel_rc_watchdog_callback.c:155 | RC watchdog | watchdog never initialized in failed-init (NV_INIT_FLAG_FIFO_WATCHDOG unset) + disabled by default | no | n/a | no | SAFE","[PDB-reader] kernel_bif_ga100.c:690 + kernel_bif_gm107.c:548 | comments only — code deliberately uses config cycles to BYPASS the PDB-gated reg macro | n/a | config reads bounded | FLR/recovery paths, not in sequence | SAFE","[PDB-reader] gpu_protobuf.c:85 | crash-dump telemetry records property value | cosmetic | no | n/a | crash dump only | SAFE","[PDB-reader] gpumgrDetachGpu gpu_mgr.c:1640 | reads PDB_PROP_GPU_IN_TIMEOUT_RECOVERY (NOT IS_LOST) → bDelClientResources=TRUE in our case | n/a | n/a | yes — runs in RmShutdownAdapter | SAFE; _gpumgrDestroyGpu DESTROYS OBJGPU here → PDB ceases to exist; any later PDB-set is structurally impossible AND unnecessary","[condwait] kernel_gsp_gh100.c:1050 | _kgspLockdownReleasedOrFmcError (MMIO HWCFG2/mailbox) | generic !=NV_OK → FMC error report + 2 mailbox reads (short-circuited) + return status | no TIMEOUT-specific branch | no — MMIO only | YES (#292/netcon2 site) | SAFE — C7-e2 break = desired","[condwait] kernel_gsp_gh100.c:133,144 | _kgspWaitForAsserted/Deasserted (MMIO FALCON_ENGINE, GPU_TIMEOUT_FLAGS_TMR) | !=NV_OK → print+DBG_BREAKPOINT, proceeds | no | no | yes (kgspResetHw) | SAFE — TMR path also self-terminates on dead PTIMER read (current=huge ≥ timeout)","[condwait] kernel_gsp_gb100.c:83 | _kgspWaitForResetAccess (MMIO PLM, TMR flags) | !=NV_OK → print, then kgspResetHw_GH100 anyway (posted writes) | no | no | yes (Blackwell reset) | SAFE","[condwait] kernel_gsp_gh100.c:620,662,1271 | SPDM booted/mailbox-cleared/CC-cleanup (MMIO mailbox) | generic goto exit / error return | no | no | NO — CC/SPDM off on this host | SAFE (dead code here; e2 still covers the ==0 polarity trap if ever enabled)","[condwait] kernel_gsp_falcon_ga102.c:92 | s_dmaPollCondFunc (MMIO DMATRFCMD) | !=NV_OK → print+DBG_BREAKPOINT+return | no | no | yes (falcon DMA during FWSEC/booter) | SAFE","[condwait] kernel_gsp_falcon_tu102.c:473 | _kgspIsReloadCompleted (MMIO) | (status!=NV_OK || secMailbox0!=NV_OK) → return NV_ERR_TIMEOUT (produced, not consumed specially upstream) | produces TIMEOUT, doesn't branch on it | no | resume path only | SAFE","[condwait] kern_fsp_gb202.c:80 (+gb100:95, gh100:828 same shape) | _kfspWaitBootCond (MMIO THERM scratch) | !=NV_OK → gpuMarkDeviceForReset (scratch-reg posted write, NOT PDB) + 5 diagnostic GPU_REG_RD32 (short-circuited) + kfspDumpDebugState (short-circuited reads) | no | no | YES — GB202's own boot wait, hit on re-open | SAFE — fails fast, bounded diagnostics","[condwait] kern_fsp.c:364 | _kfspWaitForCanSend (MMIO queue head/tail) | !=NV_OK → print+propagate | no | no | yes | SAFE","[condwait] kern_fsp_gh100.c:1158 | _kfspIsGspTargetMaskReleased (GPU_REG_RD32_UNCHECKED — still routes through osDevReadReg032 short-circuit) | returned directly | no | no | yes (lockdown chain) | SAFE","[condwait] kernel_falcon_ga102.c:264 + kernel_falcon_tu102.c:323 | falcon mem-scrub finished (MMIO HWCFG2) | returned directly; callers generic | no | no | yes (falcon resets in boot+teardown) | SAFE","[condwait] kernel_falcon_tu102.c:549 | s_riscvIsIcdNotBusy (MMIO ICD) | !=NV_OK → return NV_ERR_TIMEOUT (ICD debug path, consumed generically) | produces only | no | debug/dump path | SAFE","[condwait] kernel_gsp_tu102.c:1155 | _kgspIsProcessorSuspended (MMIO mailbox) | return value IGNORED by kgspUnloadRm caller (kernel_gsp.c:5194) | no | no | unload path (not in failed-init unwind) | SAFE","[condwait] kern_gpu_gb100.c:1578,1620,1690 | MNOC mbox receiver/credit/poll (MMIO aperture) | returned directly, callers generic | no | no | uncertain on consumer GB202 (MNOC=OOB) | SAFE either way","[condwait] kernel_bif_ad102.c:73 | _kbifPreOsCheckErotGrantAllowed (MMIO PBUS) | !=NV_OK → print+return | no | no | uncertain (ERoT absent → early-out on first read==0xFF... reads VALID_NO? dead value makes VALID test false-positive-ish but wait is bounded) | SAFE","[condwait] kernel_sec2_gb10b.c:308,376 + gb20b.c:577 | Tegra SOC SEC2 | generic | no | no | NO — Tegra iGPU only | SAFE (dead code)"],"engine_findings":["DISPATCH PROOF: gpuTimeoutCondWait is a compile-time macro (gpu_timeout.h:147 '#define gpuTimeoutCondWait(g,a,b,t) timeoutCondWait(&(g)->timeoutData, t, a, b, __LINE__)') — there is NO TIMEOUT_DATA vtable, NO installed pFn, and NO tmrApiCondWait/second engine body anywhere in the tree. timeoutCondWait (gpu_timeout.c:572-608) is the single engine behind ALL 27 call sites; the TMR/TMRDELAY/OSTIMER/OSDELAY variants differ only inside timeoutCheck/_checkTimeout, which the same loop calls. C7-e2's single edit at the loop top (~591, before osSpinLoop) intercepts everything.","Default flags on this config: timeoutInitializeGpuDefault (gpu_timeout.c:64-78) forces defaultFlags=GPU_TIMEOUT_FLAGS_OSTIMER for IS_GSP_CLIENT — so default-flag waits never touch the GPU PTIMER; the explicit TMR users (kgsp gh100:133/144, gb100:83) still funnel through timeoutCondWait AND already self-terminate on a dead bus (tmrGetCurrentTime reads dead 0xFF.. → current >= timeout instantly). C7-e2 makes them deterministic instead of accidental.","C7-e2 placement nuance (intended behavior, verify in patch): breaking with NV_ERR_TIMEOUT bypasses the engine's last-chance cond recheck at gpu_timeout.c:598-602 — correct, since on a dead bus that recheck is exactly the 0xFFFFFFFF accidental-true vector that caused the A13 failure mode. Also a cond that is TRUE on first evaluation never reaches the check (loop body not entered) — completed waits unaffected.","STATUS-SPACE INVARIANT: zero of the 27 callers branch on NV_ERR_TIMEOUT differently from generic failure, and natural timeouts on a dead bus already returned NV_ERR_TIMEOUT pre-C7 — C7-e2 changes only latency, not the reachable status space. NON-MMIO CONDS: none — all 27 cond functions read device MMIO; the only sysmem-completing waits (RPC recv queue, msgq tx-credit, status-queue init, FSP response) are the hand-rolled loops covered by C7-e3/e4/e6, statuses chosen per existing caller contracts.","GAP-7 TRACE (full sequence, source-verified): (1) C7 frees worker: _kgspRpcRecvPoll pre-loop → NV_ERR_RESET_REQUIRED → SET_GUEST_SYSTEM_INFO/GET_GSP_STATIC_INFO fail (kernel_gsp.c:5105-5117) → kgspInitRm done: (5129-5158) = log dump (sysmem) + libosPreserveLogs + group-lock release — NO PDB read/set, no HW teardown. (2) RmInitAdapter shutdown: (osinit.c:2371) → RmShutdownAdapter(2377). (3) RmShutdownAdapter (2392-2525) NEVER reads PDB_PROP_GPU_IS_LOST (only CLKS_IN_TEGRA_SOC at 2476); with failed-init flags {DMA, SCALABILITY, GPUMGR_ATTACH} set and GPU_STATE/GPU_STATE_LOAD/CORE_LOGIC/FIFO_WATCHDOG UNSET (osinit.c:865/1310/1339/1369), the heavy HW-touch paths gpuStateUnload/gpuStateDestroy/kgspUnloadRm are SKIPPED by flag, not by PDB. rmapiDelPendingDevices is a no-op (no clients pre-gpuStateInit) → no rpcRmApiFree RPCs. gpumgrDetachGpu (gpu_mgr.c:1636) → _gpumgrDestroyGpu DESTROYS the OBJGPU → PDB lifetime ends INSIDE the unwind. RmClearPrivateState memsets nvp (nvp->pGpu=NULL, preserves pmc_boot_0/status/registry). (4) worker returns NV_FALSE → nv.c:1539 → tb_egpu_recover_trigger_post_rminit_fail (gated off live, NVreg_TbEgpuRecoverEnable=0) → -EIO → failed_release_irq (pure SW frees). (5) foreground LOCKDOWN arm already ran os_pci_set_disconnected + rm_cleanup (COND_ACQUIRE deferred one-shot, no retry — osapi.c:1923-1933) → flush_work joins → -EIO; nv_stop_device/rm_shutdown_adapter NOT called (NV_FLAG_INITIALIZED never set, usage_count 0, failed open ⇒ no close). (6) late AER rm_cleanup calls (nv.c:2138/2508, nv-pci.c:3023) → NV_GET_NV_PRIV_PGPU==NULL → no-op, NULL-checked. (7) RE-OPEN: nv_check_gpu_state (nv-linux.h:1620) checks only NV_FLAG_IN_SURPRISE_REMOVAL — NOT set (sole setter osHandleGpuLost:461 never ran) → re-open proceeds → FRESH OBJGPU with PDB unset BY CONSTRUCTION (so even a worker-unwind PDB-set would not influence the re-open; only pci_dev->error_state=perm_failure persists — sink state, os-pci.c:165-177) → all MMIO short-circuits on os_pci → first cond-wait (kgspWaitForGfwBootOk, kernel_gsp.c:4990) breaks instantly via C7-e2 → same clean unwind → -EIO in ms. No per-open raw MMIO reads exist (RmInitPrivateState's raw PMC_BOOT reads are probe-time only, osinit.c:1568).","UNWIND SETS NO PDB ANYWHERE: kgspInitRm done: block, RmInitAdapter failed/shutdown labels, and rm_init_adapter wrapper (osapi.c:1732) have no PDB writes and no NV_ERR_RESET_REQUIRED-specific handling (only INSUFFICIENT_POWER/NOT_SUPPORTED/IRQ_NOT_FIRING get nvp->status mapping). gpuMarkDeviceForReset (gpu.c:6978→_gpuSetResetRequiredState) writes a GPU scratch register, NOT PDB; its in-path callers (_kgspHandleFatalTimeout:2613, _kgspCheckGspFatalHwError:2672, FSP boot-fail) are either bypassed by C7's pre-loop return or harmless posted writes. The heartbeat self-sink cleanupGpuLostStateAtomic(DETECTOR_GSP_HEARTBEAT_TIMEOUT) at kernel_gsp.c:2928-2931 requires the in-loop NV_ERR_TIMEOUT classification with bIsFatalTimeout — C7-e3's pre-loop return deliberately bypasses it, confirming the GAP-7 premise that PDB stays unset.","BOOT-RETRY BOUND: _kgspBootGspRm (kernel_gsp.c:4754) sets bRetry=TRUE on boot failure when ECC is disabled (true on consumer GDDR7), max 4 attempts (nvrm_registry.h:2797) — but on a dead bus the retry dies at iteration start: kgspIsWpr2Up reads dead 0xFFFFFFFF → 'WPR2 up' → NV_ERR_INVALID_STATE with bRetry=FALSE → loop exits after one µs-fast extra iteration. For the netcon3-class wedge (post-INIT_DONE RPCs) failure surfaces OUTSIDE the retry loop entirely. No unbounded retry."],"needs_handling":[],"surprises":["The DETECTOR_MMIO_DEAD pre-emption is structural, not incidental: osDevReadReg032's early return at os.c:2050 sits IN FRONT of its own detector at os.c:2081-2092, so once os_pci is set the RM-side PDB can only ever be set by rm_cleanup (lock-gated) or the heartbeat classifier (bypassed by C7). C7's end-state 'os_pci-set, PDB-unset' is therefore the PERMANENT steady state for the OBJGPU's remaining lifetime — but that lifetime ends inside the same unwind at gpumgrDetachGpu, which is why it's harmless.","If a PDB-set were ever wanted anyway, the one safe point exists and was identified: RmInitAdapter's shutdown: label (osinit.c:2371-2376), before RmShutdownAdapter — the worker provably holds the API lock there (rm_init_adapter acquired it at osapi.c:1761; kgspInitRm's NV_ASSERT rmapiLockIsOwner() at kernel_gsp.c:5075 guarantees reacquisition after relaxed-locking), pGpu is alive, the GPU is invisible to other threads (expanded-visibility is per-thread), and the A13b PM-bit-clobber objection (GAP-3) is moot for a never-initialized GPU with no dynpower worker. A guarded 'if (os_pci dead) cleanupGpuLostStateAtomic(pGpu, DETECTOR_*)' there would be precondition-clean. NOT REQUIRED — it would be dead state ~µs later (OBJGPU destroyed at osinit.c:2489) and cannot influence the re-open (fresh OBJGPU starts PDB-unset regardless).","rpcRmApiFree_GSP (rpc.c:11509+, guard ~11530) is the single genuine PDB-only CORRECTNESS branch in the whole teardown surface: it converts the free-RPC to NV_OK so resserv teardown asserts don't fire. It is unreachable in the GAP-7 sequence (no GSP-backed objects exist before gpuStateInit), but in the ADJACENT post-init-loss family (GPU dies initialized, rm_cleanup defers, PDB unset) every object free will now return NV_ERR_RESET_REQUIRED via C7-e3 → NV_ASSERT log storms during close. OPTIONAL future hardening (not an apnex.32 blocker, separate from #292): OR osIsGpuBusLost into the three rpc.c guards (1854, 2097, 11530) and kern_fsp_gh100.c:638's G9 for marker symmetry.","nv_check_gpu_state does NOT gate the re-open: it tests only NV_FLAG_IN_SURPRISE_REMOVAL, whose sole setter (osHandleGpuLost, osinit.c:461) never runs for a GPU that died during init. The re-open therefore always re-enters RmInitAdapter — bounded by os_pci short-circuit + C7-e2 + the A12 budget, failing in ms. This is exactly the residual A14 (sticky fail-fast gate) shortcuts; A14 remains DiD, not a correctness requirement.","kgspExecuteCoreResume_TU102 and s_riscvIsIcdNotBusy MANUFACTURE NV_ERR_TIMEOUT as their failure status — when grepping for 'TIMEOUT-specific' consumers these look alarming but are producers, not consumers; everything upstream treats them generically.","FULL_GPU_SANITY_CHECK / FULL_GPU_SANITY_FOR_PM_RESUME (g_gpu_nvoc.h:5891-5911) pass spuriously when PDB is unset (IS_CONNECTED stays TRUE) — callers then proceed as if healthy, but every downstream HW touch funnels through the guarded readers/C7-bounded RPCs; on this never-initialized GPU there are no open FDs to drive those ioctl paths anyway.","RmShutdownAdapter's eGPU serverLockAllClients branch (osinit.c:2415) and gpumgrDetachGpu both key on is_external_gpu / IN_TIMEOUT_RECOVERY — neither consults PDB_PROP_GPU_IS_LOST; the failed-init teardown's safety rests entirely on (a) the NV_INIT_FLAG gating that skips gpuStateUnload/Destroy and (b) the os_pci read short-circuit — both PDB-independent."],"verdict":"all-safe"},{"area":"GAP-7 non-teardown PDB_PROP_GPU_IS_LOST getProperty readers (os_pci-dead-but-PDB-unset end-state) + GAP-6 timeoutCondWait engine-dispatch & caller audit — pre-build checks for C7 (apnex.32), tree = /root/open-gpu-kernel-modules (apnex fork, C5-v4/A13 guards present, C7 not yet applied)","callers":["=== GAP-7 PDB READERS (col2=containing fn; col3=PDB-TRUE delta & PDB-unset behavior; col4=TIMEOUT-specific n/a unless noted; col5=unneutralized HW touch?; col6=reachable on GB202/GSP-RM/TB-eGPU; col7=verdict) ===","journal.c:565 | _getCommonJournalStateMask | PDB TRUE sets NV_RM_JOURNAL_STATE_MASK_IS_LOST bit in every journal record; unset = record merely omits the bit | n/a | none (pure sysmem) | YES (every assert/journal record incl. failed-init unwind) | SAFE (telemetry-only; forensic records will under-report 'lost' in the os_pci-only state)","journal.c:1089 | rcdbDumpInitGpuAccessibleFlag | PDB TRUE forces bGpuAccessible=FALSE; unset falls through to GPU_REG_RD32(NV_PMC_BOOT_0) at :1094 which short-circuits to 0xFFFFFFFF != chipId0 -> bGpuAccessible self-heals to FALSE | n/a | none (read short-circuited) | YES (entry of every OCA/journal dump) | SAFE (self-healing via chipId0 check; 0xFFFFFFFF can never equal a valid chipId0)","journal.c:2934 | rcdbAddRmGpuDump entry guard (fork C5-v4) | PDB TRUE returns NV_OK skipping the whole OCA dump; unset = full nvdDumpAllEngines cascade runs against dead bus — MMIO reads short-circuit, dump RPCs (DUMP_PROTOBUF_COMPONENT) die in us at C7-e4 send-side sanity, bGpuAccessible self-heals FALSE | n/a | none under C7 | YES (RmLogGpuCrash osapi.c:464 + deferred DPC) | SAFE under C7 (guard is dead in os_pci-only state but everything it protected is engine-bounded; widen in C7-e7 for noise hygiene)","journal.c:3133 | rcdProbeGpuPresent | PDB TRUE skips the probe; unset = GPU_CHECK_REG_RD32(NV_PMC_BOOT_0) probe -> short-circuited 0xFFFFFFFF = GPU_REG_VALUE_INVALID -> 'found GPU inaccessible' log + BadRead journal record; _regCheckReadFailure(gpu_access.c:838) then runs gpuSanityCheck = PCI config reads (kernel short-circuits config reads on pci_dev_is_disconnected) + possible config write (dropped/UR, non-blocking) | n/a | none | YES (fires on every NV_ASSERT after loss, release builds; recursion-latched) | SAFE (bounded, once-latched; note it does NOT heal PDB — consistent with GAP-7 premise)","nv_debug_dump.c:282 | nvdDumpAllEngines loop-break guard (fork) | PDB TRUE (or INACCESSIBLE) breaks engine loop; unset = iterates ALL engine callbacks: reg reads short-circuit, GSP RPCs bounded by C7-e4, GSP-only engines have NULL dump fns; per-callback bounded -> loop bounded | n/a | none under C7 | YES (NVD ioctl/bug-report + RC dump while GPU attached) | SAFE under C7 (guard dead; include in C7-e7 widen)","rpc.c:1854 | _issueRpcAndWait G2 guard (fork) | PDB TRUE returns NV_ERR_GPU_IS_LOST pre-send; unset+C7: _kgspRpcSendMessage calls _kgspRpcSanityCheck at kernel_gsp.c:421 -> C7-e4 returns NV_ERR_GPU_IS_LOST BEFORE msgq write/doorbell; recv-poll never entered; rpc returns in us | consumption at 1903-1914: generic !=NV_OK, but emits NV_PRINTF_COND(...LEVEL_ERROR) 'rpcSendMessage failed' PER RPC (bQuietPrints stays FALSE — only bFatalError/IN_RESET branches set it) | none (e4 fires pre-doorbell) | YES (every waited RPC incl. unwind teardown RPCs e.g. UNLOADING_GUEST_DRIVER) | NEEDS-HANDLING (low): widen to osIsGpuBusLost — not for safety (bounded) but to kill the per-RPC LEVEL_ERROR stream (netconsole-amplifier class, same class as the A13-FAIL storm)","rpc.c:2097 | _issueRpcLarge G3 guard (fork) | PDB TRUE returns NV_ERR_GPU_IS_LOST; unset+C7: first rpcSendMessage bounded by e4; BUT failure path at 2112-2124 contains bare NV_ASSERT(0) (and again at 2153) -> assert + LEVEL_ERROR per chunked control RPC | generic !=NV_OK + NV_ASSERT(0) | none | YES (large RmControl RPCs) | NEEDS-HANDLING (low): include in C7-e7 widen (kills NV_ASSERT(0) noise)","rpc.c:11530 | rpcRmApiFree_GSP guard (fork) | PDB TRUE returns NV_OK so resserv teardown completes quietly (designed semantics); unset+C7: _issueRpcAndWait returns NV_ERR_GPU_IS_LOST via e4 -> Free returns non-OK -> 'GspRmFree failed' LEVEL_ERROR per freed object + NV_ASSERT/NV_ASSERT_OK consumers fire per object (system.c:148, mem_export.c:403, kgraphics_tu102.c:223 etc. + NV_RM_RPC_FREE macro propagation); host-side destructors still complete (no leak/abort found — consumers only assert/log), GSP-side free moot | the answer to the special-attention question: with C7-e3/e4 in place a dead-bus Free RPC FAILS FAST (us, no doorbell, no wait) — it does not hang; the only divergence vs PDB-set is non-OK status + assert/print volume | none | YES — heavy on running-GPU surprise-removal teardown (hundreds of objects); light on failed-bootstrap unwind (GSP-side objects not yet created pre-INIT_DONE) | NEEDS-HANDLING (the load-bearing GAP-7 delta): widen guard to osIsGpuBusLost to restore NV_OK semantics and prevent a per-object assert/log storm","kernel_nvlink.c:1159 | knvlinkPrepareForXVEReset (remote-GPU skip) | PDB TRUE skips remote HSHUB unmap (RPC + reg writes); unset would issue them — bounded by C7-e4 / posted writes | generic | none | NO (no NVLink on GB202 consumer, single GPU, XVE-reset path) | SAFE (unreachable dead code here)","kernel_nvlink.c:1213 | knvlinkPrepareForXVEReset (self skip) | PDB TRUE skips self HSHUB unmap; note braceless-if quirk: when skipped, stale loop `status` feeds the !=NV_OK check at 1216 (upstream quirk, log-only) | generic | none | NO (same) | SAFE (unreachable)","kernel_gsp.c:315 | _kgspRpcSanityCheck | PDB TRUE -> NV_ERR_GPU_IS_LOST 'skipping RPC' (LEVEL_INFO, does NOT set bQuietPrints); C7-e4 ORs osIsGpuBusLost into THIS condition -> os_pci-only state covered by spec; called from send entry (:421) and recv loop (:2851) | generic at both call sites | none | YES (every RPC) | SAFE (covered by C7-e4 as specced)","kernel_gsp.c:2814 | _kgspRpcRecvPoll pre-loop G8 guard | PDB TRUE (or bFatalError) -> NV_ERR_RESET_REQUIRED + clears bPollingForRpcResponse (:2820); C7-e3 ORs osIsGpuBusLost into this exact condition preserving the clear | generic | none | YES (every waited RPC) | SAFE (covered by C7-e3 as specced; the OR-in placement requirement from the design is confirmed correct at source 2813-2822)","kernel_rc_watchdog_callback.c:155 | krcWatchdogTimerProc | PDB TRUE skips krcWatchdog/vblank/perf each 1Hz tick; unset = tick runs against dead bus: notifier reads are sysmem, reg reads short-circuit, RC channel-reset controls -> RPC -> C7-e4 bounded; 1Hz bounded noise | generic | none | PARTIAL — callback armed only at osSchedule1HzCallback (kernel_rc_watchdog.c:1247) after full watchdog-channel init post-GSP-up: NOT armed in the #292 failed-bootstrap window; armed for running-GPU loss until close | SAFE","gpu_protobuf.c:85 | _gpuDumpEngine_CommonFields | encodes IS_LOST bool into NVDEBUG protobuf record; unset = dump claims not-lost (mildly misleading forensics) | n/a | none | YES (any dump) | SAFE (telemetry-only)","kern_fsp_gh100.c:637-638 | _kfspWriteToEmem_GH100 G9 guard (fork) | guard requires reg==0xFFFFFFFF AND PDB -> DEAD in os_pci-only state (first term true via short-circuit, second false): falls through to junk ememOffsetStart, size/4 posted EMEMD writes (dropped harmlessly), final NV_ASSERT_OR_RETURN(:672) fires -> NV_ERR_INVALID_STATE + assert-journal noise instead of the clean NV_ERR_GPU_IS_LOST the guard was written to produce | n/a | none (writes posted; reads short-circuited; bounded) | YES (GB202 uses GH100 EMEM path for FSP commands during boot/re-open) | NEEDS-HANDLING (polish): one-line — replace getProperty(PDB) with osIsGpuBusLost(pGpu)","osapi.c:424-425 [adjacent, audited] | RmLogGpuCrash G6 guard (os_pci AND PDB) | dead in os_pci-only state -> crash-dump proceeds but bounded under C7; EEH re-eval block 441-453 (raw NV_PRIV_REG_RD32 probe + temporary PDB clear/restore at 451/486) is SKIPPED when PDB unset && IS_CONNECTED -> no raw MMIO probe, no marker flap | n/a | none under C7 | YES (XID/crash-log path) | SAFE under C7 (optional: relax AND to osIsGpuBusLost)","gpu.c:7427 [adjacent] | OOB-XID-log RPC gate | PDB TRUE skips LOG_OOB_XID RPC; unset: GSP_ERROR/GSP_RPC_TIMEOUT/XID79/bFatalError already excluded; residual XIDs' RPC bounded by C7-e4 | generic | none | YES | SAFE","mem_mgr_pwr_mgmt.c:257 [adjacent] | memmgrRestorePowerMgmtState | PDB TRUE -> fbsrBegin(FBSR_OP_DESTROY) frees saved-FB sysmem instead of DMA-restore; unset on dead bus -> attempts FBSR_OP_RESTORE: CE submission/RPCs fail fast under C7, resume fails bounded; callers funnel-bounded (A12/A13'-e1 GAP-1) | generic NV_CHECK_OK_OR_GOTO | none under C7 (DMA never starts — channel setup RPCs die first) | S3/RTD3-resume only | SAFE (bounded; PDB-unset costs a failed-restore error cascade instead of a clean destroy)","gpu_access.c:838 [adjacent] | _regCheckReadFailure | PDB TRUE -> reason=BAD_READ_LOW_POWER (skip sanity probe); unset -> gpuSanityCheck config reads (kernel-short-circuited) -> BAD_READ_GPU_OFF_BUS classification | n/a | none | YES (every checked-read failure) | SAFE","=== GAP-6 COND-WAIT CALL SITES (col2=cond-fn; col3=consumption; col4=NV_ERR_TIMEOUT-specific?; col5=non-MMIO cond?; col6=reachable; col7=verdict) ===","kernel_gsp_gh100.c:133 | _kgspWaitForAsserted (PGSP_FALCON_ENGINE reset bit, MMIO) | if(status!=NV_OK){print;DBG_BREAKPOINT} then continue to deassert | NO | NO | YES (GB202 uses GH100 KernelGsp HAL; reset during bootstrap/unwind) | SAFE (also TMR-flag wait: dead PTIMER self-terminates)","kernel_gsp_gh100.c:144 | _kgspWaitForDeasserted (same reg) | same pattern, returns status | NO | NO | YES | SAFE","kernel_gsp_gh100.c:620 | _kgspSpdmBootedOrFmcError (falcon mailbox MMIO) | !=NV_OK -> goto exit | NO | NO | NO (CC/SPDM off; the ==0 polarity trap site C7-e2 closes anyway) | SAFE","kernel_gsp_gh100.c:662 | _kgspFalconMailbox0Cleared (mailbox MMIO) | !=NV_OK -> goto exit | NO | NO | NO (SPDM path) | SAFE","kernel_gsp_gh100.c:1050 | _kgspLockdownReleasedOrFmcError (HWCFG2+mailbox MMIO) — THE #292 wedge site | !=NV_OK -> error prints + kgspFmcReportErrorCode + kfspDumpDebugState + return status (generic) | NO (verified — matches design §note) | NO | YES (bootstrap) | SAFE — C7-e2 break lands on the exact intended unwind path (NV_ERR_NOT_READY return from kgspBootstrap)","kernel_gsp_gh100.c:1271 | _kgspHasCcCleanupFinished (mailbox MMIO) | !=NV_OK -> goto exit | NO | NO | NO (CC off) | SAFE","kernel_sec2_gb10b.c:308,376 + kernel_sec2_gb20b.c:577 | sec2 cmd/target-mask/boot conds (MMIO) | generic !=NV_OK | NO | NO | NO (GB10B/GB20B = Tegra SoC iGPU HALs) | SAFE (unreachable)","kern_gpu_gb100.c:1578,1620,1690 | _gpuMnocMboxSendReceiverCond/SendCreditCond/PollCond (MNOC PRI via IoAperture REG_RD32 -> osDevReadReg032, short-circuited) | returned directly to MNOC send/poll helpers; error helper returns NV_ERR_GENERIC on ERR bit (which 0xFFFFFFFF sets) | NO | NO | unlikely on GB202 (MNOC-CPU property paths); bounded regardless | SAFE","kernel_bif_ad102.c:73 | _kbifPreOsCheckErotGrantAllowed (BIF scratch MMIO) | !=NV_OK -> print + return status | NO (line 45 is doc comment only) | NO | marginal (eRoT absent on GeForce) | SAFE","kernel_falcon_tu102.c:323 | _kflcnMemScrubbingFinished (FALCON_DMACTL MMIO) | returned directly (kflcnWaitForResetToFinish) | NO | NO | YES (all falcons incl. GB202) | SAFE","kernel_falcon_tu102.c:549 | s_riscvIsIcdNotBusy (RISCV_ICD_CMD MMIO) | !=NV_OK -> return NV_ERR_TIMEOUT (collapses ALL errors to TIMEOUT); consumers at :606,661,707,747,785 gate ICD pokes on !=NV_ERR_TIMEOUT -> prompt C7-e2 TIMEOUT lands on the identical branch a natural timeout takes, and in the SAFE direction (skips further ICD register pokes) | YES — but benign-by-construction (see consumption) | NO | YES (GSP RISC-V coredump/diag path after boot failure) | SAFE","kernel_falcon_ga102.c:264 | _kflcnWaitForScrubbingToFinish (MMIO) | returned directly | NO | NO | YES | SAFE","kern_fsp_gb202.c:80 | _kfspWaitBootCond_GB202 (THERM_I2CS_SCRATCH MMIO) | !=NV_OK -> gpuMarkDeviceForReset(pGpu) (state write, in-OBJGPU) + NV_ERROR_LOG with 5 more short-circuited reads + kfspDumpDebugState; returns status | NO | NO | YES (GB202 secure-boot poll, first wait of every (re)open) | SAFE — C7-e2 makes a dead-bus open fail this promptly; the reset-marker write is sysmem-only","kern_fsp_gb100.c:95 | _kfspWaitBootCond_GB100 | generic | NO | NO | NO (GB202 binds kfspWaitForSecureBoot_GB202 per g_kern_fsp_nvoc.c:494) | SAFE","kern_fsp_gh100.c:828 | _kfspWaitBootCond_GH100 | generic | NO | NO | NO (GH100-only binding :490) | SAFE","kern_fsp.c:364 | _kfspWaitForCanSend (kfspCanSendPacket MMIO queue regs) | !=NV_OK -> print, return (kfspPollForCanSend) | NO | NO | YES (every FSP packet send) | SAFE","kern_fsp_gh100.c:1158 | _kfspIsGspTargetMaskReleased (GPU_REG_RD32_UNCHECKED HWCFG2) | returned directly | NO | NO | YES (GB202 bound per g_kern_fsp_nvoc.c:631-634) | SAFE — note cond ACCIDENTALLY SUCCEEDS on 0xFFFFFFFF (reg!=0 && !=locked-pattern) -> returns NV_OK before C7-e2 can fire; next boot step errors out; no regression, expectation-setting only","kernel_gsp_falcon_ga102.c:92 | s_dmaPollCondFunc (FALCON_DMATRFCMD MMIO) | !=NV_OK -> print + DBG_BREAKPOINT + return | NO | NO | NO-ish (pre-Hopper booter DMA path; GB202 boots via FSP/FMC) | SAFE","kernel_gsp_falcon_tu102.c:473 | _kgspIsReloadCompleted (PGC6_BSI_SECURE_SCRATCH_14 MMIO) | (status!=NV_OK)||(secMailbox0!=NV_OK) -> return NV_ERR_TIMEOUT (maps all to TIMEOUT, no special branch) | NO | NO | S3-resume path only | SAFE","kernel_gsp_tu102.c:1155 | _kgspIsProcessorSuspended (falcon mailbox MMIO) | returned directly (kgspWaitForProcessorSuspend, bound TU102 for GB202) | NO | NO | suspend/unload path | SAFE","kernel_gsp_gb100.c:83 | _kgspWaitForResetAccess (PGSP RESET_PRIV_LEVEL_MASK MMIO) | !=NV_OK -> print+DBG_BREAKPOINT, then kgspResetHw_GH100 runs regardless (posted writes) | NO | NO | YES (kgspResetHw_GB100 is GB202's bound resetHw — on bootstrap retry/unwind) | SAFE — cond accidentally succeeds on 0xFFFFFFFF (both ENABLE bits read 1); either way bounded (TMR-flag wait + C7-e2)"],"engine_findings":["DISPATCH VERIFIED — single engine body: gpuTimeoutCondWait is a plain macro (inc/kernel/gpu/gpu_timeout.h:147: `#define gpuTimeoutCondWait(g,a,b,t) timeoutCondWait(&(g)->timeoutData, t, a, b, __LINE__)`) directly to timeoutCondWait (gpu_timeout.c:572). There is NO TIMEOUT_DATA vtable/pFn for the cond-wait path and NO tmrApiCondWait anywhere in the tree (grep over src+inc: zero other CondWait bodies). The OSTIMER/OSDELAY/TMR/TMRDELAY variants are flag branches INSIDE timeoutCheck/_checkTimeout (gpu_timeout.c:356-465), which the single while-loop calls — C7-e2's one edit intercepts ALL 27 gpuTimeoutCondWait call sites. No second engine body needs the edit.","PRECEDENT for prompt NV_ERR_TIMEOUT: timeoutCheck:483-484 already early-returns NV_ERR_TIMEOUT when API_GPU_IN_RESET_SANITY_CHECK(pGpu), and _checkTimeout's TMR/TMRDELAY branches (416-417, 438-439) return NV_ERR_TIMEOUT when the GPU is detached — every cond-wait caller in the driver must already tolerate an immediate NV_ERR_TIMEOUT. C7-e2 follows the engine's established convention.","TMR-flag waits self-terminate on a dead bus even without C7-e2: tmrGetCurrentTime reads the dead PTIMER -> 0xFFFFFFFF.. -> current >= timeout on first check. C7-e2 still adds value (deterministic, polarity-independent) but the TMR variant is not a coverage hole.","HAZARD (a) RESULT: across all 27 call sites, ZERO dangerous NV_ERR_TIMEOUT-specific consumption of the cond-wait return. The only ==/!=NV_ERR_TIMEOUT patterns found: (1) kflcnRiscvIcdWaitForIdle_TU102 collapses every non-OK to NV_ERR_TIMEOUT (tu102:549-551) and its 5 consumers gate further ICD register pokes on !=NV_ERR_TIMEOUT — a prompt C7-e2 TIMEOUT takes the identical branch a natural timeout takes, in the skip-hardware direction; (2) kfspWaitForResponse:418 and timeoutCondWait's own 598-602 are benign last-chance cond rechecks; (3) kflcnWaitForHalt_TU102:353 / kflcnWaitForHaltRiscv_GA102:294 are hand-rolled gpuCheckTimeout loops (not cond-wait engine sites) whose ==TIMEOUT branch is log+break — and they exit instantly on a dead bus anyway via accidental CPUCTL_HALTED=1 from 0xFFFFFFFF.","HAZARD (b) RESULT: ZERO non-MMIO-completing conds. Every cond function among the 27 sites reads GPU PRI space (GPU_REG_RD32 / kflcnRegRead_HAL / IoAperture REG_RD32 / GPU_REG_RD32_UNCHECKED) — all funnel to osDevReadReg032/008/016 and are short-circuited by osIsGpuBusDead. No sysmem-polled, doorbell-completed, or host-CPU-completed cond exists, so C7-e2's early break cannot convert a legitimately-completing wait into a failure. Additionally, since the dead GPU cannot DMA, no cond can NEWLY become true after loss; conds already true (including accidental-0xFFFFFFFF successes like _kfspIsGspTargetMaskReleased and _kgspWaitForResetAccess) exit via the while-condition evaluation BEFORE the loop-body C7-e2 check — no behavior change for accidental-success sites (and no regression; the next MMIO/RPC step errors out as today).","C7-e2 placement nuance confirmed safe: the quoted edit breaks without the final pCondFunc recheck at 598-602; vacuous given hazard-(b) result. Recommend inserting at loop top before osSpinLoop() exactly as the design says.","_issueRpcAndWait dead-bus/PDB-unset trace WITH C7 in place (the rpc.c special-attention question): _kgspRpcSendMessage calls _kgspRpcSanityCheck as its FIRST action (kernel_gsp.c:421, NV_CHECK_OK_OR_RETURN) -> C7-e4's osIsGpuBusLost term returns NV_ERR_GPU_IS_LOST BEFORE any message-queue write or doorbell MMIO write -> _issueRpcAndWait returns in microseconds at rpc.c:1903-1914; the recv-poll (and C7-e3) is never even reached. Teardown RPC cascades (RmShutdownAdapter frees, UNLOADING_GUEST_DRIVER) are bounded to us-per-RPC. If a send ever bypasses sanity, msgq:544 tx-full is C7-e6 and the doorbell is a posted write (dropped, no completion wait). Residual cost is print volume, not time — see needs_handling.","kfspWaitForResponse (kern_fsp.c:411, C7-e6 site #13): its ==NV_ERR_TIMEOUT last-chance recheck at :418 calls kfspIsResponseAvailable_HAL -> _kfspGetMsgQueueHeadTail reads head==tail==0xFFFFFFFF -> FALSE -> the C7-e6 break can NOT be accidentally flipped back to NV_OK. Verified kern_fsp_gh100.c:219-231.","Doorbell/MMIO WRITES are NOT short-circuited (osDevWriteReg032 has no dead-bus guard, os.c:1838-1873) — by design: posted writes to a disconnected device are dropped by the bridge without a completion wait. No write path found that blocks; no DMA is initiated from any audited path before a C7-bounded RPC/poll fails first."],"verdict":"needs-handling","needs_handling":["NO unbounded hardware touch, hang, or leak found in the os_pci-dead-but-PDB-unset end-state given C7-e1..e6 land as specced — the deltas below are low-severity semantics/log-hygiene fixes, but the first is recommended as REQUIRED because it targets the project's proven netconsole-print-storm wedge class.","C7-e7 (NEW, recommended REQUIRED): widen the fork's five PDB-only crash-safety guards to the C7 predicate `osIsGpuBusLost(pGpu)` (each a one-token change, read-only, no new locking): (1) rpc.c:1854 _issueRpcAndWait G2; (2) rpc.c:2097 _issueRpcLarge G3 (also kills the bare NV_ASSERT(0) at 2116/2153 firing per chunked RPC); (3) rpc.c:11530 rpcRmApiFree_GSP — the load-bearing one: restores the designed return-NV_OK-so-resserv-stays-quiet semantics; without it, a running-GPU teardown in the os_pci-only state emits one LEVEL_ERROR 'GspRmFree failed' + one NV_ASSERT line PER FREED OBJECT (bQuietPrints is never set on the GPU-lost sanity branch), i.e. a fresh log-storm amplifier of exactly the class that wedged apnex.31; (4) journal.c:2934 rcdbAddRmGpuDump; (5) nv_debug_dump.c:282 nvdDumpAllEngines break (keep the || PDB_PROP_GPU_INACCESSIBLE term).","C7-e8 (polish, optional): kern_fsp_gh100.c:637-638 G9 guard — replace `pGpu->getProperty(pGpu, PDB_PROP_GPU_IS_LOST)` with `osIsGpuBusLost(pGpu)`; currently the guard is dead in the os_pci-only state and _kfspWriteToEmem_GH100 exits via NV_ASSERT_OR_RETURN(:672) with NV_ERR_INVALID_STATE + assert-journal noise instead of the clean NV_ERR_GPU_IS_LOST it was written to produce (bounded either way).","Optional (note only, no change required): osapi.c:424 RmLogGpuCrash G6 uses positive-evidence AND (os_pci && PDB) — dead in the os_pci-only state, but the dump cascade beneath it is fully bounded by C7-e4; relaxing to osIsGpuBusLost would skip the useless dump and its log lines. Defensible either way.","Spec-confirmations (no delta): kernel_gsp.c:315 and :2814 are exactly where C7-e4/C7-e3 OR-in osIsGpuBusLost — both existing PDB readers become covered; C7-e3 must remain an OR-in (not a separate return) to preserve the bPollingForRpcResponse clear at 2820, verified present at source."],"surprises":["The audited tree already carries the fork's C5-v4/A13 guard layer ('v4 guard G2/G3/G6/G8/G9' comments) — most of the assigned 'readers' are the fork's OWN prior guards, and ALL of them key on PDB alone, so ALL are dead in the os_pci-set/PDB-unset end-state C7 creates. The dual-marker divergence systematically bypasses the fork's guard layer; it is C7's engine-level coverage (e2/e3/e4/e6), not the guard layer, that keeps the state bounded. C7-e7 re-coheres the layer cheaply.","GAP-7 premise verified at source: os.c:2050 short-circuit (osIsGpuBusDead returns TRUE on os_pci alone) returns at :2054 BEFORE the post-read DETECTOR_MMIO_DEAD funnel at :2081-2093 — once A13's early os_pci marker is set, the only MMIO-side PDB self-heal is permanently pre-empted; PDB can stay unset for the GPU's remaining lifetime.","RmLogGpuCrash (osapi.c:441-486) TEMPORARILY CLEARS PDB_PROP_GPU_IS_LOST (and IS_CONNECTED) and restores both after the dump (EEH/IBM-Power recovery hack) — harmless under the dual-marker design because osIsGpuBusDead also keys on os_pci, so MMIO stays short-circuited through the clear-window; in the PDB-unset state that whole block (including its RAW NV_PRIV_REG_RD32 probe that bypasses the short-circuit) is skipped entirely.","Two cond functions ACCIDENTALLY SUCCEED on the 0xFFFFFFFF dead-bus value and will return NV_OK before C7-e2 can fire: _kfspIsGspTargetMaskReleased (kern_fsp_gh100.c:1140-1144, GB202-bound) and _kgspWaitForResetAccess (kernel_gsp_gb100.c:51-60, GB202-bound via kgspResetHw_GB100). Both are immediately followed by steps that fail bounded — no action needed, but live-test log signatures should not expect a TIMEOUT from those two waits.","kfspWaitForSecureBoot_GB202 (kern_fsp_gb202.c:84) calls gpuMarkDeviceForReset(pGpu) on its timeout path — with C7-e2 this now fires promptly on a dead-bus re-open; it is an in-OBJGPU sysmem marker that dies with the teardown (no persistence concern), but it will appear in logs as a new early line.","knvlinkPrepareForXVEReset:1213-1216 has a braceless-if quirk (the PDB skip leaves stale loop `status` feeding the subsequent !=NV_OK error print) — upstream cosmetic bug, unreachable on this config, noted for any future upstream-PR sweep.","kernel_gsp.c's GPU-lost sanity branch (:317) logs at LEVEL_INFO and does NOT set pRpc->bQuietPrints (unlike the bFatalError branch at :302) — this is why the per-RPC/per-free LEVEL_ERROR consumers stay loud in the os_pci-only state, and is the mechanism behind the C7-e7 recommendation."]}]
