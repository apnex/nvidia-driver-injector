# #292 redesign — RAW workflow analysis (appendix to design-2026-06-06)

Full 19-agent output: 6 ground + 3 verify + 4 design + 4 review + critic + synthesis.
The synthesis is the design-of-record; this preserves the supporting analysis for perfect resume.

## GROUND
[{"area":"GSP-bootstrap poll-site / lost-marker coverage map for re-open RmInitAdapter->kgspBootstrap (#292 / A13 live-FAIL root-cause)","findings":["CORE MECHANISM (source-true): every blocking poll reachable from a re-open's kgspBootstrap is driven by one of TWO engines, and NEITHER honors os_pci_is_disconnected directly. (a) timeoutCondWait (gpu_timeout.c:591) loops `while(!condFn)`; its only non-timeout abort is API_GPU_IN_RESET_SANITY_CHECK inside timeoutCheck (gpu_timeout.c:483). (b) _kgspRpcRecvPoll (kernel_gsp.c:2682) aborts on bFatalError|PDB_PROP_GPU_IS_LOST (pre-loop 2813) and via _kgspRpcSanityCheck (2851). os_pci is honored ONLY inside osDevReadReg0xx via osIsGpuBusDead (os.c:1884,1892) i.e. it changes what the cond's MMIO READS return, never the loop's abort predicate. osIsGpuBusDead is referenced ONLY in os.c (MMIO read funnel) — confirmed not in kernel_gsp.c/gpu_timeout.c.","WHY A13 GOT PAST THE LOCKDOWN POLL BUT NOT THE RPC POLL: _kgspLockdownReleasedOrFmcError (kernel_gsp_gh100.c:499-542,1050) reads MAILBOX0 via MMIO; A13's os_pci makes that read return 0xFFFFFFFF; the cond's final clause `(mailbox0 != 0)` then evaluates TRUE -> the wait exits in one iteration. So os_pci 'covers' the lockdown poll ONLY BY ACCIDENT (0xFFFFFFFF is interpreted as an FMC error code), not by honoring the marker. The same accidental exit happens for kfspWaitForSecureBoot's _kfspWaitBootCond_GH100 (kern_fsp_gh100.c:805: NV_THERM_I2CS_SCRATCH reads 0xFF==_FSP_BOOT_COMPLETE_SUCCESS -> FALSE 'boot complete') and _kfspIsGspTargetMaskReleased. _kgspRpcRecvPoll has NO such MMIO clause in its abort predicate, so the all-1s reads cannot terminate it.","THE STORM SITE is the post-INIT_DONE init-RPC sequence (kgspInitRm SET_GUEST_SYSTEM_INFO @kernel_gsp.c:5105, GET_GSP_STATIC_INFO @5112, and all subsequent RmInitAdapter device-init control RPCs), each waiting via _issueRpcAndWait->rpcRecvPoll->_kgspRpcRecvPoll (vgpu/rpc.c:2002). Proof it is post-INIT_DONE: capture shows heartbeat timeout=5200, and heartbeat is only initialized in kgspWaitForRmInitDone (kernel_gsp.c:6162) AFTER INIT_DONE. Each storm block = one _kgspRpcRecvPoll return hitting the done: label heartbeat prints (kernel_gsp.c:2974-2980); 'Bad TimeLo ffffffff' is tmrGetTimeEx_GH100 (timer_gh100.c:124,153) reading the dead PTIMER inside the heartbeat check.","THE RPC POLL'S OWN LOST-DETECTOR IS DEAD CODE ON A DEAD BUS: in the timeout branch, _kgspCheckGspFatalHwError (kernel_gsp.c:2651) reads kflcnGetPendingHostInterrupts==0xFFFFFFFF -> kgspCheckGspPoisonError matches -> returns TRUE -> `goto done` (line 2877-2881) BEFORE the `if(bIsFatalTimeout) cleanupGpuLostStateAtomic(DETECTOR_GSP_HEARTBEAT_TIMEOUT)` at 2928-2931. So the v4 detector [c] that WOULD set PDB_PROP_GPU_IS_LOST never runs. Confirmed by capture: no 'GSP-RM unresponsive'/'GSP core unresponsive' classification lines (those live in _kgspClassifyGspTimeout, also skipped by the goto). Also gpuMarkDeviceForReset (gpu.c:6978->_gpuSetResetRequiredState) only writes a scratch register, NOT PDB_PROP_GPU_IN_FULLCHIP_RESET, so API_GPU_IN_RESET_SANITY_CHECK never trips either.","THE DISCONNECT-PATH SINK WAS LOCK-DEFERRED, not absent. nv_pci_error_detected's default/DISCONNECT case DOES call rm_cleanup_gpu_lost_state (nv-pci.c:3023) which WOULD set PDB_PROP_GPU_IS_LOST. But rm_cleanup_gpu_lost_state (osapi.c:1889) uses rmapiLockAcquire(API_LOCK_FLAGS_COND_ACQUIRE) at line 1913 — NON-BLOCKING. During the post-INIT_DONE storm the bootstrap worker has REACQUIRED the RM API lock (relaxed-init releases it only AROUND the GSP poll @kernel_gsp.c:4785, then reacquires — per the A10-v2 lock-model comment in nv.c:1959-1968), so COND_ACQUIRE is CONTENDED -> takes the else branch (osapi.c:1923-1933) -> logs 'API lock contended, deferring' -> PDB_PROP_GPU_IS_LOST NEVER SET. This refines the finding doc's 'A13 never set PDB_PROP_GPU_IS_LOST' to its true mechanism.","A13's OWN MARKER SUPPRESSES THE FALLBACK rm_cleanup RELIES ON (the decisive surprise). rm_cleanup's deferring branch (osapi.c:1925-1932) assumes 'subsequent MMIO reads will converge on the sink via DETECTOR_MMIO_DEAD'. But the DETECTOR_MMIO_DEAD funnel (os.c:2081-2092) is reached ONLY after a REAL hardware read (NV_PRIV_REG_RD32 @2062) returns 0xFFFFFFFF. Once A13 sets os_pci_is_disconnected, osDevReadReg032 short-circuits at os.c:2050-2055 and returns the dead value BEFORE ever reaching the funnel. So A13's early os_pci marker DISABLES the only remaining path that would have eventually set PDB_PROP_GPU_IS_LOST. Net: with A13, NO path sets PDB_PROP_GPU_IS_LOST (heartbeat-detector bypassed by poison-goto; rm_cleanup deferred by lock; MMIO funnel suppressed by os_pci). The bus is os_pci-dead but RM-alive -> the RPC poll spins forever.","THE CIRCULAR DEADLOCK: worker frees itself <= needs PDB_PROP_GPU_IS_LOST (the only marker _kgspRpcRecvPoll honors); PDB_PROP_GPU_IS_LOST set by AER DISCONNECT path <= needs the RM API lock; API lock held by the worker (reacquired post-bootstrap) <= worker not freed. A13 attacked this with os_pci, which breaks the cycle ONLY for MMIO-cond polls (lockdown/FSP), not for _kgspRpcRecvPoll. The wedge is then the foreground holding nvl->ldata_lock across the bounded flush_work, amplified by the synchronous netconsole printk storm (console_loglevel=8) — an observability-perturbs-the-bug factor (the 5200ms heartbeat bound means the worker is per-RPC bounded; the hard wedge is the lock-hold + printk-storm, not an unbounded single poll).","REDESIGN DIRECTION (coverage-complete, reconciled): The robust fix must set PDB_PROP_GPU_IS_LOST (and clear PDB_PROP_GPU_IS_CONNECTED) LOCK-FREE from the AER early path — NOT via rm_cleanup_gpu_lost_state (its COND_ACQUIRE will defer under exactly this contention). gpuSetDisconnectedProperties (gpu.c:5267) is SAFE at raised IRQL: it is pure property-bit writes + an osQueueWorkItem (bFallbackToDpc, bLockGpuGroupDevice) that defers the recovery-action refresh to a worker — no inline lock. A new lock-free rm entry that resolves pGpu via NV_GET_NV_PRIV_PGPU(nv) (the same deref rm_cleanup uses, no lock) and calls gpuSetDisconnectedProperties(pGpu)+os_pci_set_disconnected(nv->handle) would set BOTH markers so the RPC poll's pre-loop (PDB_PROP_GPU_IS_LOST) and _kgspRpcSanityCheck (!PDB_PROP_GPU_IS_CONNECTED + PDB_PROP_GPU_IS_LOST) BOTH short-circuit before the for-loop and before the done: heartbeat prints (no storm). The finding doc's fix-direction #3 (C' fail-fast BEFORE bringup) remains the only option that enters ZERO GSP polls and is the true whack-a-mole exit; A13b/option-1 is a per-poll backstop that, done right (lock-free dual-marker), also covers _kgspRpcRecvPoll. Option-2 (teach _kgspRpcRecvPoll to honor osIsGpuBusDead) is an L1 NVIDIA-core patch and only covers the RPC family.","SECONDARY/RESIDUAL: the exact storm multiplier (RmInitAdapter issuing a long sequence of post-INIT_DONE control RPCs each returning fast vs a tight re-dispatch) is not fully pinned from the capture window alone; the per-RPC return appears to be ~0.1-0.5ms which implies each _kgspRpcRecvPoll is returning faster than its full OSTIMER timeout — most consistent with a long RPC sequence where an early RPC's failure has not yet aborted RmInitAdapter, or a stuck-IRQ re-drive. This does not affect the marker-coverage conclusions (the done: heartbeat prints unambiguously come from _kgspRpcRecvPoll returns). Flag for the netconsole-minimised re-test the finding doc recommends."],"table":["poll-site | cond-fn | abort-marker-it-honors | timeout | reachable-from-reopen | covered-by-A13(os_pci)?","kfspWaitForSecureBoot_GH100 (kern_fsp_gh100.c:828; via kgspWaitForGfwBootOk_HAL @kernel_gsp.c:4990) | _kfspWaitBootCond_GH100 (reads NV_THERM_I2CS_SCRATCH, MMIO) | bare OSTIMER timeout + API_GPU_IN_RESET (timeoutCheck) | 4s OSTIMER (NV_MAX(4000000,defaultus)) | YES (early GFW/devinit) | ACCIDENTAL: 0xFFFFFFFF -> field reads 0xFF==_SUCCESS -> cond TRUE, exits 1 iter (false 'boot complete')","kfspPollForResponse/kfspWaitForResponse (kern_fsp.c:411) | kfspIsResponseAvailable_HAL (FSP EMEM queue head/tail, MMIO) | bare OSTIMER timeout + API_GPU_IN_RESET | GPU_TIMEOUT_DEFAULT OSTIMER | YES (FSP boot cmd responses) | ACCIDENTAL: cond evaluates on 0xFFFFFFFF MMIO; exits on garbage-or-timeout","kfspWaitForGspTargetMaskReleased_GH100 (kern_fsp_gh100.c:1158; @kgspBootstrap_GH100:986) | _kfspIsGspTargetMaskReleased (NV_PGSP HWCFG2 via GPU_REG_RD32_UNCHECKED, MMIO) | bare default timeout + API_GPU_IN_RESET | GPU_TIMEOUT_DEFAULT (thread-state) | YES | ACCIDENTAL: reg=0xFFFFFFFF -> (reg!=0 && (reg&mask)!=privErrTargetLocked) TRUE -> exits 1 iter","kgspResetHw_GH100 asserted/deasserted (kernel_gsp_gh100.c:133,144) | _kgspWaitForAsserted/_kgspWaitForDeasserted (FALCON_ENGINE RESET_STATUS, MMIO) | bare TMR timeout + API_GPU_IN_RESET | defaultResetFSMStateTransitionUs (GPU_TIMEOUT_FLAGS_TMR=PTIMER) | CONDITIONAL (reset/unload path; likely not in clean cold re-open) | ACCIDENTAL via MMIO; CAVEAT TMR timeout reads dead PTIMER->tmrGetTimeEx returns 0 -> timeout may not advance (edge hang)","_kgspEstablishSpdmSession SPDM/mailbox waits (kernel_gsp_gh100.c:620,662) | _kgspSpdmBootedOrFmcError / _kgspFalconMailbox0Cleared (MAILBOX0, MMIO) | bare default timeout + API_GPU_IN_RESET | GPU_TIMEOUT_DEFAULT | NO on this GPU (SPDM/CC disabled, no ConfCompute) | N/A (path not taken); would be ACCIDENTAL via MMIO if enabled","GspMsgQueueSendCommand tx-buffer-full wait (message_queue_cpu.c:544; via kgspSendInitRpcs + every RPC send) | msgqTxGetWriteBuffer!=NULL (libos TX queue free space, SYSMEM/DMA - NOT MMIO) | bare 1s OSTIMER per element + API_GPU_IN_RESET | 1s per queue element | YES (init RPC send) | NO - sysmem read, os_pci short-circuit does not apply; aborts only on bare 1s timeout","gpuTimeoutCondWait(_kgspLockdownReleasedOrFmcError) (kernel_gsp_gh100.c:1050) [ORIGINAL apnex.30 wedge] | _kgspLockdownReleasedOrFmcError (MAILBOX0/1 + HWCFG2, MMIO) | bare default timeout + API_GPU_IN_RESET | GPU_TIMEOUT_DEFAULT (thread-state RM timeout) | YES | YES (effective): 0xFFFFFFFF -> mailbox0!=0 -> cond TRUE 1 iter -> kgspBootstrap_GH100:1073 returns NV_ERR_NOT_READY (clean fail). This is what A13/A10-v2 target.","GspStatusQueueInit link wait (message_queue_cpu.c:352; @kgspBootstrap_GH100:1104) | msgqRxLink==0 (status queue header, SYSMEM) + kgspHealthCheck_HAL (MMIO mailbox) | 4s timeout + API_GPU_IN_RESET + kgspHealthCheck->NV_ERR_RESET_REQUIRED | 4s (GPU_TIMEOUT_DEFAULT) | YES (between lockdown and INIT_DONE) | PARTIAL: queue cond is sysmem (immune); but kgspHealthCheck MMIO short-circuits 0xFFFFFFFF -> may break via RESET_REQUIRED; else bare 4s timeout","kgspWaitForRmInitDone -> rpcRecvPoll(INIT_DONE) -> _kgspRpcRecvPoll (kernel_gsp.c:6151 / 2682) | _kgspRpcDrainEvents (INIT_DONE in sysmem queue) + per-iter _kgspRpcSanityCheck | pre-loop(2813): bFatalError|PDB_PROP_GPU_IS_LOST; sanity(2851): bFatalError, API_GPU_IN_RESET, !PDB_PROP_GPU_IS_CONNECTED|PDB_PROP_GPU_IS_LOST, osIsGpuShutdown(nv->is_shutdown), !fullPower, !sysmem | ~1.5x defaultus OSTIMER (heartbeat NOT yet init) | YES | NO: none of its abort markers check os_pci; MMIO reads short-circuit silently (suppress DETECTOR_MMIO_DEAD); exits only on bare timeout then poison-goto bypasses cleanupGpuLostStateAtomic","POST-INIT_DONE init RPCs -> _issueRpcAndWait -> rpcRecvPoll -> _kgspRpcRecvPoll (vgpu/rpc.c:2002; SET_GUEST_SYSTEM_INFO @kernel_gsp.c:5105, GET_GSP_STATIC_INFO @5112, all RmInitAdapter ctrl RPCs) [LIVE A13 STORM/WEDGE SITE] | same as INIT_DONE row + heartbeat checks (mailbox + tmrGetTimeEx PTIMER, MMIO) | same markers (pre-loop PDB_PROP_GPU_IS_LOST/bFatalError; _kgspRpcSanityCheck) | ~1.5x defaultus OSTIMER per RPC; heartbeat 5200ms | YES (the captured wedge) | NO: same gap AS ABOVE, PLUS worker now holds RM API lock (reacquired post-bootstrap) so rm_cleanup_gpu_lost_state COND_ACQUIRE DEFERS -> PDB_PROP_GPU_IS_LOST never set by DISCONNECT path either"],"surprises":["A13's own os_pci marker DISABLES the DETECTOR_MMIO_DEAD post-read funnel (os.c:2050 short-circuits before os.c:2081), defeating the exact 'MMIO reads converge on the sink' fallback that rm_cleanup_gpu_lost_state's deferring branch (osapi.c:1925) explicitly relies on. A13 thus removes the one remaining path that would have eventually set PDB_PROP_GPU_IS_LOST.","The DISCONNECT path DID invoke the full C5 sink (rm_cleanup_gpu_lost_state, nv-pci.c:3023) but it silently DEFERRED on rmapiLockAcquire(COND_ACQUIRE) (osapi.c:1913) because the worker holds the RM API lock during post-INIT_DONE RPCs. So 'A13 never set PDB_PROP_GPU_IS_LOST' is true in effect but the RM-marker-setter actually ran and was lock-gated out — a subtler mechanism than the finding doc states.","_kgspRpcRecvPoll's own GSP-lost detector (cleanupGpuLostStateAtomic via DETECTOR_GSP_HEARTBEAT_TIMEOUT, line 2930) is unreachable on an all-1s bus: _kgspCheckGspFatalHwError reads 0xFFFFFFFF intrStatus, matches poison, and `goto done` (2877) before the detector. The poll's lost-detection self-defeats precisely when the GPU is most-lost.","os_pci 'covering' the lockdown poll is an ACCIDENT of semantics: _kgspLockdownReleasedOrFmcError treats mailbox0==0xFFFFFFFF as an FMC error code (mailbox0!=0). Likewise _kfspWaitBootCond_GH100 reads 0xFF==FSP_BOOT_COMPLETE_SUCCESS on a dead bus (a FALSE success). A13/A10-v2's 'one-iteration self-terminate' chain works only for cond functions whose abort clause happens to be satisfied by all-1s reads.","gpuMarkDeviceForReset (gpu.c:6978) does NOT set PDB_PROP_GPU_IN_FULLCHIP_RESET (only a scratch register via gpuSetResetScratchBit_HAL), so the poison path does NOT trip API_GPU_IN_RESET_SANITY_CHECK — removing the one timeoutCheck/sanity-check abort that might have fast-failed the polls.","_kgspRpcSanityCheck's connectivity gate uses osIsGpuShutdown (os.c:6056) which reads ONLY nv->is_shutdown, never os_pci_is_disconnected — yet another lost-marker the RPC poll honors that A13 does not set."],"contradicts_prior":"\"No hard contradiction; two REFINEMENTS of prior findings. (1) Finding fix-direction #1 frames the A13b risk as 'gpuSetDisconnectedProperties/the sink may need the GPU group lock the worker holds.' Source-true: gpuSetDisconnectedProperties (gpu.c:5267) needs NO inline lock (pure property writes + a deferred osQueueWorkItem, safe at raised IRQL); the ACTUAL contention is the RM API lock taken by rm_cleanup_gpu_lost_state's COND_ACQUIRE (osapi.c:1913), which the worker holds AFTER bootstrap reacquisition (kernel_gsp.c:4785 reacquire). So A13b MUST set the dual marker lock-free via a NEW direct entry (NV_GET_NV_PRIV_PGPU + gpuSetDisconnectedProperties), NOT by routing through rm_cleanup_gpu_lost_state, or it will defer exactly as the live FAIL did. (2) MEMORY/A30 pins #292 as the lockdown gpuTimeoutCondWait holding the GPU group lock; the live A13 result ADVANCES (not contradicts) this: the post-A13 wedge is _kgspRpcRecvPoll holding the GPU group lock AND the reacquired RM API lock — same group-lock-hold family, deeper site. The 'silent lockdown hang' (netcon2) vs 'RPC heartbeat storm' (netcon3) difference is just WHEN the diverged chip falls off the bus (at lockdown vs after INIT_DONE), reinforcing the mandate to cover every poll site, not a changed root cause.\"","code_sites":["/root/open-gpu-kernel-modules/src/nvidia/src/kernel/gpu/gsp/kernel_gsp.c:2682 (_kgspRpcRecvPoll)","/root/open-gpu-kernel-modules/src/nvidia/src/kernel/gpu/gsp/kernel_gsp.c:2813 (pre-loop short-circuit: bFatalError||PDB_PROP_GPU_IS_LOST)","/root/open-gpu-kernel-modules/src/nvidia/src/kernel/gpu/gsp/kernel_gsp.c:2851 (_kgspRpcSanityCheck call) and :291-335 (def; osIsGpuShutdown @320)","/root/open-gpu-kernel-modules/src/nvidia/src/kernel/gpu/gsp/kernel_gsp.c:2877 (_kgspCheckGspFatalHwError goto done) :2651 (def) :2930 (cleanupGpuLostStateAtomic, bypassed)","/root/open-gpu-kernel-modules/src/nvidia/src/kernel/gpu/gsp/kernel_gsp.c:2974-2980 (done: heartbeat-timeout prints = the storm)","/root/open-gpu-kernel-modules/src/nvidia/src/kernel/gpu/gsp/kernel_gsp.c:6151 (kgspWaitForRmInitDone rpcRecvPoll INIT_DONE) :6162 (_kgspHeartbeatInit)","/root/open-gpu-kernel-modules/src/nvidia/src/kernel/gpu/gsp/kernel_gsp.c:5105/5112 (post-INIT_DONE SET_GUEST_SYSTEM_INFO/GET_GSP_STATIC_INFO) :5047-5077 (boot retry loop)","/root/open-gpu-kernel-modules/src/nvidia/src/kernel/gpu/gsp/arch/hopper/kernel_gsp_gh100.c:1050 (lockdown gpuTimeoutCondWait) :499-542 (_kgspLockdownReleasedOrFmcError) :133/144 (kgspResetHw waits) :620/662 (SPDM) :1073 (mailbox0!=0 clean-fail)","/root/open-gpu-kernel-modules/src/nvidia/src/kernel/gpu/gpu_timeout.c:591 (timeoutCondWait loop) :483 (API_GPU_IN_RESET in timeoutCheck)","/root/open-gpu-kernel-modules/src/nvidia/arch/nvalloc/unix/src/os.c:1884 (osIsGpuBusDead) :1921 (cleanupGpuLostStateAtomic) :2050-2055 (dead-bus short-circuit) :2081-2092 (DETECTOR_MMIO_DEAD funnel, suppressed) :6056 (osIsGpuShutdown=nv->is_shutdown)","/root/open-gpu-kernel-modules/src/nvidia/arch/nvalloc/unix/src/osapi.c:1889 (rm_cleanup_gpu_lost_state) :1913 (COND_ACQUIRE) :1923-1933 (deferring branch)","/root/open-gpu-kernel-modules/src/nvidia/src/kernel/gpu/gpu.c:5267 (gpuSetDisconnectedProperties, raised-IRQL-safe) :6978 (gpuMarkDeviceForReset->scratch only)","/root/open-gpu-kernel-modules/src/nvidia/generated/g_gpu_nvoc.h:5873 (API_GPU_IN_RESET_SANITY_CHECK) :5883 (API_GPU_ATTACHED_SANITY_CHECK)","/root/open-gpu-kernel-modules/src/nvidia/src/kernel/gpu/fsp/arch/hopper/kern_fsp_gh100.c:805/828 (_kfspWaitBootCond_GH100/kfspWaitForSecureBoot) :1143/1158 (_kfspIsGspTargetMaskReleased) and /root/open-gpu-kernel-modules/src/nvidia/src/kernel/gpu/fsp/kern_fsp.c:411 (kfspWaitForResponse)","/root/open-gpu-kernel-modules/src/nvidia/src/kernel/gpu/gsp/message_queue_cpu.c:352 (GspStatusQueueInit) :544 (GspMsgQueueSendCommand tx-full)","/root/open-gpu-kernel-modules/src/nvidia/src/kernel/vgpu/rpc.c:1834/2002 (_issueRpcAndWait->rpcRecvPoll)","/root/open-gpu-kernel-modules/kernel-open/nvidia/nv-pci.c:2974-2982 (A13 early lock-free os_pci-only) :3023 (DISCONNECT-path rm_cleanup_gpu_lost_state)","/root/open-gpu-kernel-modules/kernel-open/nvidia/nv.c:1924 (set bootstrap_in_flight) :2002-2003 (A10-v2 lockdown arm os_pci+rm_cleanup) :1959-1968 (lock-model comment) :2037 (flush_work join)","/root/open-gpu-kernel-modules/src/nvidia/src/kernel/gpu/timer/arch/hopper/timer_gh100.c:124/153 (tmrGetTimeEx_GH100 'Bad TimeLo')"]},{"area":"MARKER+LOCK matrix for #292 GSP-bootstrap re-open wedge: who-sets/who-checks/lock-to-set for os_pci_is_disconnected, PDB_PROP_GPU_IS_LOST, pci_channel_state, the C5 sink; plus full enumeration of every GSP-bootstrap poll/wait site reachable from a re-open and which abort-marker each honors. Decides A13b full-sink viability vs self-deadlock.","findings":["A13b VERDICT — DOES NOT self-deadlock on the GPU group lock, but the standard entry (rm_cleanup_gpu_lost_state) is UNRELIABLE during in-flight init. Lock trace: gpuSetDisconnectedProperties_IMPL (gpu.c:5266) sets PDB_PROP_GPU_IS_LOST via setProperty — a LOCK-FREE in-memory PDB bit write — at line 5272, with NO group-lock and NO API-lock acquired. Its ONLY lock-touching side effect is a DEFERRED osQueueWorkItem(_gpuSetDisconnectedPropertiesWorker, bLockGpuGroupDevice=TRUE) at 5285; osQueueWorkItem (os.c:770) → os_queue_work_item (os-interface.c:1175) merely kmalloc+nv_kthread_q_schedule_q_item and returns immediately — it NEVER blocks on the group lock. The deferred worker (which does want the group lock) runs async, queues behind the bootstrap worker, and runs after bootstrap releases. So the AER thread can set PDB_PROP_GPU_IS_LOST without blocking on the group lock the worker holds. The historical self-deadlock (pre-A10) was an API-LOCK inversion from a BLOCKING rmapiLockAcquire(API_LOCK_FLAGS_NONE), already replaced by COND_ACQUIRE (osapi.c:1913).","ROOT CAUSE of A13 live FAIL CONFIRMED against source: _kgspRpcRecvPoll honors ONLY RM markers, never os_pci. Two checks: pre-loop (kernel_gsp.c:2813-2814 `pKernelGsp->bFatalError || getProperty(PDB_PROP_GPU_IS_LOST)` → return NV_ERR_RESET_REQUIRED) and per-iteration via _kgspRpcSanityCheck (kernel_gsp.c:314-318 `!API_GPU_ATTACHED_SANITY_CHECK(pGpu) || getProperty(PDB_PROP_GPU_IS_LOST)` → NV_ERR_GPU_IS_LOST, called every loop iter at 2851). NEITHER checks os_pci_is_disconnected. osIsGpuBusDead (os.c:1884, the function that DOES OR-in os_pci) is `static inline` in os.c and referenced ONLY by the osDevReadRegNNN crash-safety guards — it is NOT called from kernel_gsp.c. A13 sets ONLY os_pci_set_disconnected (nv-pci.c:2981, lock-free) and never PDB_PROP_GPU_IS_LOST, so the RPC poll never short-circuits. User hypothesis #3 verified true.","WHY the AER path's existing C5 sink did not rescue it: nv_pci_error_detected DISCONNECT branch ALREADY calls rm_cleanup_gpu_lost_state(NV_GPU_LOST_DETECTOR_AER_FATAL) (nv-pci.c:3023). That wrapper (osapi.c:1889) gates the sink behind rmapiLockAcquire(API_LOCK_FLAGS_COND_ACQUIRE) on the API lock (osapi.c:1913) and behind pGpu=NV_GET_NV_PRIV_PGPU(pNv)!=NULL (1915-1916). COND_ACQUIRE is non-blocking (A10/C1 fix) so NO deadlock, but it is a NO-OP when contended. CAPTURE PROOF (netcon3): 'lost via detector_class' count = 0 → cleanupGpuLostStateAtomic NEVER ran its log (os.c:1966), i.e. PDB_PROP_GPU_IS_LOST was never set by the sink; meanwhile DISCONNECT=2 and 'in-flight bootstrap'=2 (A13 os_pci marker fired). So the wrapper-routed full-sink demonstrably did NOT land the RM marker. Conclusion: A13b ‘full-sink from the AER thread via the wrapper’ is already effectively present and is INSUFFICIENT — it is safe-but-ineffective, not missing.","LOCK-MODEL reconciliation (no regression): the worker holds the GPU GROUP lock across the bootstrap poll, NOT the API lock, when relaxed GSP-init locking is active. Verified at kernel_gsp.c:4783-4820: bRelaxedLocking=_kgspShouldRelaxGspInitLocking(pGpu) → rmapiLockRelease() at 4785 BEFORE kgspBootstrap_HAL (4798) and _kgspBootReacquireLocks after (4818). kgspBootstrap_HAL(GH100) contains BOTH the lockdown poll (gh100:1050) and kgspWaitForRmInitDone→RPC poll (gh100:1107). This CONFIRMS the nv.c ‘corrected 2026-06-02’ comment (nv.c:1959-1968) and the superseded-lock-model note. The outer rm_init_adapter DOES take the API lock (osapi.c:1761, blocking, across RmInitAdapter) but kgspInitRm transiently drops it across the poll under relaxed locking — so the A10 comment’s phrase ‘worker holding the same [API] lock’ is only literally true when relaxed-locking is OFF or in the brief reacquire window.","timeoutCondWait (gpu_timeout.c:571) — the shared primitive behind EVERY gpuTimeoutCondWait site (lockdown/SPDM/mailbox/reset/CC-cleanup) — honors NO lost-marker at all. Its loop is `while(!pCondFunc(pGpu,data)){osSpinLoop(); timeoutCheck();}`; timeoutCheck (470) only checks API_GPU_IN_RESET_SANITY_CHECK (483) + thread/local timeout. It does NOT check PDB_PROP_GPU_IS_LOST and does NOT check os_pci. Therefore a cond-wait’s only dead-bus abort path is whether the cond-fn’s OWN MMIO read of 0xFFFFFFFF happens to evaluate TRUE. This makes coverage ACCIDENTAL and per-cond-fn: _kgspLockdownReleasedOrFmcError (gh100:499) returns TRUE on dead read (mailbox0!=0 branch) so the lockdown poll DID terminate fast under A13 (why A13 got PAST lockdown into the RPC poll); but _kgspFalconMailbox0Cleared (gh100:582 returns mailbox0==0) returns FALSE on 0xFFFFFFFF → would hang the full timeout. This is the ‘incomplete per-poll marker coverage’ class the user flagged.","RECOMMENDED viable redesign (lock-independent, reliable): make the poll SITES honor the LOCK-FREE os_pci marker A13 already sets, instead of depending on the COND-gated AER→RM sink. Two landing zones cover everything: (1) add an osIsGpuBusDead-equivalent (os_pci OR PDB) short-circuit INSIDE timeoutCondWait’s loop (gpu_timeout.c:591) → covers ALL gpuTimeoutCondWait sites uniformly (lockdown, SPDM, mailbox, reset asserted/deasserted, CC-cleanup) in one place; (2) add the same check to _kgspRpcRecvPoll pre-loop (2813) AND _kgspRpcSanityCheck (314) → covers the RPC INIT_DONE poll and all RPC waits. osIsGpuBusDead is static inline in os.c, so the implementation needs either to un-static it + header-declare, or expose os_pci_is_disconnected(NV_GET_NV_STATE(pGpu)->handle) via a thin gpu-level helper. This needs NO new lock acquisition and rides A13’s already-proven lock-free WRITE_ONCE marker.","ALTERNATIVE A13b that IS viable without deadlock: from the AER handler, set PDB_PROP_GPU_IS_LOST LOCK-FREE and DIRECTLY (bypass the wrapper’s COND API-lock guard) — i.e. call gpuSetDisconnectedProperties(pGpu) (or pGpu->setProperty(PDB_PROP_GPU_IS_LOST,NV_TRUE)) without rmapiLockAcquire. This is a bitfield write → no group-lock, no API-lock, no deadlock. It would trip the RPC poll’s existing PDB checks. Caveats: (a) requires resolving pGpu from the Linux layer (NV_GET_NV_PRIV_PGPU, non-NULL since osAttachGpu links nvp->pGpu=pGpu early, osinit.c:311/osAttachGpu, before kgspBootstrap); (b) a narrow concurrent unsynchronized PDB-bitfield write vs the worker (monotonic false→true, same-word torn-write risk only); (c) it sets PDB but NOT PDB_PROP_GPU_IS_CONNECTED unless full gpuSetDisconnectedProperties is used. The poll-site-honors-os_pci approach (above) is cleaner because os_pci is already set lock-free and proven.","pci_channel_state gate bug (the ‘(a)’ the user cited): nv_pci_error_detected receives pci_channel_state_t state from the Linux AER core. A13 DELIBERATELY does NOT gate the early-free on channel state (nv-pci.c:2966 ‘do NOT test channel state’) because the captured #292 AER is Uncorrectable NON-fatal = pci_channel_io_normal; gating on frozen/perm_failure would have missed it. The early-free is gated on atomic_read(&nvl->bootstrap_in_flight) ALONE (nv-pci.c:2974). The Linux pci_dev_is_disconnected flag (what os_pci_is_disconnected READ_ONCEs) is set both by A13’s os_pci_set_disconnected WRITE_ONCE and by the kernel AER state machine on the DISCONNECT return path (nv-pci.c:3006).","The C5 sink cleanupGpuLostStateAtomic (os.c:1921) is itself LOCK-FREE: idempotent gate on getProperty(PDB_PROP_GPU_IS_LOST) (1935), then gpuSetDisconnectedProperties (1938, lock-free + async queue) + os_pci_set_disconnected (1942, lock-free WRITE_ONCE) + a per-(gpu,detector) __atomic_fetch_or log latch (1962). It takes NO lock. The 8 detector classes are DETECTOR_MMIO_DEAD=0, OSHANDLEGPULOST_RETRY_EXHAUSTED=1, GSP_HEARTBEAT_TIMEOUT=2, AER_FATAL=3, QWATCHDOG_DMA_WEDGE=4, PROBE_BAR_FAILURE=5, SYSFS_DISCONNECTED=6, UVM_FATAL=7 (nv-gpu-lost.h:114-121). RM-context callers (osinit.c:442, os.c:2090, kernel_gsp.c:2930) call it directly under their own locks; the cross-layer AER/QWD path enters via the rm_cleanup_gpu_lost_state COND-API-lock wrapper.","API_GPU_ATTACHED_SANITY_CHECK (g_gpu_nvoc.h:5883) keys on PDB_PROP_GPU_IS_CONNECTED (which gpuSetDisconnectedProperties clears at gpu.c:5273) — NOT os_pci. So a FULL sink would short-circuit the RPC poll via TWO conditions (sanity line 314 connected=false OR line 315 lost=true). This is further confirmation that the missing ingredient is landing the RM marker, and that the os_pci-only marker is invisible to every RM sanity gate."],"table":["poll-site=kgspResetHw_GH100 gpuTimeoutCondWait(gh100:133) | cond-fn=_kgspWaitForAsserted (RESET_STATUS bit, MMIO) | honors=cond-fn-MMIO + API_GPU_IN_RESET | timeout=local RMTIMEOUT | reachable-from-reopen=YES(early HW reset) | A13/os_pci-covered=PARTIAL (0xFFFFFFFF may flip asserted TRUE) | A13b-full-sink-needed=via timeoutCondWait fix","poll-site=kgspResetHw_GH100 gpuTimeoutCondWait(gh100:144) | cond-fn=_kgspWaitForDeasserted (RESET_STATUS bit) | honors=cond-fn-MMIO + API_GPU_IN_RESET | timeout=local | reachable=YES | A13/os_pci-covered=NO (deasserted FALSE on 0xFFFFFFFF → full timeout) | needs=timeoutCondWait marker check","poll-site=kgspWaitForGfwBootOk_GH100→kfspWaitForSecureBoot (gh100:247/257) | cond-fn=GFW_BOOT scratch own-poll | honors=its MMIO + timeout | reachable=YES (pre-lockdown) | A13/os_pci-covered=depends on dead-value decode | needs=audit + marker check in fsp poll","poll-site=kfspWaitForGspTargetMaskReleased (gh100:986) | cond-fn=target-mask own-poll | honors=its MMIO + timeout | reachable=YES | A13/os_pci-covered=depends | needs=audit","poll-site=_kgspEstablishSpdmSession gpuTimeoutCondWait(gh100:620) | cond-fn=_kgspSpdmBootedOrFmcError (mailbox0!=0→TRUE) | honors=cond-fn-MMIO | reachable=ONLY-if-SPDM (off on consumer 5090) | A13/os_pci-covered=YES-incidental (dead→TRUE) | needs=timeoutCondWait fix for robustness","poll-site=_kgspEstablishSpdmSession gpuTimeoutCondWait(gh100:662) | cond-fn=_kgspFalconMailbox0Cleared (mailbox0==0) | honors=cond-fn-MMIO | reachable=ONLY-if-SPDM | A13/os_pci-covered=NO (0xFFFFFFFF!=0→FALSE→full timeout) | needs=timeoutCondWait marker check","poll-site=gpuTimeoutCondWait LOCKDOWN (gh100:1050) | cond-fn=_kgspLockdownReleasedOrFmcError (mailbox0!=0 || HWCFG2 _UNLOCK) | honors=cond-fn-MMIO + API_GPU_IN_RESET (NOT PDB, NOT os_pci) | timeout=RM gpuTimeout(~unbounded) | reachable=YES | A13/os_pci-covered=YES-incidental (dead→mailbox0!=0→TRUE) ← apnex.30 SILENT wedge site; A13 sped this up | needs=timeoutCondWait marker check (don't rely on FMC-error accident)","poll-site=kgspWaitForRmInitDone→_kgspRpcRecvPoll(GSP_INIT_DONE) (gh100:1107→kernel_gsp.c:6151→2682) | cond-fn=RPC drain loop | honors=PDB_PROP_GPU_IS_LOST (pre-loop 2814 + sanity 315), PDB_PROP_GPU_IS_CONNECTED (sanity 314), API_GPU_IN_RESET, osIsGpuShutdown — NOT os_pci | timeout=1.5×default RPC + heartbeat 5200ms | reachable=YES | A13/os_pci-covered=NO ← #292 MOVED HERE, A13 STORM/wedge site | needs=add os_pci to pre-loop 2813 + _kgspRpcSanityCheck 314","poll-site=storm-amplifier gpuCheckTimeout in RPC loop (kernel_gsp.c:2830) | cond-fn=tmrGetTimeEx_GH100 PTIMER MMIO | honors=nothing(time read) | reachable=YES | effect=0xFFFFFFFF→'Bad TimeLo'→fast spin → printk storm (console_loglevel=8 netconsole amplifies) | needs=upstream of fix; marker short-circuit at 314/2813 exits loop before this storms","poll-site=kgspCheckGspRmCcCleanup gpuTimeoutCondWait(gh100:1271) | cond-fn=_kgspHasCcCleanupFinished | honors=cond-fn-MMIO | reachable=teardown/CC-only (not normal reopen) | A13/os_pci-covered=depends | needs=timeoutCondWait fix covers it"],"surprises":["The AER handler ALREADY dispatches the full C5 sink (rm_cleanup_gpu_lost_state) in its DISCONNECT branch (nv-pci.c:3023) — so 'A13b = add the full sink to the AER path' is NOT a new idea; it is already coded and the capture (detector_class log=0) proves it did not land the RM marker. The bug is that the sink is COND-API-lock-gated and pGpu-gated, not that it's absent.","timeoutCondWait honors NO GPU-lost marker whatsoever — not even PDB_PROP_GPU_IS_LOST. Every lockdown/SPDM/mailbox poll's dead-bus termination is an accident of whether 0xFFFFFFFF makes that specific cond-fn return TRUE. _kgspLockdownReleasedOrFmcError happens to (mailbox0!=0), which is the ONLY reason A13 advanced past lockdown rather than hanging there too.","A13 made the host wedge MORE violent, not less: by short-circuiting dead-bus MMIO reads to a fast 0xFFFFFFFF (via osIsGpuBusDead in osDevReadReg032), it converted apnex.30's slow-silent lockdown hang into a fast-spinning RPC-poll printk storm (Bad TimeLo×47, Heartbeat×46). The 5200ms bounded heartbeat timeout means the worker IS bounded — the hard wedge is partly observability-induced (synchronous netconsole printk at loglevel 8) layered on the real ldata_lock hold.","The pre-loop short-circuit in _kgspRpcRecvPoll (2813) is checked ONCE; the per-iteration coverage that actually matters comes from _kgspRpcSanityCheck (315) being called every loop iteration at 2851 BEFORE the timeout/heartbeat block (2866) — so adding os_pci there exits the loop cleanly on the first post-marker iteration, before any storm.","setProperty(PDB_PROP_GPU_IS_LOST) is genuinely lock-free; the ONLY group-lock acquisition anywhere in gpuSetDisconnectedProperties is a DEFERRED async work item (recovery-action refresh, gpu.c:5285) — cosmetic, and harmless if it queues behind the bootstrap worker. So the entire 'A13b self-deadlocks on the group lock' worry is unfounded for the marker write itself."],"contradicts_prior":"\"NO hard contradiction with the established (post-supersession) understanding — my trace CONFIRMS the 'corrected 2026-06-02' model (relaxed GSP-init locking releases the API lock at kernel_gsp.c:4785 so the worker holds only the GPU GROUP lock across the bootstrap poll). One REFINEMENT, not a contradiction: the A10/C1 comment in rm_cleanup_gpu_lost_state (osapi.c:1906-1912) describes the worker as 'holding the same [API] lock', which is only literally true when relaxed-locking is OFF (or in the brief reacquire window at 4818); under default relaxed-locking the API lock is FREE during the poll, in which case rm_cleanup's COND_ACQUIRE would SUCCEED — yet the capture shows the sink still did not land PDB (detector_class log=0). That residual (whether relaxed-locking was active on this particular persistence-OFF re-open, vs a dropped LEVEL_WARNING 'API lock contended' line amid the storm, vs an unlikely NULL pGpu) is NOT fully pinned by the capture and should be confirmed with higher-fidelity (non-storm) capture. It does NOT change the verdict: every branch leaves the RPC poll honoring only RM markers while A13 sets only os_pci, so the lock-independent fix (make the poll sites honor the lock-free os_pci marker) is correct regardless.\"","code_sites":["/root/open-gpu-kernel-modules/src/nvidia/src/kernel/gpu/gpu.c:5266 gpuSetDisconnectedProperties_IMPL (setProperty PDB_PROP_GPU_IS_LOST lock-free @5272; deferred bLockGpuGroupDevice queue @5285)","/root/open-gpu-kernel-modules/src/nvidia/arch/nvalloc/unix/src/os.c:1884 osIsGpuBusDead (static inline; ORs os_pci @1892 + PDB @1895)","/root/open-gpu-kernel-modules/src/nvidia/arch/nvalloc/unix/src/os.c:1921 cleanupGpuLostStateAtomic (C5 sink, lock-free, idempotent @1935)","/root/open-gpu-kernel-modules/src/nvidia/arch/nvalloc/unix/src/osapi.c:1889 rm_cleanup_gpu_lost_state (COND_ACQUIRE API lock @1913; pGpu @1915; deferring branch @1928)","/root/open-gpu-kernel-modules/src/nvidia/arch/nvalloc/unix/src/osapi.c:1732 rm_init_adapter (API lock blocking @1761 across RmInitAdapter @1772)","/root/open-gpu-kernel-modules/src/nvidia/src/kernel/gpu/gsp/kernel_gsp.c:4783 _kgspShouldRelaxGspInitLocking + rmapiLockRelease @4785 / reacquire @4818","/root/open-gpu-kernel-modules/src/nvidia/src/kernel/gpu/gsp/kernel_gsp.c:2682 _kgspRpcRecvPoll (pre-loop PDB/bFatalError @2813-2822; loop @2824; sanity call @2851)","/root/open-gpu-kernel-modules/src/nvidia/src/kernel/gpu/gsp/kernel_gsp.c:314 _kgspRpcSanityCheck (PDB_PROP_GPU_IS_LOST + API_GPU_ATTACHED_SANITY_CHECK @314-318)","/root/open-gpu-kernel-modules/src/nvidia/src/kernel/gpu/gsp/kernel_gsp.c:2278 _kgspIsHeartbeatTimedOut (storm print @2311); cleanupGpuLostStateAtomic(DETECTOR_GSP_HEARTBEAT_TIMEOUT) @2930","/root/open-gpu-kernel-modules/src/nvidia/src/kernel/gpu/gsp/arch/hopper/kernel_gsp_gh100.c:499 _kgspLockdownReleasedOrFmcError; lockdown gpuTimeoutCondWait @1050; kgspBootstrap_GH100 @935; RmInitDone wait @1107","/root/open-gpu-kernel-modules/src/nvidia/src/kernel/gpu/gpu_timeout.c:571 timeoutCondWait (honors no lost-marker; timeoutCheck @470 only API_GPU_IN_RESET @483)","/root/open-gpu-kernel-modules/src/nvidia/arch/nvalloc/unix/src/os.c:770 osQueueWorkItem → /root/open-gpu-kernel-modules/kernel-open/nvidia/os-interface.c:1175 os_queue_work_item (enqueue-only, non-blocking)","/root/open-gpu-kernel-modules/kernel-open/nvidia/nv-pci.c:2956 nv_pci_error_detected (A13 early os_pci marker @2974-2982; DISCONNECT→rm_cleanup @3023; no channel-state gate @2966)","/root/open-gpu-kernel-modules/kernel-open/nvidia/nv.c:1959 corrected lock-model comment; F40b lockdown-arm os_pci+sink @2002-2003","/root/open-gpu-kernel-modules/src/nvidia/generated/g_gpu_nvoc.h:5883 API_GPU_ATTACHED_SANITY_CHECK (PDB_PROP_GPU_IS_CONNECTED, not os_pci)","/root/open-gpu-kernel-modules/src/nvidia/inc/kernel/gpu/nv-gpu-lost.h:114 DETECTOR_* enum (AER_FATAL=3)","/root/open-gpu-kernel-modules/src/nvidia/arch/nvalloc/unix/src/osinit.c:311 osAttachGpu nvp->pGpu=pGpu (early linkage → NV_GET_NV_PRIV_PGPU non-NULL during poll)","/root/nvidia-driver-injector/patches/addon/A13-292-inflight-aer-earlyfree.patch (bootstrap_in_flight gate; os_pci-only, never PDB)"]},{"area":"#292 A13-live RPC-poll hard-wedge: mechanism (ldata_lock/F44 vs printk-storm), boundedness of the worker, and what a minimised-observability re-test proves","findings":["ROOT IS NEITHER (a) NOR (b) — it is an UNBOUNDED GSP-RPC-poll retry storm that A13's bare marker created. A13 calls os_pci_set_disconnected DIRECTLY (nv-pci.c:2981), setting ONLY the Linux marker, never PDB_PROP_GPU_IS_LOST. The RPC poll's pre-loop abort gate (kernel_gsp.c:2813-2822) checks ONLY `pKernelGsp->bFatalError || PDB_PROP_GPU_IS_LOST` — it does NOT consult os_pci_is_disconnected. So the pre-loop never short-circuits; the worker enters the for-loop on every re-invocation.","EACH 6-line group in the storm = ONE return of _kgspRpcRecvPoll. The 'GSP RM/LibOS heartbeat timed out' strings live ONLY at the done: label (kernel_gsp.c:2974-2981), printed unconditionally whenever the heartbeat helpers return true. Cadence 0.3-3ms/return (e.g. netcon3:707 [1397.628173] -> netcon3:713 [1397.630532] = 2.36ms) proves each call returns FAST, NOT after a multi-second timeout. So the worker is being re-invoked by an OUTER retry loop ABOVE kgspWaitForRmInitDone (which itself calls rpcRecvPoll once, NV_CHECK_OK_OR_RETURN, kernel_gsp.c:6150-6151).","WHY each call returns fast and never sets the lost-property: _kgspRpcSanityCheck (kernel_gsp.c:314-318) runs API_GPU_ATTACHED_SANITY_CHECK (a PMC_BOOT_0 MMIO read). With A13's os_pci marker set, osIsGpuBusDead()==TRUE (os.c:1884-1896 via os_pci_is_disconnected), so osDevReadReg032 SHORT-CIRCUITS at os.c:2050 returning 0xffffffff — which makes the sanity check fail -> goto done (kernel_gsp.c:2859) on the FIRST loop iteration. This pre-empts the per-call gpuCheckTimeout (line 2830) from ever reaching NV_ERR_TIMEOUT, so the fatal-timeout path (line 2866) and its `cleanupGpuLostStateAtomic(DETECTOR_GSP_HEARTBEAT_TIMEOUT)` at line 2930 — the ONLY in-loop setter of PDB_PROP_GPU_IS_LOST — are NEVER reached.","A13 IS ACTIVELY COUNTERPRODUCTIVE AT THIS POLL SITE, not merely 'insufficient'. The os.c:2050 dead-bus short-circuit returns 0xffffffff BEFORE the post-read detector at os.c:2081-2093, which is `cleanupGpuLostStateAtomic(pGpu, DETECTOR_MMIO_DEAD)` — the natural self-termination that WOULD set PDB_PROP_GPU_IS_LOST (via gpuSetDisconnectedProperties, os.c:1938) and free the worker. By pre-setting os_pci, A13 SUPPRESSES the very detector that makes the vanilla/surprise-removal path self-heal. This is the second miss the prior workflow didn't catch.","THE 5200ms HEARTBEAT IS A RED HERRING — it is NOT a loop terminator. _kgspIsHeartbeatTimedOut (kernel_gsp.c:2279-2321) computes elapsed time from the GPU PTIMER: `tmrGetTime(pGpu, pTmr)` at line 2294, explicitly 'instead of using actual system time'. On the dead bus tmrGetTimeEx_GH100 returns 0 with 'Consistently Bad TimeLo value ffffffff' (timer_gh100.c:152-162), so currentTimeMs=0, heartbeat=0xffffffff, diff=1714079098 > 5200 -> always 'timed out', but this boolean is only LOGGED (done: block). It never breaks the loop nor sets any marker. The worker does NOT self-return at +5200ms.","THE REAL BOUND IS A12's 3000ms FOREGROUND TIMEOUT, NOT the heartbeat. The foreground (nvidia_open) holds nvl->ldata_lock and is parked in wait_for_completion_timeout(3000ms) (nv.c:1927). At +3000ms it takes the LOCKDOWN arm: os_pci_set_disconnected (already set) THEN rm_cleanup_gpu_lost_state(DETECTOR_AER_FATAL) (nv.c:2002-2003) THEN flush_work (nv.c:2037). rm_cleanup_gpu_lost_state (osapi.c) does rmapiLockAcquire(COND_ACQUIRE); the worker RELEASED the API lock before the poll (relaxed GSP init, kernel_gsp.c:4785, holds only the GPU GROUP lock — nv.c:1959-1968), so the API lock is FREE -> COND_ACQUIRE SUCCEEDS -> cleanupGpuLostStateAtomic sets PDB_PROP_GPU_IS_LOST lock-free -> the worker's NEXT pre-loop short-circuit (kernel_gsp.c:2813) returns NV_ERR_RESET_REQUIRED -> bootstrap unwinds -> flush_work joins -> foreground returns -EIO and RELEASES ldata_lock.","ANSWER TO (a)/(b)/(c): the HARD HOST WEDGE (power-cycle required) is (c) BOTH, but with distinct roles: (a) the ldata_lock/F44 topology is the BLAST-RADIUS substrate (foreground parked holding ldata_lock; a 2nd contender would wedge) but it is BOUNDED at 3000ms by A12; (b) the synchronous netconsole printk-storm at console_loglevel=8 is the HARD-WEDGE CONVERTER — it saturates the storming CPU in the netpoll/console path and kills the host (and netconsole) at +19ms (capture dies netcon3:844 [1397.647496], ~1.075s after the roll), ~1.93s BEFORE A12's 3000ms rescue (~uptime 1399.57) could ever fire. The printk amplifier is what prevents the host from living long enough for A12 to recover it.","ANSWER TO 'absent the printk amplifier, recoverable stall or still wedge?': PREDICTED RECOVERABLE — a ~3s GPU-subsystem stall, NOT a hard wedge, recovered at +3000ms by A12's foreground LOCKDOWN arm (rm_cleanup_gpu_lost_state COND_ACQUIRE -> PDB_PROP_GPU_IS_LOST -> worker freed -> ldata_lock released, chip sunk DETECTOR_AER_FATAL, open returns -EIO). NOT recovered at +5200ms and NOT by the worker self-terminating. The worker itself stays unbounded; A12's foreground is the rescuer. RESIDUAL RISK: if rmapiLockAcquire COND_ACQUIRE were contended at +3000ms it would take the DEFERRING branch (sets only the Linux marker) whose documented MMIO_DEAD backstop A13 has SUPPRESSED -> then it would NOT recover even absent printk. Per the lock model the API lock is free, so recovery is the expected outcome, but this is the one branch the re-test must verify.","MINIMISED-OBSERVABILITY RE-TEST THAT PROVES IT: re-run the exact #292 A13 repro (apnex.31, persistence-OFF re-open of the EQ-diverged, fix-bar1-recovered 32G chip) with the printk amplifier REMOVED — `echo 4 4 1 7 > /proc/sys/kernel/printk` (or cmdline loglevel=4) so NVRM LEVEL_ERROR RPC-storm prints never reach the synchronous console; keep async /dev/kmsg + netconsole armed at a level that EXCLUDES the storm. Add OUT-OF-BAND host-liveness independent of the wedged path: external ping/ssh probe at 100ms cadence + the project's fsync'd heartbeat-file timer. Arm a PASSIVE sysrq dump (NMI/serial sysrq or `echo w/t/l > /proc/sysrq-trigger` from the still-alive host) to capture blocked-task + all-CPU backtraces. DISCRIMINATING OUTCOMES: (1) host stays alive and RECOVERS at ~+3000ms (open -EIO, ldata_lock released, GPU sunk) => proves the hard wedge was the printk amplifier (b) and the underlying (a) is a bounded A12-recoverable stall; the 5200ms is a red herring. (2) host still hard-wedges <1s with storm silenced => A12's +3000ms is NOT freeing the worker (rm_cleanup deferred / group-lock inversion / outer retry ignores the property) => genuine unbounded (a), fix must set PDB_PROP_GPU_IS_LOST (A13b) or fail-fast re-open gate (C'). (3) host alive but spins past +3000ms => isolates the recovery gap to rm_cleanup not setting the RM marker, independent of printk. Per project memory, use PASSIVE channels only (loglevel/async-kmsg/external-liveness/sysrq) — bpftrace/kprobe perturb the bug and IBT blocks kprobe on the closed-RM path; kdump/drgn cannot capture the live eGPU wedge."],"table":["poll-site | cond-fn | abort-marker-it-honors | timeout | reachable-from-reopen | covered-by-A13(os_pci)?","GFW boot poll | kgspWaitForGfwBootOk_GH100 (gh100:247) | MMIO scratch read -> osDevReadReg -> osIsGpuBusDead(os_pci OR PDB_LOST) | RM gpuTimeout (bounded) | YES (early, pre-lockdown) | YES — MMIO short-circuit returns 0xffffffff, poll resolves","FSP boot poll | _kfspWaitBootCond_GH100 (kern_fsp_gh100:828, gpuTimeoutCondWait) | mailbox MMIO -> osIsGpuBusDead | bounded gpuTimeoutCondWait | YES | YES (marker -> dead read satisfies/aborts cond)","Lockdown release | _kgspLockdownReleasedOrFmcError (gh100:1050, gpuTimeoutCondWait) | HWCFG2/MAILBOX0 MMIO -> osIsGpuBusDead(os_pci) | bounded gpuTimeoutCondWait | YES — netcon2 SILENT hang was HERE | YES — THIS is A13/A10-v2 target; 0xffffffff -> RISCV_BR_PRIV_LOCKDOWN _UNLOCK true -> frees","SPDM booted | _kgspSpdmBootedOrFmcError (gh100:620, gpuTimeoutCondWait) | mailbox0 MMIO -> osIsGpuBusDead | bounded gpuTimeoutCondWait | YES | YES — 0xffffffff -> mailbox0!=0 -> returns true -> frees","Falcon mbox0 cleared | _kgspFalconMailbox0Cleared (gh100:662, gpuTimeoutCondWait) | MAILBOX0==0 MMIO | bounded gpuTimeoutCondWait | YES | NO self-terminate on marker (waits for 0; dead read 0xffffffff!=0) BUT bounded by gpuTimeoutCondWait's own timeout","GSP RPC INIT_DONE | _kgspRpcRecvPoll (kernel_gsp.c:2682) via kgspWaitForRmInitDone (6151) | pre-loop:2813 bFatalError||PDB_PROP_GPU_IS_LOST; sanity:314 API_GPU_ATTACHED(MMIO)||PDB_LOST | per-call gpuCheckTimeout OSTIMER/CPU-clock ~defaultus*1.5, RE-ARMED every call -> OUTER RETRY UNBOUNDED | YES — netcon3 STORM is HERE | NO — pre-loop ignores os_pci; sanity-MMIO short-circuit only makes each call return fast while outer retry spins. THE GAP. Needs PDB_PROP_GPU_IS_LOST.","heartbeat boolean | _kgspIsHeartbeatTimedOut (2279) | none — reads GPU PTIMER tmrGetTime(2294), not a marker | 5200ms but PTIMER dead(0xffffffff) -> garbage, NOT a loop terminator | YES (logged throughout storm) | N/A — cosmetic; never breaks the loop","A12 funnel (foreground) | nv_bootstrap_bounded wait_for_completion_timeout (nv.c:1927) + LOCKDOWN arm rm_cleanup_gpu_lost_state (2003) | sets PDB_PROP_GPU_IS_LOST via cleanupGpuLostStateAtomic IF API-lock COND_ACQUIRE succeeds (osapi.c) | 3000ms + grace, then flush_work | YES — this is THE actual bound on the worker | INDIRECT — frees the RPC worker by setting PDB_PROP_GPU_IS_LOST at +3000ms (the real recovery path the finding overlooked)"],"surprises":["A13 is COUNTERPRODUCTIVE at the RPC poll, not just insufficient: its bare os_pci_set_disconnected makes osDevReadReg032 short-circuit at os.c:2050 BEFORE the post-read DETECTOR_MMIO_DEAD at os.c:2081-2093, suppressing the natural self-termination that would set PDB_PROP_GPU_IS_LOST. The vanilla surprise-removal path self-heals precisely because that detector fires (netcon2:125 cleanupGpuLostStateAtomic detector_class=1); A13 disabled it for the re-open.","The 5200ms heartbeat is computed from the GPU PTIMER (tmrGetTime, kernel_gsp.c:2294), which is dead (0xffffffff). It is a logged boolean, never a loop bound. The premise 'the worker is BOUNDED by 5200ms' is FALSE — the worker is unbounded; the only bound is A12's 3000ms foreground.","A12 already contains a latent recovery path the finding missed: at +3000ms the LOCKDOWN arm's rm_cleanup_gpu_lost_state COND_ACQUIRE (API lock is free because the worker holds only the GPU GROUP lock across the poll) sets PDB_PROP_GPU_IS_LOST and frees the RPC worker. The host wedged ONLY because the printk storm killed it at +19ms, ~1.93s before that rescue.","A12's LOCKDOWN-arm freeing assumption is poll-specific: setting os_pci alone frees the gpuTimeoutCondWait polls (cond reads 0xffffffff -> condition true) but does NOT free the _kgspRpcRecvPoll OUTER retry, which honors only PDB_PROP_GPU_IS_LOST. The rescue works only because rm_cleanup additionally sets the RM marker — if its COND_ACQUIRE were ever contended, A13's suppression of DETECTOR_MMIO_DEAD removes the documented backstop and recovery fails.","Each _kgspRpcRecvPoll returns in 0.3-3ms (not seconds): the gpuCheckTimeout OSTIMER (CPU-clock, IS_GSP_CLIENT default GPU_TIMEOUT_FLAGS_OSTIMER, gpu_timeout.c:64-77) is REAL but re-armed on every call (gpuSetTimeout line 2794), so the multi-second per-RPC timeout never accumulates -> the fatal-timeout path that would set the lost-property is structurally unreachable in this storm."],"contradicts_prior":"[\"finding-2026-06-06 + prompt hypothesis #3 frame the wedge as 'F44, amplified by printk' with the 5200ms heartbeat as the operative 'worker is BOUNDED' mechanism, and A13 as merely 'necessary but insufficient'. THREE refinements/contradictions: (1) the 5200ms heartbeat is NOT a bound (PTIMER-based, dead, cosmetic) — the real bound is A12's 3000ms foreground. (2) A13 is COUNTERPRODUCTIVE at the RPC poll (suppresses DETECTOR_MMIO_DEAD via os.c:2050 pre-empting os.c:2090), not merely insufficient. (3) absent printk the host most likely RECOVERS at +3000ms via A12+rm_cleanup COND_ACQUIRE, so the HARD wedge is largely printk(observability)-induced — which matches the finding's own option-4 INTUITION ('host might survive') but via a DIFFERENT mechanism (A12 3000ms, not the 5200ms heartbeat). NO contradiction with the established lock model (worker holds GPU GROUP lock not API lock; A12 bounds caller not worker; relaxed-GSP-init releases API lock at kernel_gsp.c:4785) — those are CONFIRMED. NO contradiction with #292='in-flight re-OPEN GSP bring-up' — netcon2 stuck at the lockdown gpuTimeoutCondWait, A13 advanced PAST it to _kgspRpcRecvPoll, a distinct downstream poll site.\"]","code_sites":["/root/open-gpu-kernel-modules/src/nvidia/src/kernel/gpu/gsp/kernel_gsp.c:2813-2822 (RPC pre-loop abort gate: ONLY bFatalError||PDB_PROP_GPU_IS_LOST, ignores os_pci)","/root/open-gpu-kernel-modules/src/nvidia/src/kernel/gpu/gsp/kernel_gsp.c:2824-2933 (for-loop; 2830 gpuCheckTimeout, 2866 NV_ERR_TIMEOUT branch, 2930 cleanupGpuLostStateAtomic — only in-loop lost-setter, unreachable here)","/root/open-gpu-kernel-modules/src/nvidia/src/kernel/gpu/gsp/kernel_gsp.c:2974-2981 (done: heartbeat prints = the storm), 2294 (_kgspIsHeartbeatTimedOut uses PTIMER tmrGetTime), 6150-6151 (kgspWaitForRmInitDone single rpcRecvPoll)","/root/open-gpu-kernel-modules/src/nvidia/src/kernel/gpu/gsp/kernel_gsp.c:314-318 (_kgspRpcSanityCheck: API_GPU_ATTACHED MMIO || PDB_LOST -> NV_ERR_GPU_IS_LOST, the fast-exit each call)","/root/open-gpu-kernel-modules/src/nvidia/src/kernel/gpu/timer/arch/hopper/timer_gh100.c:124-163 (tmrGetTimeEx_GH100 -> 'Consistently Bad TimeLo value ffffffff' + return 0 on dead PTIMER)","/root/open-gpu-kernel-modules/src/nvidia/src/kernel/gpu/gpu_timeout.c:64-77 (IS_GSP_CLIENT default GPU_TIMEOUT_FLAGS_OSTIMER = CPU clock, not PTIMER)","/root/open-gpu-kernel-modules/src/nvidia/arch/nvalloc/unix/src/os.c:1884-1896 (osIsGpuBusDead = os_pci_is_disconnected OR PDB_PROP_GPU_IS_LOST)","/root/open-gpu-kernel-modules/src/nvidia/arch/nvalloc/unix/src/os.c:1921-1971 (cleanupGpuLostStateAtomic: ONLY dual-marker setter, idempotent on PDB_PROP_GPU_IS_LOST)","/root/open-gpu-kernel-modules/src/nvidia/arch/nvalloc/unix/src/os.c:2050-2095 (osDevReadReg032: short-circuit at 2050 PRE-EMPTS DETECTOR_MMIO_DEAD at 2081-2093 — A13's suppression site)","/root/open-gpu-kernel-modules/src/nvidia/arch/nvalloc/unix/src/osapi.c rm_cleanup_gpu_lost_state (COND_ACQUIRE API lock -> cleanupGpuLostStateAtomic; deferring branch on contention)","/root/open-gpu-kernel-modules/kernel-open/nvidia/nv.c:1922-1940,1977-2043 (A12 funnel: ldata_lock held; 3000ms wait; LOCKDOWN arm os_pci+rm_cleanup at 2002-2003; flush_work 2037; bootstrap_in_flight set 1924/clear 1937,2040)","/root/open-gpu-kernel-modules/kernel-open/nvidia/nv.c:1959-1968 (lock model: worker holds GPU GROUP lock not API lock; API lock released at kernel_gsp.c:4785)","/root/open-gpu-kernel-modules/kernel-open/nvidia/nv-pci.c:2960-2981 (A13: bootstrap_in_flight gate -> bare os_pci_set_disconnected, NOT cleanupGpuLostStateAtomic)","/root/nvidia-driver-injector/docs/missions/mission-1-egpu-hot-plug-hot-power/captures/netcon3-2026-06-06-A13-live-FAIL-rpcpoll-wedge.log:686 (roll 1396.572), :687-691 (AER CmpltTO 1397.590), :699-700 (A13 fires 1397.5939), :705-844 (storm 1397.628->1397.647 EOF)"]},{"area":"C' divergence-detection + complete GSP-bootstrap poll/marker coverage audit for #292 (A13 live-FAIL re-derivation), verified against open-gpu-kernel-modules source + apnex.31 capture.","findings":["MARKER MODEL (verified). Two software 'lost' markers exist and they are honored by DISJOINT code regions. (1) Linux marker `os_pci_is_disconnected` (set by `os_pci_set_disconnected`) is consulted ONLY inside `osIsGpuBusDead()` (os.c:1884), which is referenced ONLY by the os.c MMIO readers `osDevReadReg008/016/032` (os.c:1987/2011/2050) and osinit.c:432. grep confirms `osIsGpuBusDead` / `os_pci_is_disconnected` appear NOWHERE in kernel_gsp.c, kernel_gsp_gh100.c, gpu_timeout.c, kern_fsp_*.c. (2) RM marker `PDB_PROP_GPU_IS_LOST` is what the GSP RPC poll honors. A13's early-AER block sets ONLY (1). So A13's marker is structurally invisible to every GSP poll loop except indirectly via the dead-VALUE 0xFFFFFFFF that os.c readers return.","ROOT-CAUSE CONFIRMED AND SHARPENED. `_kgspRpcRecvPoll` (kernel_gsp.c:2682) honors a software marker in exactly two places, NEITHER is os_pci: (a) pre-loop short-circuit kernel_gsp.c:2813 = `pKernelGsp->bFatalError || PDB_PROP_GPU_IS_LOST` -> returns NV_ERR_RESET_REQUIRED; (b) per-iteration `_kgspRpcSanityCheck` (kernel_gsp.c:2851->290) checks API_GPU_ATTACHED(PDB_PROP_GPU_IS_CONNECTED), PDB_PROP_GPU_IS_LOST, and `osIsGpuShutdown` (=`nv->is_shutdown`, os.c:6056) — NONE of which is os_pci. With only os_pci set, IS_CONNECTED stays TRUE, IS_LOST stays clear, is_shutdown stays FALSE -> sanity returns NV_OK -> the loop only exits on the RPC/heartbeat timeout (the 5200 ms storm). The user's hypothesis #3 is correct.","THE 0xFFFFFFFF DEAD-VALUE IS A FRAGILE, POLARITY-DEPENDENT 'coverage'. `gpuTimeoutCondWait`->`timeoutCondWait` (gpu_timeout.c:572) honors NO software lost marker at all; it loops `while(!condFunc) {osSpinLoop; timeoutCheck;}`. os_pci only helps because os.c readers return 0xFFFFFFFF. That terminates conds whose predicate is 'register NON-ZERO => error/done' (`_kgspLockdownReleasedOrFmcError` mailbox0!=0 -> TRUE; `_kfspIsGspTargetMaskReleased` reg!=0 && !=0xBADF41 -> TRUE) but does the OPPOSITE for conds that wait for a SPECIFIC value (`_kgspFalconMailbox0Cleared` waits ==0; 0xFFFFFFFF!=0 -> FALSE FOREVER -> spins to full timeout). So 'covered by A13' is true for the lockdown gate by luck of polarity and false for the RPC poll by construction.","WHY A13 'MOVED' THE WEDGE (verified end-to-end). kflcnRegRead (kernel_falcon_tu102.c:54) and GPU_REG_RD32_UNCHECKED (g_gpu_access_nvoc.h:245) both route through `osDevReadReg032` -> dead-bus guard. So once A13 sets os_pci early, `_kgspLockdownReleasedOrFmcError` reads mailbox0=0xFFFFFFFF (NV_GPU_BUS_DEAD_VALUE_U32, nv-gpu-lost.h:47) -> NV_TRUE in one iteration -> the lockdown wait that SILENTLY stuck on apnex.30 now COMPLETES, advancing the worker into `kgspWaitForRmInitDone`->`rpcRecvPoll(GSP_INIT_DONE)`->`_kgspRpcRecvPoll`, which os_pci cannot reach. Capture netcon3 confirms: 47x 'Bad TimeLo value ffffffff' (tmrGetTimeEx_GH100 reading the dead PTIMER), 46x _kgspRpcRecvPoll, 23x 'GSP RM heartbeat timed out', 46x 'timeout 5200'.","FULL RE-OPEN POLL CHAIN (RmInitAdapter -> kgspInitRm -> _kgspBootGspRm -> kgspBootstrap_GH100). GB202/5090 uses the GH100 KernelGsp HAL (g_kernel_gsp_nvoc.h:1611 has only kgspBootstrap_GH100; no GB20x override) — capture's tmrGetTimeEx_GH100 + _kgspLockdownReleasedOrFmcError confirm. Ordered wait sites: kfspWaitForSecureBoot_GB202 (GFW boot, kgspInitRm:4990) -> kfspWaitForGspTargetMaskReleased_GH100 (kgspBootstrap_GH100:986) -> [SPDM session: skipped, CC off] -> kgspSendInitRpcs (SEND only, 1040) -> gpuTimeoutCondWait(_kgspLockdownReleasedOrFmcError) (1050) -> kgspWaitForRmInitDone -> _kgspRpcRecvPoll(GSP_INIT_DONE) (1107->2682). All MMIO in these conds is dead-bus-guarded, so the worker is BOUNDED (5200 ms heartbeat self-bound); the hard wedge is the synchronous netconsole printk-storm under console_loglevel=8 livelocking the CPU while the foreground holds nvl->ldata_lock (F44).","C' DIVERGENCE-DETECTION SURVEY — brutally honest verdict: the #979 EQ-divergence is NOT reliably detectable in-driver by any passive config-space proxy on the live #292 substrate, because the wedging chip is 'alive-but-diverged' (PMC_BOOT_0=0x1b2000a1, WPR2 up, link Gen3/Gen5 x4 — finding-2026-06-05 §2). Only ONE signal is reliable and it is OUT-OF-BAND: a userspace fix-bar1 sticky sysfs assertion. Per-mechanism below in surprises/table-notes.","DIVERGENCE PROXY DETAIL. (1) PCIe LnkSta width/speed (pcie_capability_read_word PCI_EXP_LNKSTA): config-space, safe, but USELESS — the diverged link is NOT width/speed-degraded (it trains Gen3 x4 fine); divergence is an equalization-completion / Receiver-Error issue (memory project_gen3_signal_integrity). False-negative ~certain. (2) Secondary/16GT PCIe Eq-status extended cap (Phy16Sta.EquComplete): config-space, safe — this IS the documented divergence signature: fix-bar1.sh hazard note says after PCI-layer recovery 'the chip's PCIe equalization status bits (Phy16Sta.EquComplete) are clear vs cold-plug'. Low false-positive on a true cold-plug (which SETS EquComplete). BUT: TB controllers virtualize bridge PCIe registers (memory feedback_lspci_lnkcap_tb_virtual) so confidence is endpoint-cap-only + single hardware sample (n=2); the driver does not currently read it. (3) fix-bar1 sticky 0200 sysfs flag (precedent: tb_egpu_recover_force_trigger_store, nv-tb-egpu-recover.c:735/774): the ROBUST answer — fix-bar1 KNOWS it recovered a diverged chip; have it WRITE a per-nvl sticky 'diverged-recovered' bit the A12 funnel reads. 100% reliable, ZERO false-positive on clean cold-plug (fresh enumeration re-creates nvl -> bit clear). This is the design-of-record's C' input. (4) recovered-diverged bit set on in-driver F40b fire: USELESS for #292 — the wedging re-open shows NO in-driver fire history (cold-plug/recovery re-created nvl; counters in nv_state are fresh). False-negative. (5) last-close-WPR2-cleared bit: NOT divergence-specific — WPR2=0 after a clean shutdown is NORMAL (memory project_wpr2_mechanism: WPR2 set only on FAILED rm_init); fires on every clean close -> false-positive ~always; only useful as ONE conjunct (external AND wpr2-cleared AND diverged-sticky) as the design doc already specifies, never alone.","REDESIGN DIRECTION (proven by the audit). Two complementary, both needed: (A) set the marker the polls ACTUALLY honor, LOCK-FREE, at the earliest dead-bus point: have A13's early-AER block set PDB_PROP_GPU_IS_LOST via `gpuSetDisconnectedProperties` (a plain property write, no lock) in addition to os_pci — so _kgspRpcRecvPoll's pre-loop short-circuit (2813) AND every per-iteration sanity check fire. Do NOT rely on rm_cleanup_gpu_lost_state for this (see surprise: it is COND_ACQUIRE-gated and can silently skip the RM marker). (B) STRUCTURAL fix in the GSP core (base/C-series, L1): teach `_kgspRpcRecvPoll` pre-loop + `_kgspRpcSanityCheck` (and ideally `gpuTimeoutCondWait`) to consult `osIsGpuBusDead` (os_pci OR PDB) — this is the symmetric completion of the os.c reader pattern and ends the per-poll whack-a-mole. Plus (C') funnel fail-fast before queue_work using the fix-bar1 sticky bit. Plus a console-minimised re-test (worker is bounded) to quantify how much of the wedge is the netconsole amplifier."],"table":["poll-site=kfspWaitForSecureBoot_GB202 (GFW boot) kern_fsp_gb202.c:80 | cond-fn=_kfspWaitBootCond_GB202 (THERM_I2CS_SCRATCH FSP_BOOT_COMPLETE==SUCCESS, GPU_REG_RD32 checked) | abort-marker=NONE (cond-true or timeout only; no software lost-marker) | timeout=~5s OSTIMER+BYPASS_THREAD_STATE | reachable=YES (kgspInitRm:4990, pre-bootstrap) | covered-by-A13(os_pci)=INDIRECT/ambiguous (dead-value 0xFFFFFFFF in _SUCCESS sub-field is encoding-dependent; chip alive past GFW in capture so not the wedge site)","poll-site=kfspWaitForGspTargetMaskReleased_GH100 kern_fsp_gh100.c:1158 | cond-fn=_kfspIsGspTargetMaskReleased (HWCFG2 !=0 && (reg&0xFFFFFF00)!=0xBADF4100, GPU_REG_RD32_UNCHECKED->osDevReadReg032) | abort-marker=NONE | timeout=default GPU timeout (NULL) | reachable=YES (kgspBootstrap_GH100:986) | covered-by-A13(os_pci)=YES-indirect (0xFFFFFFFF -> cond TRUE -> terminates in 1 iter via dead-bus guard)","poll-site=gpuTimeoutCondWait(_kgspSpdmBootedOrFmcError) kgsp_gh100.c:620 | cond-fn=_kgspSpdmBootedOrFmcError (MAILBOX0!=0) | abort-marker=NONE | timeout=default GPU timeout | reachable=NO (SPDM/CC disabled on consumer 5090) | covered-by-A13(os_pci)=N/A (if reachable: mailbox!=0 -> TRUE -> terminates)","poll-site=gpuTimeoutCondWait(_kgspFalconMailbox0Cleared) kgsp_gh100.c:662 | cond-fn=_kgspFalconMailbox0Cleared (MAILBOX0==0) | abort-marker=NONE | timeout=default GPU timeout | reachable=NO (SPDM-only) | covered-by-A13(os_pci)=NO if reachable (waits ==0; dead-value 0xFFFFFFFF !=0 -> FALSE forever -> spins to full timeout) — POLARITY TRAP","poll-site=gpuTimeoutCondWait(_kgspLockdownReleasedOrFmcError) kgsp_gh100.c:1050 | cond-fn=_kgspLockdownReleasedOrFmcError (HWCFG2 RISCV_BR_PRIV_LOCKDOWN _UNLOCK || MAILBOX0!=0) | abort-marker=NONE (cond/timeout only) | timeout=default GPU timeout | reachable=YES | covered-by-A13(os_pci)=YES-indirect (0xFFFFFFFF mailbox0!=0 -> TRUE in 1 iter) — THIS is the A12/A10-v2/A13 target AND the ORIGINAL apnex.30 silent-hang site","poll-site=_kgspRpcRecvPoll(GSP_INIT_DONE) via kgspWaitForRmInitDone kernel_gsp.c:2682 (call kgsp_gh100.c:1107) | cond-fn=_kgspRpcDrainEvents + _kgspRpcSanityCheck per iter | abort-marker=bFatalError + PDB_PROP_GPU_IS_LOST (pre-loop 2813); per-iter also IS_CONNECTED + osIsGpuShutdown(nv->is_shutdown) — os_pci NOT in any of them | timeout=RPC defaultus*1.5 + GSP/LibOS heartbeat 5200ms | reachable=YES | covered-by-A13(os_pci)=NO — storms to heartbeat timeout. **THE A13 WEDGE SITE**","poll-site=kgspSendInitRpcs (GSP_SET_SYSTEM_INFO/SET_REGISTRY) kgsp_gh100.c:1040 | cond-fn=n/a (SEND to msg queue + doorbell; GSP-side polls) | abort-marker=n/a (no CPU recv-poll loop) | timeout=n/a | reachable=YES | covered-by-A13(os_pci)=N/A (not a CPU-side wait; doorbell write is dead-bus-guarded on write path)","poll-site=gpuTimeoutCondWait(_kgspWaitForAsserted/_kgspWaitForDeasserted) kgsp_gh100.c:133/144 (kgspResetHw_GH100) | cond-fn=FALCON_ENGINE _RESET_STATUS _ASSERTED/_DEASSERTED | abort-marker=NONE | timeout=defaultResetFSMStateTransitionUs (TMR source) | reachable=CONDITIONAL (only if a reset runs in re-open; A13 took NO bus reset) | covered-by-A13(os_pci)=INDIRECT/ambiguous (reset-status field bits in 0xFFFFFFFF)","poll-site=gpuTimeoutCondWait(_kgspWaitForResetAccess) kernel_gsp_gb100.c:83 (Blackwell reset) | cond-fn=_kgspWaitForResetAccess | abort-marker=NONE | timeout=explicit RMTIMEOUT | reachable=CONDITIONAL (reset path only) | covered-by-A13(os_pci)=INDIRECT/ambiguous","poll-site=GENERIC gpuTimeoutCondWait/timeoutCondWait gpu_timeout.c:572 | cond-fn=caller-supplied | abort-marker=NONE by design (loop = while(!cond){osSpinLoop;timeoutCheck}) | timeout=per-call | reachable=substrate for all cond-wait rows above | covered-by-A13(os_pci)=NO direct awareness — ALL os_pci 'coverage' is incidental via os.c readers returning 0xFFFFFFFF; this is the structural gap"],"surprises":["The A13 finding's parenthetical 'A13 ... never sets PDB_PROP_GPU_IS_LOST (only the full C5 sink sets both)' is mechanistically IMPRECISE. The DISCONNECT branch of nv_pci_error_detected (nv-pci.c:3023) DOES call the full sink rm_cleanup_gpu_lost_state(DETECTOR_AER_FATAL) — and the capture shows 'error_detected -> DISCONNECT' fired. BUT rm_cleanup_gpu_lost_state (osapi.c:1913) gates the cleanupGpuLostStateAtomic (and thus the PDB_PROP_GPU_IS_LOST write) behind `rmapiLockAcquire(API_LOCK_FLAGS_COND_ACQUIRE)` and DEFERS on contention. So the RM marker is set ATTEMPTED-but-conditional, not 'never attempted'. The ~23 repeated _kgspRpcRecvPoll re-entries WITHOUT the pre-loop PDB short-circuit firing imply PDB was in fact NOT set during the storm => the COND_ACQUIRE deferred (API lock contended at that instant). This is the precise gap: the redesign must set PDB LOCK-FREE (gpuSetDisconnectedProperties direct), NOT via the COND_ACQUIRE-gated rm_cleanup path.","The dead-value 'fix' is polarity-dependent and could silently FAIL on a sibling poll: any future/extant cond that waits for a register to become a SPECIFIC value (like _kgspFalconMailbox0Cleared waiting ==0) is made WORSE by os_pci (0xFFFFFFFF never matches -> spins the full timeout). So even option-2 ('teach RPC poll to honor os_pci') does not generalize via dead-value; only a real osIsGpuBusDead check in the loop logic does.","The worker is provably BOUNDED (5200 ms GSP/LibOS heartbeat self-bound -> cleanupGpuLostStateAtomic(DETECTOR_GSP_HEARTBEAT_TIMEOUT) at kernel_gsp.c:2930 eventually sets BOTH markers and exits). Therefore a meaningful fraction of the HARD wedge is plausibly the synchronous netconsole printk-storm at console_loglevel=8 (LEVEL_ERROR 'Bad TimeLo' + 'heartbeat timed out' every ~0.1ms livelocking the CPU while ldata_lock is held), not an infinite hang. The finding's fix-direction #4 (console-minimised re-test) is well-founded and should be run before assuming a deeper deadlock.","tmrGetTimeEx_GH100 (timer_gh100.c:124) is the storm amplifier: with os_pci set, tmrReadTimeLoReg returns 0xFFFFFFFF, `(TimeLo & ~nsecShiftMask)!=0` never breaks, so after retryTimes it logs LEVEL_ERROR 'Consistently Bad TimeLo' + DBG_BREAKPOINT and returns 0 — every heartbeat/timeout check. So the very dead-bus guard that 'saves' the lockdown poll generates the printk storm in the RPC poll. Observability and mitigation are coupled.","Divergence is genuinely invisible to in-driver fire history on the #292 substrate: the wedging re-open is on a fix-bar1-recovered chip whose nv_state/F40b counters are fresh, and PMC_BOOT_0/WPR2/LnkSta all read HEALTHY. The ONLY divergence-specific signal documented anywhere is Phy16Sta.EquComplete (config-space, in fix-bar1.sh's own hazard note) and even that is endpoint-cap-only under TB register virtualization and n=2. The deterministic detector is therefore necessarily OUT-OF-BAND (fix-bar1 sticky sysfs bit), which is exactly why the design-of-record makes C' probabilistic and mandates it ship WITH A', never instead."],"contradicts_prior":"\"One MINOR imprecision flagged, not a substantive contradiction. finding-2026-06-06-A13-live-FAIL §Root-cause point 3 states A13 'never sets PDB_PROP_GPU_IS_LOST (it calls os_pci_set_disconnected directly; only the full C5 sink ... sets both markers)'. Source shows the AER DISCONNECT branch (nv-pci.c:3023) DOES invoke the full sink rm_cleanup_gpu_lost_state, and it fired ('error_detected -> DISCONNECT' in the capture) — so PDB-set was ATTEMPTED. The accurate statement is: the early-free block sets only os_pci (lock-free), and the DISCONNECT branch's PDB write is gated behind rmapiLockAcquire(COND_ACQUIRE) (osapi.c:1913) and was DEFERRED under contention, so PDB ended up effectively unset for the RPC poll. The finding's OUTCOME (RPC poll not covered -> storm -> wedge) is correct; only the 'never attempted' mechanism is sharpened. All other prior claims (per-poll marker coverage gap, os_pci honored only in os.c/osinit.c, RPC poll honors PDB at 2814 not os_pci, no bus reset, bounded worker, divergence invisible live) are CONFIRMED against source. No regression in understanding.\"","code_sites":["/root/open-gpu-kernel-modules/src/nvidia/src/kernel/gpu/gsp/kernel_gsp.c:2813 (pre-loop short-circuit: bFatalError || PDB_PROP_GPU_IS_LOST — NOT os_pci)","/root/open-gpu-kernel-modules/src/nvidia/src/kernel/gpu/gsp/kernel_gsp.c:2851 + 290-336 (_kgspRpcSanityCheck: IS_CONNECTED/IS_LOST/osIsGpuShutdown — NOT os_pci)","/root/open-gpu-kernel-modules/src/nvidia/src/kernel/gpu/gsp/kernel_gsp.c:2682 (_kgspRpcRecvPoll loop), :6133 kgspWaitForRmInitDone->rpcRecvPoll(GSP_INIT_DONE)","/root/open-gpu-kernel-modules/src/nvidia/src/kernel/gpu/gsp/arch/hopper/kernel_gsp_gh100.c:1050 (_kgspLockdownReleasedOrFmcError wait), :1107 (kgspWaitForRmInitDone call), :986 (target-mask wait), :499 + :582 + :588 cond-fns","/root/open-gpu-kernel-modules/src/nvidia/src/kernel/gpu/gpu_timeout.c:572 (timeoutCondWait — honors no software lost marker)","/root/open-gpu-kernel-modules/src/nvidia/arch/nvalloc/unix/src/os.c:1884 (osIsGpuBusDead = os_pci OR PDB), :1921 (cleanupGpuLostStateAtomic dual-marker sink), :2050 (osDevReadReg032 dead-bus guard returns 0xFFFFFFFF)","/root/open-gpu-kernel-modules/src/nvidia/arch/nvalloc/unix/src/os.c:6056 (osIsGpuShutdown = nv->is_shutdown, not os_pci)","/root/open-gpu-kernel-modules/src/nvidia/arch/nvalloc/unix/src/osapi.c:1913 (rm_cleanup_gpu_lost_state COND_ACQUIRE gate — defers PDB set under contention)","/root/open-gpu-kernel-modules/src/nvidia/src/kernel/gpu/timer/arch/hopper/timer_gh100.c:152 (tmrGetTimeEx_GH100 'Bad TimeLo' storm source)","/root/open-gpu-kernel-modules/src/nvidia/src/kernel/gpu/fsp/arch/blackwell/kern_fsp_gb202.c:80 + hopper/kern_fsp_gh100.c:1158 (FSP GFW-boot + target-mask cond-waits)","/root/open-gpu-kernel-modules/src/nvidia/inc/kernel/gpu/nv-gpu-lost.h:47 (NV_GPU_BUS_DEAD_VALUE_U32 = 0xFFFFFFFF)","/root/open-gpu-kernel-modules/kernel-open/nvidia/nv-pci.c:2974 (A13 early os_pci-only block), :3023 (DISCONNECT branch rm_cleanup_gpu_lost_state)","/root/open-gpu-kernel-modules/kernel-open/nvidia/nv.c:1888 (nv_bootstrap_bounded), :1924 bootstrap_in_flight set, :2002-2003 lockdown-arm marker+sink","/root/open-gpu-kernel-modules/kernel-open/nvidia/nv-tb-egpu-recover.c:735 + :774 (tb_egpu_recover_force_trigger_store, 0200 sysfs precedent for a fix-bar1 sticky bit), :306 (WPR2-at-probe — note: this is an MMIO read, not passive)","/root/nvidia-driver-injector/tools/fix-bar1.sh (hazard note: Phy16Sta.EquComplete clear vs cold-plug = the divergence signature)","/root/nvidia-driver-injector/docs/missions/mission-1-egpu-hot-plug-hot-power/captures/netcon3-2026-06-06-A13-live-FAIL-rpcpoll-wedge.log (46x _kgspRpcRecvPoll, 23x GSP RM heartbeat timed out, 47x Bad TimeLo ffffffff)"]},{"area":"NO-REGRESSION AUDIT — reconciling the A13-FAIL (apnex.31) RPC-poll-storm wedge against ALL prior #292 findings, the A10-v2 deployed lockdown arm (nv.c:1990-2004), A12 funnel, the v3 forensics + source-true ldata_lock reconciliation, the superseded 06-02 lock model, and experiment-register R1-R4. Verdict: every load-bearing prior claim VERIFIED against source; the A13-FAIL is a NEW coverage gap (a second, differently-gated GSP poll), NOT a regression of R1-R4 or A10-v2. One prior claim must be SHARPENED and one design option (A13b) must be REJECTED before the redesign.","findings":["USER HYPOTHESIS #1-#4 ALL CONFIRMED in source+capture. (#1) A13 fired: netcon3:699-700 'AER during in-flight bootstrap -> early dead-bus marker (#292)' then 'error_detected -> DISCONNECT (sink-set: GPU already declared lost (C5 sink))' — and crucially CHANGED the first fire from netcon2's NEED_RESET (netcon2:894) to DISCONNECT, no bus reset. (#2) The wedge MOVED: netcon2 (no A13) has ZERO heartbeat/Bad-TimeLo lines (silent lockdown hang); netcon3 (A13) has 93 storm lines. (#3) root cause CONFIRMED. (#4) both wedge at IDENTICAL +1.066-1.067s after open (the chip's MMIO CmpltTO latency is the trigger in both).","PRIOR-CLAIM RECONCILIATION MATRIX (claim | still-holds? | refined-by-A13-FAIL): [v3 finding '#292 = in-flight re-OPEN bring-up deadlock, busy-polls FOREVER holding GPU group lock' | LOCUS HOLDS / 'FOREVER' REFINED | every gpuTimeoutCondWait is OSTIMER-bounded by gpuTimeout (confirmed: GSP-client => defaultFlags=GPU_TIMEOUT_FLAGS_OSTIMER, gpu_timeout.c:64-77), NOT infinite; the wedge is a bounded-per-poll STALL made host-fatal by (a) a printk storm and (b) a 2nd lock contender — memory already softened 'forever'->gpuTimeout]; [v3 'lockdown poll self-terminates via os_pci->osIsGpuBusDead->0xFFFFFFFF (A10-v2 marker)' | HOLDS, capture-confirmed | covers ONLY MMIO-cond polls; _kgspRpcRecvPoll is NOT MMIO-cond-gated]; [design-of-record A13 (A') 'marker frees worker in ONE iteration; foreground takes BUDGET arm before AER becomes F44 contender; verified end-to-end in source' | CONTRADICTED live | the source verification stopped at the lockdown cond and MISSED _kgspRpcRecvPoll's PDB-gate — exactly the 'incomplete per-poll coverage' the mandate names]; [experiment-register R1-R4 (WPR2-fast-fail contained, resets contain-only) | HOLDS, NOT regressed | A13/redesign never touch the fast-fail path]; [A10-v2 lockdown arm nv.c:1990-2004 sets os_pci THEN rm_cleanup (BOTH markers) | HOLDS byte-identical | the redesign only makes its flush_work join faster, strictly better].","POLL-SITE COVERAGE — TWO MARKER FAMILIES, NOT ONE. (a) os_pci_is_disconnected = Linux, LOCK-FREE; honored ONLY transitively via osIsGpuBusDead (os.c:1884-1896) inside the osDevReadReg* MMIO short-circuits (os.c:2050) — so any cond-fn that READS MMIO sees 0xFFFFFFFF and self-satisfies (kflcnRegRead_TU102 routes through GPU_REG_RD32 -> osDevReadReg032, confirmed). (b) PDB_PROP_GPU_IS_LOST = RM property; honored at _kgspRpcRecvPoll pre-loop (kernel_gsp.c:2813-2814) AND _kgspRpcSanityCheck (kernel_gsp.c:314-318). _kgspRpcRecvPoll NEVER calls osIsGpuBusDead, so A13's lock-free os_pci marker is INVISIBLE to it. THIS is the gap.","THE SHARPER ROOT CAUSE (refines the finding, which said 'A13 never sets PDB_PROP_GPU_IS_LOST; only the full C5 sink sets both'): the full C5 sink IS dispatched in A13's run — error_detected's DISCONNECT branch calls rm_cleanup_gpu_lost_state(...AER_FATAL) at nv-pci.c:3023. But that wrapper (osapi.c:1889-1935) uses rmapiLockAcquire(API_LOCK_FLAGS_COND_ACQUIRE) — the NON-BLOCKING acquire that the A10/C1/C6 F44-fix INTRODUCED — and on contention takes the 'API lock contended, deferring' branch WITHOUT calling cleanupGpuLostStateAtomic. So PDB_PROP_GPU_IS_LOST is never set. Proof it never got set: the storm runs via the done: label (kernel_gsp.c:2974), and the pre-loop short-circuit (2820) returns BEFORE done:; if PDB had been set the storm would have stopped at the pre-loop. The mechanism is COND_ACQUIRE-DEFERRAL, not absence of a sink call.","WHY THE COND_ACQUIRE DEFERS — partially CONTRADICTS the v3/nv.c lock-model note. The v3 reconciliation + nv.c:1959-1968 say the worker 'RELEASES the RM API lock at kernel_gsp.c:4785 and holds only the GPU group lock across the poll.' That is true ONLY for the EARLY bootstrap window (the lockdown cond at gh100:1050). kgspInitRm REACQUIRES both API + GPU locks at kernel_gsp.c:4818-4820 (_kgspBootReacquireLocks) BEFORE the GSP_INIT_DONE wait / kgspWaitForGfwBootOk (kernel_gsp.c:4990) and the post-bootstrap control RPCs. So during the _kgspRpcRecvPoll STORM the worker HOLDS the RM API lock again => the AER thread's COND_ACQUIRE necessarily fails => PDB never set. The lock-model note must be extended: 'API lock released across the lockdown cond, REACQUIRED across the RPC-handshake phase.'","=> THE CONTRADICTION THE REDESIGN MUST RESOLVE: finding option-1 'A13b — have error_detected set BOTH markers via the C5 sink' is UNWORKABLE as stated. The C5 sink already runs there and CANNOT set the RM marker without the API lock; making it blocking re-introduces F44 (the exact wedge A10/C1/C6 fixed); keeping it COND_ACQUIRE leaves PDB unset. You cannot reliably set PDB_PROP_GPU_IS_LOST from the AER thread while an in-flight worker owns the API lock. Therefore the lock-free os_pci marker MUST be made sufficient, or RM bring-up must not be entered at all.","ROBUST FIX DIRECTIONS that do NOT regress R1-R4 or A10-v2: (Option 2 / kernel_gsp.c dead-bus short-circuit, L1) make _kgspRpcRecvPoll honor osIsGpuBusDead at the pre-loop gate (2813), at _kgspRpcSanityCheck (314-318), AND at the done: heartbeat re-check (2974-2981, to silence the storm) — then A13's existing lock-free os_pci marker covers this poll too. No regression: osIsGpuBusDead is true ONLY when os_pci OR PDB is set, which a healthy init / WPR2-fast-fail never has (R2 grace arm skips the marker), so healthy and fast-fail paths are untouched; A10-v2's join only gets faster. (Option C' / funnel fail-fast) reject the diverged post-WPR2-cleared re-open at nv_bootstrap_bounded BEFORE queue_work so NO GSP poll is entered — sidesteps the whack-a-mole entirely; caveat: divergence is driver-invisible live (needs the userspace fix-bar1 sticky assertion or a passive PCIe-Eq proxy).","BOUNDEDNESS / observability caveat (supports the finding's 'may be observability-induced' but adds a threshold caveat): each _kgspRpcRecvPoll is OSTIMER-bounded per call (gpuCheckTimeout, ~1.5x defaultus) and rm_init_adapter 'soldiers on' through many RPCs (kernel_gsp.c:2862); the in-loop SELF-sink at kernel_gsp.c:2928-2930 (cleanupGpuLostStateAtomic) would set BOTH markers and stop everything, but ONLY when pRpc->timeoutCount==gspRpcTimeoutGpuResetThreshold (DEFAULT=3) — and the observed fast ~1ms storm exits (via _kgspRpcDrainEvents/sanity, NOT the NV_ERR_TIMEOUT branch) DO NOT increment timeoutCount, so the self-sink is bypassed for a long time. The storm itself is the done: heartbeat re-check reading the dead PTIMER (tmrGetTimeEx_GH100 returns 0 on 'Consistently Bad TimeLo ffffffff'); synchronous netconsole at console_loglevel=8 amplifies it. So 'bounded' is true in principle but the practical bound is up to ~3x OSTIMER gpuTimeout PLUS a soldier-on RPC fan-out — long enough to wedge with the printk amplifier and/or a 2nd ldata_lock contender.","NON-REGRESSION VERDICT: A13 itself does NOT regress R1-R4 or A10-v2 (the bootstrap_in_flight flag + early marker fire only on the is_external bounded funnel and only when an uncorrectable AER lands during the in-flight window; cleared on both worker-return arms at nv.c:1937 and nv.c:2040; WPR2-fast-fail never enters error_detected). The deployed A10-v2 lockdown arm and C1/C6 COND_ACQUIRE are PRESERVED and load-bearing — the redesign MUST keep them (they are the F44 fix; weakening COND_ACQUIRE to blocking would reintroduce the original hard wedge). The A13-FAIL is a genuine NEW coverage gap surfaced by advancing the worker one stage deeper, not a regression."],"table":["poll-site | cond-fn | abort-marker-it-honors | timeout | reachable-from-reopen | covered-by-A13(os_pci)?","kernel_gsp_gh100.c:1050 lockdown wait | _kgspLockdownReleasedOrFmcError (reads HWCFG2 RISCV_BR_PRIV_LOCKDOWN via kflcnRegRead->GPU_REG_RD32->osDevReadReg032) | os_pci (via osIsGpuBusDead->0xFFFFFFFF reads as _UNLOCK) AND PDB (same) | gpuTimeout OSTIMER (gpuTimeoutCondWait) | YES — capture-confirmed reached+freed | YES — A13/A10-v2 PRIMARY target; self-terminates in 1 iteration on the lock-free marker","kernel_gsp.c:2682 _kgspRpcRecvPoll (GSP_INIT_DONE + EVERY post-bootstrap control RPC) | pre-loop gate 2813-2814 + _kgspRpcSanityCheck 314-318 | bFatalError OR PDB_PROP_GPU_IS_LOST ONLY — does NOT call osIsGpuBusDead | per-call gpuCheckTimeout OSTIMER (~1.5x defaultus); whole-init self-sink at 2928-2930 only @ timeoutCount==3 | YES — capture-confirmed STORM (93 lines) | NO — THE GAP: gates on PDB, which A13's lock-free os_pci marker never sets; rm_cleanup COND_ACQUIRE-defers because worker reacquired API lock at 4820","kernel_gsp.c:2974-2981 done: heartbeat re-check (the storm SOURCE) | _kgspHeartbeatIsGspRm/LibosHeartbeatTimedOut (reads dead PTIMER -> tmrGetTimeEx_GH100 ret 0) | NONE — unconditionally prints on every poll exit | n/a (executes once per _kgspRpcRecvPoll return) | YES — capture-confirmed | NO — must be gated on osIsGpuBusDead to silence the printk storm","kernel_gsp_gh100.c:986 kfspWaitForGspTargetMaskReleased | FSP target-mask MMIO poll | MMIO 0xFFFFFFFF self-satisfy + bounded timeout | bounded (FSP poll timeout) | YES (pre-lockdown) | PARTIAL — marker makes the MMIO read fast/satisfy; else times out","kernel_gsp_gh100.c:247 kgspWaitForGfwBootOk_GH100 | GFW_BOOT status MMIO poll | MMIO status + bounded timeout | bounded | YES (post-bootstrap, before INIT_DONE) | PARTIAL — MMIO short-circuit; not a hard PDB gate","kernel_gsp_gh100.c:620 _kgspSpdmBootedOrFmcError | gpuTimeoutCondWait, MMIO mailbox0/FMC | os_pci via MMIO 0xFFFFFFFF + OSTIMER | gpuTimeout OSTIMER | CONDITIONAL — SPDM/CC only (likely DISABLED on consumer 5090) | PARTIAL (if reached)","kernel_gsp_gh100.c:662 _kgspFalconMailbox0Cleared | gpuTimeoutCondWait, MMIO mailbox0 | os_pci via MMIO 0xFFFFFFFF + OSTIMER | gpuTimeout OSTIMER | CONDITIONAL | PARTIAL (if reached)","(NOT reachable from normal re-open — scope exclusions) _kgspHasCcCleanupFinished gh100:1271 (CC), _kgspIsProcessorSuspended tu102:1155 (suspend), _kgspWaitForResetAccess gb100:83 (reset), _kgspIsReloadCompleted tu102:473 (reload), s_dmaPollCondFunc ga102:92 | various | OSTIMER | gpuTimeout | NO (not on cold re-open bootstrap path) | n/a"],"surprises":["SURPRISE 1 (changes the fix space): error_detected ALREADY dispatches the full C5 sink in A13's run (nv-pci.c:3023 rm_cleanup_gpu_lost_state), but it COND_ACQUIRE-DEFERS (osapi.c:1922 'API lock contended, deferring') because the worker REACQUIRED the API lock at kernel_gsp.c:4820 before the RPC phase. So the finding's framing 'only the full C5 sink sets both, and A13 doesn't call it' is imprecise — it IS called but cannot set the RM marker. This means 'A13b: set both markers' is a dead end and the redesign must instead make the lock-free os_pci marker sufficient (teach _kgspRpcRecvPoll osIsGpuBusDead) or fail-fast before bring-up.","SURPRISE 2: gpuTimeoutCondWait is NOT an infinite busy-poll. GSP clients force defaultFlags=GPU_TIMEOUT_FLAGS_OSTIMER (gpu_timeout.c:64-77), so it bounds on CPU monotonic time (osGetMonotonicTimeNs), independent of the dead GPU PTIMER. Every cond-wait self-terminates at gpuTimeout even WITHOUT the dead-bus marker; the marker just makes it 1 iteration instead of ~gpuTimeout. The v3 'busy-polls FOREVER' was loose (memory already corrected to 'bound is gpuTimeout').","SURPRISE 3: the _kgspRpcRecvPoll loop has its OWN dual-marker self-sink at kernel_gsp.c:2928-2930 (cleanupGpuLostStateAtomic on bIsFatalTimeout) — the worker is the API-lock owner so it CAN set PDB. But it only triggers at pRpc->timeoutCount==gspRpcTimeoutGpuResetThreshold (DEFAULT=3, nvrm_registry.h:3198) and ONLY via the NV_ERR_TIMEOUT branch; the observed fast ~1ms storm exits bypass the timeoutCount increment, so the worker's own self-heal is effectively starved during the storm.","SURPRISE 4: the storm cadence (~1ms/iteration, NOT the 5200ms heartbeat nor the ~6s OSTIMER) proves the for-loop is exiting fast each call (via _kgspRpcDrainEvents/sanity), and the heartbeat 'timeout 5200' text comes from the unconditional done: re-check (2974), not from a 5.2s wait. The '5200ms => bounded' inference in the finding is right on the bound's existence but wrong on it being the cadence driver.","SURPRISE 5: A13 made the system WORSE-OBSERVABLE-but-same-fatal: netcon2 (no A13) wedged SILENTLY at the lockdown stage with NEED_RESET; A13 converted the first fire to DISCONNECT (its marker worked) and advanced the worker into a LOUD heartbeat storm. Same +1.066s fatal timing, deeper failure stage — the A13 AER-handler half is correct and should be KEPT; it is necessary-but-insufficient."],"contradicts_prior":"\"ONE prior claim must be corrected (it is the load-bearing constraint for the redesign): the v3 forensics + the nv.c:1959-1968 'source-true' lock-model note state the worker 'releases the RM API lock at kernel_gsp.c:4785 and holds only the GPU group lock across the poll.' Source shows this is true ONLY for the early lockdown-cond window; kgspInitRm REACQUIRES the API lock at kernel_gsp.c:4818-4820 (_kgspBootReacquireLocks) BEFORE the GSP_INIT_DONE / control-RPC phase where the A13-FAIL storm occurs. Consequence: during the _kgspRpcRecvPoll storm the worker HOLDS the API lock, which is precisely why the AER-thread C5 sink (rm_cleanup_gpu_lost_state, COND_ACQUIRE) defers and never sets PDB_PROP_GPU_IS_LOST. This does NOT contradict the deployed A10-v2 arm or R1-R4 (those are about the lockdown window and the WPR2-fast-fail substrate respectively, both still valid and not regressed) — but it DOES refute design option A13b ('set both markers in error_detected') and it sharpens the finding's root cause from 'A13 only sets os_pci by design' to 'the only RM-marker-setting path reachable from the AER thread is COND_ACQUIRE-blocked by the worker's reacquired API lock.' Everything else (A13 fired correctly, wedge moved to the RPC poll, _kgspRpcRecvPoll gates on PDB not os_pci, resets contain-only, fast-fail not regressed) is CONFIRMED with no contradiction.\"","code_sites":["/root/open-gpu-kernel-modules/src/nvidia/src/kernel/gpu/gsp/kernel_gsp.c:2813-2822 (_kgspRpcRecvPoll pre-loop short-circuit: bFatalError||PDB_PROP_GPU_IS_LOST — NOT os_pci; the uncovered gate)","/root/open-gpu-kernel-modules/src/nvidia/src/kernel/gpu/gsp/kernel_gsp.c:314-318 (_kgspRpcSanityCheck: gates on PDB_PROP_GPU_IS_LOST, not osIsGpuBusDead)","/root/open-gpu-kernel-modules/src/nvidia/src/kernel/gpu/gsp/kernel_gsp.c:2928-2930 (in-loop self-sink cleanupGpuLostStateAtomic @ bIsFatalTimeout, timeoutCount==threshold)","/root/open-gpu-kernel-modules/src/nvidia/src/kernel/gpu/gsp/kernel_gsp.c:2974-2981 (done: heartbeat re-check — the storm source, reads dead PTIMER, unconditional)","/root/open-gpu-kernel-modules/src/nvidia/src/kernel/gpu/gsp/kernel_gsp.c:2632-2646 + nvrm_registry.h:3198 (_kgspIsTimeoutFatal; gspRpcTimeoutGpuResetThreshold DEFAULT=3)","/root/open-gpu-kernel-modules/src/nvidia/src/kernel/gpu/gsp/kernel_gsp.c:4782-4798 + 4818-4820 (relaxed-locking API-lock RELEASE at 4785 then REACQUIRE at 4820 before INIT_DONE — the lock-model correction)","/root/open-gpu-kernel-modules/src/nvidia/src/kernel/gpu/gsp/arch/hopper/kernel_gsp_gh100.c:499-542 (_kgspLockdownReleasedOrFmcError: MMIO HWCFG2 cond, self-satisfies on 0xFFFFFFFF) and :935-1060 (kgspBootstrap_GH100 poll sequence)","/root/open-gpu-kernel-modules/src/nvidia/src/kernel/gpu/timer/arch/hopper/timer_gh100.c:124-173 (tmrGetTimeEx_GH100 returns 0 on 'Consistently Bad TimeLo ffffffff')","/root/open-gpu-kernel-modules/src/nvidia/src/kernel/gpu/gpu_timeout.c:64-77 (GSP-client => defaultFlags=GPU_TIMEOUT_FLAGS_OSTIMER) and :571-608 (timeoutCondWait while(!cond) loop)","/root/open-gpu-kernel-modules/src/nvidia/arch/nvalloc/unix/src/os.c:1884-1896 (osIsGpuBusDead: os_pci OR PDB) and :1921-1971 (cleanupGpuLostStateAtomic dual-marker sink) and :2050-2093 (osDevReadReg032 dead-bus short-circuit)","/root/open-gpu-kernel-modules/src/nvidia/src/kernel/gpu/gpu.c:5266-5291 (gpuSetDisconnectedProperties_IMPL: sets PDB_PROP_GPU_IS_LOST SYNCHRONOUSLY at 5272; only the recovery-action refresh is deferred to a workitem)","/root/open-gpu-kernel-modules/src/nvidia/arch/nvalloc/unix/src/osapi.c:1889-1935 (rm_cleanup_gpu_lost_state: API_LOCK_FLAGS_COND_ACQUIRE; DEFERS sink on contention — why PDB never set in A13-FAIL)","/root/open-gpu-kernel-modules/kernel-open/nvidia/nv.c:1922-1939 + 1990-2004 + 2038-2040 (A13 flag set/clear; A10-v2 lockdown arm sets os_pci THEN rm_cleanup = BOTH markers — PRESERVE)","/root/open-gpu-kernel-modules/kernel-open/nvidia/nv-pci.c:2959-3032 (A13 edit-3 early lock-free os_pci marker; DISCONNECT branch dispatches rm_cleanup_gpu_lost_state at 3023)","/root/nvidia-driver-injector/docs/missions/mission-1-egpu-hot-plug-hot-power/captures/netcon3-2026-06-06-A13-live-FAIL-rpcpoll-wedge.log:699-700,705-758,tail (A13 fired->DISCONNECT then 93-line heartbeat storm via done: label)","/root/nvidia-driver-injector/docs/missions/mission-1-egpu-hot-plug-hot-power/captures/netcon2-2026-06-05-292-pathB-wedge.log:881-898 (no A13: NEED_RESET, ZERO storm lines, silent lockdown wedge — proves the wedge MOVED)"]},{"area":"CAPTURE re-decode side-by-side: netcon2 (apnex.30 original #292 silent wedge) vs netcon3 (apnex.31/A13 FAIL rpc-poll storm); GSP-bootstrap poll-site coverage vs A13's os_pci marker; verdict on FACTS claim 4 (\"wedge moved from lockdown-poll to RPC-poll\").","findings":["TIMING netcon2 (apnex.30, original #292 Path-B wedge): re-open scheduled L881 [3472.870770] 'open scheduled to bounded worker (timeout=3000 ms)'; AER CmpltTO L882 [3473.887623] = re-open+1.017s; AER handler L894 [3473.891113] 'error_detected -> NEED_RESET (scheduling bus reset; attempts=1/3)' then L896 [3473.891130] 'error_detected -> DISCONNECT (rate-limited (H2); attempts=1/3)'; L897 'AER: device recovery failed'; LAST LINE L898 [3473.937751] 'nvidia 0000:04:00.0: external GPU detected' -> then total silence (host wedged ~50ms after AER). A10-v2 3000ms budget had NOT expired (would be ~3475.87) -> netcon2 wedged on the AER/bus-reset path, not via the funnel timeout.","TIMING netcon3 (apnex.31, A13 FAIL): re-open scheduled L686 [1396.572395] (timeout=3000ms); AER CmpltTO L687 [1397.590320] = re-open+1.018s (IDENTICAL to netcon2's 1.017s); A13 fires L699 [1397.593882] 'AER during in-flight bootstrap -> early dead-bus marker to free stuck open worker (#292)' = AER+4ms; L700 [1397.593944] 'error_detected -> DISCONNECT (sink-set: GPU already declared lost (C5 sink); attempts=0/3)' -> NO bus reset (attempts=0/3, no NEED_RESET); storm starts L705 [1397.628161] = A13-marker+34ms; capture ends mid-storm L844 [1397.647496] (~19ms of visible storm). Budget 3000ms not expired either (~1399.57).","EXACT STUCK POLL — netcon3: _kgspRpcRecvPoll (kernel_gsp.c:2682), reached via rpcRecvPoll(NV_VGPU_MSG_EVENT_GSP_INIT_DONE) at kernel_gsp.c:6151. Literal storm lines: L705 'tmrGetTimeEx_GH100: ... Consistently Bad TimeLo value ffffffff' (= osDevReadReg032 returning the dead-bus sentinel for PTIMER), L706 '_kgspIsHeartbeatTimedOut: Heartbeat timed out ... heartbeat 4294967295 ... timeout 5200', L707 '_kgspRpcRecvPoll: GSP RM heartbeat timed out' (and 'LibOS' variant). 46 _kgspRpcRecvPoll heartbeat-timeout lines in the ~19ms window = ~23 returns of _kgspRpcRecvPoll (each done-block emits a GSP-RM + LibOS pair, kernel_gsp.c:2974-2981) -> a tight re-call storm, NOT one bounded 5200ms wait.","EXACT STUCK POLL — netcon2: NOT directly evidenced. grep of netcon2 for _kgspRpcRecvPoll|_kgspLockdownReleasedOrFmcError|_kgspIsHeartbeatTimedOut|tmrGetTimeEx|heartbeat = 0 matches. netconsole is passive (no stack). The 'lockdown poll' placement comes ENTIRELY from prior forensics (finding-2026-06-05 lines 136-137), which itself flags it 'inferred from the open-path fingerprint + 2026-06-02 origin trace + R4 -- not from a captured wedged-CPU stack' (that doc, lines 176-178).","WHAT A13 CHANGED (verified in working tree): (a) nv.c:1924/1937/2040 set/clear atomic nvl->bootstrap_in_flight around queue_work in the bounded open funnel; (b) nv-pci.c:2974-2982 in nv_pci_error_detected, gated on bootstrap_in_flight ALONE (no channel-state test), calls os_pci_set_disconnected(lost_nv->handle) DIRECTLY -> NOT cleanupGpuLostStateAtomic, so PDB_PROP_GPU_IS_LOST is never set by A13. Behavioral delta in the logs: netcon2 AER -> NEED_RESET bus reset (attempts=1/3); netcon3 AER -> A13 early os_pci marker -> DISCONNECT sink-set, NO bus reset (attempts=0/3). A13's AER-handler half works exactly as designed (FACTS claims 1 + the no-bus-reset delta CONFIRMED).","ROOT-CAUSE (FACTS claim 3) CONFIRMED + SHARPENED at source: osIsGpuBusDead (os.c:1884) = (os_pci_is_disconnected OR PDB_PROP_GPU_IS_LOST); it is honored ONLY by the osDevReadRegNNN MMIO readers (os.c:1987/2011/2050). kernel_gsp.c + kernel_gsp_gh100.c have ZERO references to os_pci_is_disconnected/osIsGpuBusDead (grep=0). _kgspRpcRecvPoll gates on bFatalError||PDB_PROP_GPU_IS_LOST at the pre-loop short-circuit (kernel_gsp.c:2813-2814) AND at the in-loop _kgspRpcSanityCheck (kernel_gsp.c:314-318, called :2851); its in-loop self-sink cleanupGpuLostStateAtomic (:2930) fires ONLY when bIsFatalTimeout. A13 set only os_pci -> none of these three gates trip -> storm. The storm continuing is itself PROOF PDB_PROP_GPU_IS_LOST was never set (the :314 sanity check would have returned NV_ERR_GPU_IS_LOST otherwise).","WHY A13'S MARKER WORKS FOR LOCKDOWN BUT NOT RPC: _kgspLockdownReleasedOrFmcError (gh100:499, called :1050) terminates because its cond READS MMIO (mailbox0/hwcfg2) -> osDevReadReg032 -> osIsGpuBusDead(os_pci) -> 0xFFFFFFFF -> mailbox0!=0 -> returns NV_TRUE. _kgspRpcRecvPoll does NOT decide loop-exit from a single MMIO-cond sentinel; it decides from PTIMER-based gpuCheckTimeout + the PDB/bFatalError gates. A13's marker DOES make the RPC poll's MMIO reads (PTIMER, heartbeat mailboxes) return 0xFFFFFFFF INSTANTLY (no PCIe completion-timeout wait) -> which is precisely why the storm is FAST and visible rather than slow/silent. So A13's marker is what UNMASKED the storm.","FACTS claim 4 VERDICT — HALF CONFIRMED, HALF CORRECTED. (a) 'SILENTLY (no storm)' for netcon2: LITERALLY TRUE (0 GSP-poll printks; ends at 'external GPU detected' then silence). (b) 'stuck ... at the lockdown poll': NOT literally in netcon2 (no kgsp/stack at all); it is inherited inference, flagged-as-such by the source forensics. (c) The destination half of 'wedge moved ... to RPC-poll' IS literal in netcon3 (kernel_gsp.c:2682 lines quoted). NET: the statement 'the wedge moved from lockdown-poll to RPC-poll' is only HALF literally true in the logs -- the RPC-poll destination is literal; the lockdown-poll ORIGIN is not in the logs and is probably a mis-attribution (see surprise on identical timing).","REACHABILITY/LOCK MODEL preserved (no regression): _kgspRpcRecvPoll asserts rmGpuGroupLockIsOwner(GPU_LOCK_GRP_SUBDEVICE) at kernel_gsp.c:2788 -- so it holds the SAME GPU group lock as the lockdown poll. Whether the worker is stuck at _kgspLockdownReleasedOrFmcError OR _kgspRpcRecvPoll, the wedge mechanism is unchanged: worker spins holding the GPU group lock while the foreground holds nvl->ldata_lock across flush_work (nv.c:2037). The prior lock-model (ldata_lock + unbounded flush, group lock not API lock) holds for the RPC-poll site too."],"table":["poll-site | cond-fn | abort-marker-it-honors | timeout | reachable-from-reopen | covered-by-A13(os_pci)?","_kgspLockdownReleasedOrFmcError (gh100 :499, called kgspBootstrap :1050) | reads FALCON_MAILBOX0/HWCFG2 via kflcnRegRead->osDevReadReg032 | osIsGpuBusDead = os_pci_is_disconnected OR PDB_PROP_GPU_IS_LOST (sentinel 0xFFFFFFFF -> mailbox0!=0 -> cond TRUE) | RM gpuTimeout | YES (early, pre-INIT_DONE) | YES — this is the A13/A10-v2 target; works (both runs' ~1.0s timing implies worker already PAST it)","_kgspWaitForAsserted/_kgspWaitForDeasserted (gh100 kgspResetHw :133/:144) | MMIO PGSP_FALCON_ENGINE reset-status field | osIsGpuBusDead via osDevReadReg (sentinel feeds field) | bounded TMR (resetFSMStateTransitionUs) | YES (reset phase) | YES (MMIO-cond sees os_pci sentinel) + bounded anyway","_kgspSpdmBootedOrFmcError (gh100 :548, called :620) | mailbox0 != 0 | sentinel 0xFFFFFFFF -> !=0 -> TRUE | gpuTimeout | ONLY if SPDM/ConfCompute enabled — NOT on this consumer 5090 | YES-if-reached","_kgspFalconMailbox0Cleared (gh100 :582, called :662) | mailbox0 == 0 | sentinel 0xFFFFFFFF != 0 -> NEVER satisfied; exits only via gpuTimeout | gpuTimeout | ONLY if SPDM/CC — NOT this host | NO (dead-bus value defeats the ==0 cond; latent hazard if CC ever enabled)","_kgspHasCcCleanupFinished (gh100, called :1271) | MMIO mailbox cond | MMIO sentinel via osIsGpuBusDead | gpuTimeout | ONLY if ConfCompute — NOT this host | n/a (not reached)","_kgspRpcRecvPoll INIT_DONE handshake (kernel_gsp.c :2682, called rpcRecvPoll :6151) | _kgspRpcDrainEvents + gpuCheckTimeout(PTIMER) + _kgspRpcSanityCheck | pre-loop :2813-2814 bFatalError||PDB_PROP_GPU_IS_LOST; in-loop sanity :314-318 PDB_PROP_GPU_IS_LOST/API_GPU_ATTACHED; in-loop self-sink :2930 cleanupGpuLostStateAtomic ONLY if bIsFatalTimeout | RPC timeout=defaultus*1.5; heartbeat-staleness 5200ms | YES (after lockdown; THE netcon3 wedge site) | NO ← THE A13 GAP: honors PDB_PROP_GPU_IS_LOST, never os_pci_is_disconnected"],"surprises":["IDENTICAL re-open->AER timing (netcon2 1.017s vs netcon3 1.018s, both ~1.0s) strongly implies the worker reached the SAME bring-up depth before the chip fell off the bus in BOTH runs. The CmpltTO is the CHIP dying mid-init (a property of the diverged chip, not of A13, which changes nothing upstream of the death). Inference: the netcon2 worker had ALSO advanced past lockdown into the INIT_DONE RPC poll. So A13 most likely did NOT 'move' the wedge deeper at all — it changed the AER RESPONSE (suppressed the bus reset; made every MMIO read return 0xFFFFFFFF instantly) which UNMASKED an RPC-poll storm that netcon2's bus-reset path had rendered silent. The 'lockdown -> RPC' relocation narrative is probably an artifact of the (caveated) netcon2 inference, not a real change of poll-site.","The '5200ms heartbeat timeout => worker is BOUNDED' note (FACTS claim 3 tail) is misleading: 5200ms is the heartbeat STALENESS threshold, not a wall-clock bound. The capture shows _kgspRpcRecvPoll RETURNING ~every 0.8ms and being re-called -> a tight re-call storm, effectively UNBOUNDED until the host wedged. Each individual RPC call is bounded; the higher GSP bring-up retry loop that re-enters it is what is unbounded. (The exact re-caller is not pinned from the log.)","Latent hazard found in the coverage sweep: _kgspFalconMailbox0Cleared waits for mailbox0==0, but a dead bus returns 0xFFFFFFFF (!=0), so the dead-bus sentinel does NOT satisfy that cond — it would only exit via gpuTimeout. Harmless today (SPDM/ConfCompute disabled on this consumer 5090, so unreached) but it is a second os_pci-immune poll if CC is ever enabled.","A13b ('set both markers') would plausibly stop the storm via the in-loop _kgspRpcSanityCheck (:314) on the next re-call iteration — but ONLY because the storm RE-ENTERS _kgspRpcRecvPoll; a worker already parked inside a single for-loop iteration would not be rescued, because the pre-loop guard (:2813) is evaluated once. This makes a re-open FAIL-FAST gate (option C', refuse the diverged re-open before RM bring-up) the only solution that avoids per-poll whack-a-mole."],"contradicts_prior":"\"PARTIAL/REFINEMENT, not a hard contradiction. Prior forensics (finding-2026-06-05 lines 136-137) and MEMORY (project_surprise_removal: 'in-flight re-OPEN RM GSP-lockdown bring-up deadlock RmInitAdapter->kgspBootstrap->gpuTimeoutCondWait(_kgspLockdownReleasedOrFmcError)') attribute the netcon2 wedge to the LOCKDOWN poll. The logs do not support that for netcon2 (zero GSP-poll printks; the source doc itself labels the placement 'inferred ... not from a captured stack', lines 176-178), and the identical ~1.0s re-open->AER timing in BOTH captures argues the netcon2 worker had likely also reached the INIT_DONE RPC poll (_kgspRpcRecvPoll), same as netcon3. The refinement does NOT overturn the core prior conclusions (in-flight re-OPEN GSP bring-up deadlock; worker holds the GPU group lock; foreground holds ldata_lock across flush_work; A13 necessary-but-insufficient; need PDB_PROP_GPU_IS_LOST coverage or a fail-fast gate) — _kgspRpcRecvPoll also asserts the GPU group lock (:2788), so the lock model survives. It only corrects WHICH poll is the operative wedge site for the persistence-OFF Path-B substrate: the INIT_DONE RPC poll, not the lockdown poll.\"","code_sites":["/root/open-gpu-kernel-modules/src/nvidia/src/kernel/gpu/gsp/kernel_gsp.c:2682 (_kgspRpcRecvPoll def — netcon3 wedge site)","/root/open-gpu-kernel-modules/src/nvidia/src/kernel/gpu/gsp/kernel_gsp.c:2813 (pre-loop short-circuit: bFatalError||PDB_PROP_GPU_IS_LOST — NOT os_pci)","/root/open-gpu-kernel-modules/src/nvidia/src/kernel/gpu/gsp/kernel_gsp.c:314 (_kgspRpcSanityCheck in-loop PDB_PROP_GPU_IS_LOST gate)","/root/open-gpu-kernel-modules/src/nvidia/src/kernel/gpu/gsp/kernel_gsp.c:2930 (in-loop self-sink cleanupGpuLostStateAtomic, only if bIsFatalTimeout)","/root/open-gpu-kernel-modules/src/nvidia/src/kernel/gpu/gsp/kernel_gsp.c:6151 (rpcRecvPoll INIT_DONE — reachability from re-open)","/root/open-gpu-kernel-modules/src/nvidia/src/kernel/gpu/gsp/kernel_gsp.c:2279 (_kgspIsHeartbeatTimedOut, timeout 5200 path)","/root/open-gpu-kernel-modules/src/nvidia/src/kernel/gpu/gsp/arch/hopper/kernel_gsp_gh100.c:499 + :1050 (_kgspLockdownReleasedOrFmcError, the A13-covered poll)","/root/open-gpu-kernel-modules/src/nvidia/arch/nvalloc/unix/src/os.c:1884 (osIsGpuBusDead = os_pci_is_disconnected OR PDB_PROP_GPU_IS_LOST)","/root/open-gpu-kernel-modules/src/nvidia/arch/nvalloc/unix/src/os.c:2050 (osDevReadReg032 dead-bus short-circuit -> 0xFFFFFFFF)","/root/open-gpu-kernel-modules/src/nvidia/arch/nvalloc/unix/src/os.c:1921 (cleanupGpuLostStateAtomic — sets BOTH markers; A13 does NOT call it)","/root/open-gpu-kernel-modules/kernel-open/nvidia/nv-pci.c:2974-2982 (A13 in-flight early os_pci_set_disconnected, direct, no PDB)","/root/open-gpu-kernel-modules/kernel-open/nvidia/nv.c:1924/1937/2040 (bootstrap_in_flight set/clear) + nv.c:2002 (A10-v2 lockdown-arm os_pci marker)","/root/nvidia-driver-injector/docs/missions/mission-1-egpu-hot-plug-hot-power/captures/netcon2-2026-06-05-292-pathB-wedge.log:881-898 (re-open->AER->NEED_RESET->DISCONNECT->silence)","/root/nvidia-driver-injector/docs/missions/mission-1-egpu-hot-plug-hot-power/captures/netcon3-2026-06-06-A13-live-FAIL-rpcpoll-wedge.log:686-707 (re-open->AER->A13 marker->DISCONNECT sink-set->RPC heartbeat storm)","/root/nvidia-driver-injector/docs/missions/mission-1-egpu-hot-plug-hot-power/finding-2026-06-05-recovery-bringup-wedge-forensics.md:176-178 (prior doc's own 'inferred, not a captured stack' caveat on lockdown placement)"]}]

## VERIFY
[{"claim":"A13 is insufficient because the re-open advances past the lockdown poll into _kgspRpcRecvPoll, which honors PDB_PROP_GPU_IS_LOST (not os_pci_is_disconnected), and A13 never sets PDB_PROP_GPU_IS_LOST.","refuted":false,"confidence":"high","strongest_counter":"Two angles attack the claim but neither lands. (1) Mechanistic imprecision in \"A13 never sets PDB_PROP_GPU_IS_LOST\": the DISCONNECT/default branch of nv_pci_error_detected (nv-pci.c ~3023, the pre-existing C5/v4 sink, NOT A13's diff) DOES dispatch rm_cleanup_gpu_lost_state(NV_GPU_LOST_DETECTOR_AER_FATAL), which routes to cleanupGpuLostStateAtomic and WOULD set PDB. So the run is not one where \"nothing attempts PDB.\" This fails as a refutation: that path is gated behind rmapiLockAcquire(API_LOCK_FLAGS_COND_ACQUIRE) (osapi.c ~1913) and the worker holds the lock during the RPC phase, so it defers; the storm continuing in netcon3 (46x _kgspRpcRecvPoll, 47x Bad TimeLo, 0x lockdown poll) proves PDB stayed clear; and \"A13\" narrowly = the early-free block, which calls os_pci_set_disconnected directly and verifiably never touches PDB. (2) Causal-completeness: each _kgspRpcRecvPoll returns in ~1-3ms (OSTIMER-bounded, not the 5200ms heartbeat) and A12's 3000ms foreground is the real bound, so the HARD power-cycle wedge is co-caused by the synchronous netconsole printk-storm (console_loglevel=8) + ldata_lock F44 substrate, not the marker gap alone — the capture dies ~19ms into the storm, long before +3000ms recovery could fire. This also fails as a refutation: A13 is still empirically insufficient (host wedged), and the marker-coverage gap is the necessary upstream cause of the storm — had _kgspRpcRecvPoll honored os_pci, no storm, no wedge.","what_would_confirm":"Source + both captures already confirm the technical core: _kgspRpcRecvPoll pre-loop gate (kernel_gsp.c ~2813-2814) and _kgspRpcSanityCheck (kernel_gsp.c:290-336) gate only on PDB_PROP_GPU_IS_LOST/API_GPU_ATTACHED/osIsGpuShutdown — never os_pci; osIsGpuBusDead has zero refs in the GSP dir/gpu_timeout.c (grep empty); A13's nv-pci.c block sets only os_pci_set_disconnected; netcon3 shows the storm is purely _kgspRpcRecvPoll past a cleared lockdown poll. Remaining clean-confirmation would come from the recommended minimised-observability re-test (printk loglevel<=4, passive sysrq blocked-task dump) showing the worker spins at _kgspRpcRecvPoll with PDB_PROP_GPU_IS_LOST clear throughout, plus a captured wedged-CPU stack.","residual":"(1) netcon3 contains neither the rm_cleanup success log nor the \"API lock contended, deferring\" LEVEL_WARNING, so the exact reason PDB stayed unset (COND_ACQUIRE deferral vs NULL pGpu vs a dropped line amid the storm) is not pinned from the capture alone — only the outcome (PDB clear) is proven. (2) The claim's \"advances PAST the lockdown poll\" premise about the ORIGINAL apnex.30 wedge is inference: netcon2 has zero GSP-poll printks (passive netconsole, no stack), and the identical ~1.0s re-open->AER timing in both runs suggests netcon2 may also have reached the RPC poll — i.e. A13 may have unmasked, not relocated, the storm. (3) How much of the hard wedge is printk/F44-amplified vs a genuine unbounded hang is not separated without the non-storm re-test.","alternative":"The most precise framing: A13 is insufficient because its only marker, os_pci_is_disconnected, is structurally invisible to _kgspRpcRecvPoll (which honors PDB_PROP_GPU_IS_LOST), and the one path that would set PDB from the AER thread (rm_cleanup_gpu_lost_state via the DISCONNECT branch) is COND_ACQUIRE-deferred by the worker's reacquired API lock — so PDB is never effectively set. The robust fix must either set PDB lock-free at the dead-bus point or teach _kgspRpcRecvPoll/timeoutCondWait to honor osIsGpuBusDead, plus a fail-fast re-open gate."},{"claim":"The hard wedge is the F44 ldata_lock contention; the netconsole printk-storm is an amplifier but not the sole cause (covering the RPC poll would prevent the wedge even with netconsole armed).","refuted":true,"confidence":"medium","strongest_counter":"The single strongest case FOR the claim is source-grounded and survives scrutiny: the storm is NOT at the INIT_DONE poll (which runs API-lock-released inside kgspBootstrap_GH100) but at the POST-INIT_DONE control RPCs. Proof: _kgspIsHeartbeatTimedOut (kernel_gsp.c:2288) returns NV_FALSE unless _kgspHeartbeatIsInitialized, yet the capture shows it firing 46x — so the worker drained INIT_DONE (6151), initialized the heartbeat (6162), exited kgspWaitForRmInitDone, and _kgspBootGspRm REACQUIRED the RM API lock at kernel_gsp.c:4820 before issuing SET_GUEST_SYSTEM_INFO @5105 / GET_GSP_STATIC_INFO @5112 — the actual storm site. With the worker holding the reacquired API lock, A12's foreground lockdown-arm rm_cleanup_gpu_lost_state COND_ACQUIRE (osapi.c:1913) would DEFER, PDB_PROP_GPU_IS_LOST is never set, the RPC poll's pre-loop short-circuit (kernel_gsp.c:2813) never fires, flush_work (nv.c:2037) blocks on the never-returning worker, and the foreground holds ldata_lock unboundedly — a genuine F44 wedge that would occur even with the printk storm silenced. Under this (correctly-located) storm site, finding-3's 'absent-printk recovers at +3000ms' prediction is likely wrong and the claim's 'printk is just an amplifier' framing is plausibly correct. This is exactly why the verdict is medium, not high.","what_would_confirm":"The discriminating minimized-observability re-test (recommended by the finding doc, not yet run): re-run the apnex.31 A13 repro with console_loglevel minimised (echo 4 4 1 7 > /proc/sys/kernel/printk, keep async kmsg + external 100ms ping/ssh liveness + passive sysrq-w/t/l). Outcomes: (1) host survives the storm and RECOVERS at ~+3000/+5000ms (open returns -EIO, ldata_lock released, GPU sunk) => claim REFUTED, printk was the hard-wedge converter; (2) host still hard-wedges <3s with the storm silenced => claim CONFIRMED, the F44 ldata_lock hold is the real (unbounded) wedge and printk was merely an amplifier; (3) host spins past +3000ms but stays alive => isolates the gap to A12's COND_ACQUIRE deferring under the reacquired API lock. The capture cannot decide this because the host died at +1.075s, ~1.9-3.9s before any A12 bound could be tested.","residual":"The eventual outcome absent the storm is genuinely unresolved (n=1 capture, died before the bound). The grounding findings internally CONTRADICT each other on the load-bearing API-lock question (finding-3: worker holds only the group lock, COND_ACQUIRE succeeds, host recovers; finding-1/finding-4: worker reacquired the API lock for the post-INIT_DONE RPCs, COND_ACQUIRE defers, no recovery). My source read (heartbeat-initialized => storm is post-INIT_DONE => API lock reacquired @4820) favors the no-recovery side, which softens the refutation. A countervailing residual: the worker's OWN in-loop self-sink cleanupGpuLostStateAtomic(DETECTOR_GSP_HEARTBEAT_TIMEOUT) at bIsFatalTimeout (kernel_gsp.c:~2929, timeoutCount==gspRpcTimeoutGpuResetThreshold=3) is lock-free and could still bound the worker even with the API lock held — its reachability under the fast storm exits is contested. Finally, the claim's parenthetical ('covering the RPC poll would prevent the wedge even with netconsole armed') is TRUE and uncontested — covering the poll eliminates the storm at its source — but it is necessary-condition logic consistent with BOTH the claim and its negation, so it does not establish the headline attribution.","alternative":"Better-supported reading: the OBSERVED hard wedge in netcon3 is the synchronous netconsole printk-storm saturating the CPU at +1.075s, while the F44 ldata_lock hold is the bounded blast-radius SUBSTRATE (foreground parked in wait_for_completion_timeout, lock held ~1.075s of a 3000ms+2000ms budget), NOT the proximate killer. Both netcon2 (died +1.067s via the bus-reset path, no storm) and netcon3 (died +1.075s via the storm) perished well within A12's bounded-lock window from NON-lock mechanisms — so neither capture shows an expired/deadlocked ldata_lock as the cause of death. Whether an unbounded lock-hold would wedge the host absent the storm is a real, source-plausible possibility (post-INIT_DONE RPC storm with the API lock reacquired) but is UNVERIFIED; the finding doc itself flags 'the host might survive' and frames the printk-amplifier role as a hypothesis to test, not an established fact. The root lever everyone agrees on is covering the RPC poll (set PDB_PROP_GPU_IS_LOST lock-free or teach _kgspRpcRecvPoll osIsGpuBusDead), which removes both the storm and the lock-hold regardless of which is decisive."},{"claim":"The robust fix is the re-open FAIL-FAST GATE (C') because per-poll marker coverage is whack-a-mole; marker-based approaches (A13/A13b/kernel_gsp) are at best in-flight backstops.","refuted":true,"confidence":"medium","strongest_counter":"The claim is not baseless and has a real kernel of truth. (1) finding-2026-06-06-A13-live-FAIL-rpcpoll-wedge.md:51-61 explicitly calls C' \"the robust answer\" and recommends \"C' as primary, with A13b/option-2 as the in-flight backstop\" — the claim is a near-verbatim restatement of that doc. (2) There is a genuine architectural argument: C' enters ZERO GSP polls, so it structurally CANNOT have a per-poll coverage gap, whereas any marker approach must enumerate and correctly cover poll sites. (3) NAIVE per-cond-fn marker coverage genuinely IS fragile/whack-a-mole — the source-true \"polarity trap\" proves it: _kgspFalconMailbox0Cleared (kernel_gsp_gh100.c) waits for MAILBOX0==0, but the dead-bus value 0xFFFFFFFF != 0 NEVER satisfies it, so a dead-value-based marker makes that poll WORSE, and A13's os_pci marker also suppresses the DETECTOR_MMIO_DEAD self-heal funnel (os.c:2050 short-circuits before os.c:2081). So for the dead-value form, \"whack-a-mole\" is accurate, and the structural marker fix that ends it is an L1 NVIDIA-core patch the project's sovereign-modules policy disfavors.","what_would_confirm":"Two things would flip this to confirmed: (1) A reliable PASSIVE in-driver divergence detector (e.g. Phy16Sta.EquComplete validated as low-false-negative across many hardware samples, not n=2 under TB register virtualization) so C' fires deterministically WITHOUT the out-of-band userspace fix-bar1 sticky bit — that would make C' self-contained and genuinely \"the robust fix.\" (2) A minimised-observability re-test (printk rate-limited / console_loglevel lowered, per finding-3's methodology) showing the host STILL hard-wedges even with a correctly-placed lock-free dual-marker OR an osIsGpuBusDead check inside timeoutCondWait + _kgspRpcRecvPoll — i.e. proof the worker is genuinely unbounded and markers cannot free it, leaving zero-poll fail-fast as the only survivor.","residual":"The dispute is really about ORDERING and the word \"robust,\" not about whether C' has value — no source argues C' should ship instead of markers. The design-of-record (design-2026-06-05:18,28-31,150) is authoritative and explicit: the MARKER (A') is \"the primary structural fix\" and C' is \"phase-2 defense-in-depth ... Probabilistic ... must ship with (A'), never instead\" because divergence is invisible to the driver live. The claim inverts this (markers demoted to \"backstops,\" C' elevated to \"the robust fix\"). Source verification confirms the structural marker fix is coverage-complete at exactly TWO chokepoints (timeoutCondWait honors no marker and is the shared primitive for every gpuTimeoutCondWait site; osIsGpuBusDead referenced only in os.c/osinit.c/nv.c/nv-pci.c), so \"whack-a-mole\" is false for the chokepoint form even though it is true for naive per-cond-fn patching. The claim also wrongly lumps the kernel_gsp structural fix with A13/A13b as \"at best backstops,\" whereas multiple audits state it \"ends the per-poll whack-a-mole.\" Confidence is medium (not high) because a real intra-project document (finding-2026-06-06) does recommend C'-primary, and the claim's whack-a-mole premise is partially correct; but its strong form (\"C' is THE robust fix; markers at best backstops\") is cleanly contradicted by the design-of-record and the boundedness/divergence-invisibility evidence. Best-supported synthesis: ship the correctly-placed lock-free/structural marker fix as the load-bearing robust layer AND C' as a probabilistic defense-in-depth gate — neither alone, with the marker primary."}]

## DESIGNS
[{"candidate":"C' RE-OPEN FAIL-FAST GATE (proposed addon A14, fork build apnex.32). A pre-bringup gate at the top of nv_bootstrap_bounded (nv.c) that refuses a known-#979-EQ-diverged external re-open with -EIO BEFORE queue_work, so the bootstrap worker (RmInitAdapter->kgspInitRm->kgspBootstrap_GH100) is never created and NO GSP poll of any kind is entered.\n\nPREDICATE (3 conjuncts, all lock-free atomics on nvl):\n  nv->is_external_gpu\n  && atomic_read(&nvl->diverged_recovered)     // sticky; OUT-OF-BAND, asserted by fix-bar1 (the oracle that performed the EQ-diverged hot-add recovery)\n  && atomic_read(&nvl->reopen_gsp_torndown)    // armed at the diverged chip's WPR2-clearing LAST-CLOSE (GSP actually torn down -> next open is a full cold re-bringup)\n\nDETECTABLE SIGNAL (per ground:divergence-detect, brutally honest): divergence is NOT reliably detectable in-driver live on the #292 substrate (PMC_BOOT_0=0x1b2000a1, WPR2 up pre-close, LnkSta=0x1041 Gen3 x4 — all read HEALTHY; the only config-space proxy, Phy16Sta.EquComplete, is endpoint-cap-only under TB register virtualization, n=2). So the RELIABLE detector is necessarily out-of-band: fix-bar1.sh KNOWS it recovered a diverged chip (it just wrote ReBAR CTRL + slot-cycled a chip that hot-added at 256MB = definitionally a #979-diverged hot-add), and asserts it via a new write-only sysfs bit. This is 100% reliable for the substrate that actually wedges (R1-R4 + both captures: only fix-bar1-recovered chips wedge on persistence-OFF re-open). The in-band ARMING signal (WPR2=0 at LAST-CLOSE) is read PASSIVELY by the existing A4 close diag while the chip is still alive (capture-confirmed netcon2 L878 / netcon3 L682 WPR2=0x00000000 wpr2_up:no) — no new MMIO poke at open time.\n\nWHERE THE STICKY BITS LIVE: nv_linux_state_t (nv-linux.h:~1462, beside bootstrap_in_flight) — atomic_t diverged_recovered, atomic_t reopen_gsp_torndown. Per-device, lock-free, kzalloc-zero-initialised at nv_pci_probe, destroyed with nvl on remove/re-enumeration. On nvl (NOT ->recover) so they work with NVreg_TbEgpuRecoverEnable=0, mirroring bootstrap_in_flight's documented placement rationale.\n\nUSERSPACE CONTRACT: fix-bar1.sh --bind, after a successful recover+modprobe+persistence-engage, writes `echo 1 > /sys/.../tb_egpu_diverged_recovered` (best-effort, warn-on-fail). The injector container's production bind path does the same. An open returning -EIO on a device with the bit set MEANS \"cold recovery required\": re-run fix-bar1 --bind (slot power-cycle re-enumerates -> fresh nvl -> both bits clear -> clean bringup). Optional read-only companion tb_egpu_reopen_blocked (0444) lets orchestration distinguish a C' refusal from other -EIO.","closes_all_polls_how":"C' is coverage-complete BY CONSTRUCTION, not by per-poll marker patching: every GSP-bootstrap poll/wait in the ground table is reachable ONLY from inside the bootstrap worker (w->fn = __nv_start_device_locked -> RmInitAdapter -> kgspInitRm -> _kgspBootGspRm -> kgspBootstrap_GH100 / kgspWaitForRmInitDone). C' returns -EIO at the TOP of nv_bootstrap_bounded, before atomic_set(bootstrap_in_flight,1) and before queue_work(system_long_wq,&w->work). No worker is ever created, so NONE of the poll sites is entered. This is precisely the whack-a-mole exit A13 lacked: A13 patched the lockdown poll's marker and the wedge MOVED to the RPC poll; C' patches NEITHER poll — it prevents the worker that would reach either.\n\nEVERY GROUND-TABLE POLL SITE, all closed by the same mechanism (unreachable, no bringup scheduled): (1) kfspWaitForSecureBoot_GH100/GB202 GFW-boot; (2) kfspPollForResponse/kfspWaitForResponse FSP-EMEM; (3) kfspWaitForGspTargetMaskReleased_GH100; (4) kgspResetHw_GH100 asserted/deasserted; (5) _kgspEstablishSpdmSession (SPDM, unreachable anyway-CC off); (6) _kgspFalconMailbox0Cleared (the ==0 polarity-trap poll os_pci can NEVER satisfy); (7) GspMsgQueueSendCommand tx-full (sysmem, os_pci-immune); (8) gpuTimeoutCondWait(_kgspLockdownReleasedOrFmcError) [netcon2 silent wedge]; (9) GspStatusQueueInit link wait; (10) kgspWaitForRmInitDone->_kgspRpcRecvPoll(INIT_DONE); (11) POST-INIT_DONE control RPCs->_kgspRpcRecvPoll [netcon3 storm wedge]; (12) kgspCheckGspRmCcCleanup; (13) the generic timeoutCondWait/gpuTimeoutCondWait substrate. All 13 sit on the worker thread downstream of kgspBootstrap; C' refuses before the worker exists -> all 13 unreachable for the detected diverged-re-open substrate. Note: for a NON-diverged clean cold-plug C' does NOT fire and the worker IS queued and these sites ARE entered — on a healthy chip they complete normally (that is normal cold-init, which must not regress). C' does not 'close' the polls for healthy chips; it makes them UNREACHABLE only for the diverged substrate that wedges. That is the intended scope.\n\n=== netcon2 LINE-BY-LINE (apnex.30 original SILENT lockdown wedge) ===\nL860 [3373.43] capture armed, chip BAR1=32G diverged, persistence about to be disabled -> diverged_recovered=1 (fix-bar1 --bind asserted it on this recovered chip).\nL861 [3472.155758] open scheduled; L862 open completed rc=0 -> C' does NOT fire here: reopen_gsp_torndown is still 0 (no WPR2-clearing LAST-CLOSE yet; prior closes were persistence-ON rm_disable_adapter, GSP never torn down). Correct: C' must allow this pre-close open.\nL870 [3472.169027] close-entry WPR2=0x07f4a000 wpr2_up:YES; L873 rm_disable_adapter; L875 rm_shutdown_adapter (persistence now OFF -> full GSP teardown).\nL877-878 [3472.834766] post-shutdown LAST-CLOSE WPR2=0x00000000 wpr2_up:no -> C' ARMS: is_last_close && diverged_recovered && WPR2 cleared -> atomic_set(reopen_gsp_torndown,1). (This site is reached ONLY on the persistence-OFF nv_shutdown_adapter branch, nv.c:2668 — itself corroborating GSP was torn down.)\nL881 [3472.870770] open scheduled to bounded worker — THE RE-OPEN. WITH C': at nv_bootstrap_bounded entry, before queue_work, gate = is_external(yes) && diverged_recovered(1) && reopen_gsp_torndown(1) = TRUE -> return -EIO at ~3472.870, worker NEVER queued.\nL882 [3473.887623] AER CmpltTO (+1.017s) — WITHOUT C' this is the diverged chip falling off the bus mid-bringup. WITH C' there is no bringup: the worker never issued a single MMIO transaction to the GSP, so no CmpltTO, no AER. L894 NEED_RESET, L896 DISCONNECT, L898 'external GPU detected' then silence: ALL PREVENTED. The silent lockdown-poll hang (_kgspLockdownReleasedOrFmcError, inferred site) is never entered. Host stays alive; nvidia-smi gets -EIO.\n\n=== netcon3 LINE-BY-LINE (apnex.31 A13 RPC-poll STORM wedge) ===\nL682 [1356.792467] post-shutdown LAST-CLOSE WPR2=0x00000000 wpr2_up:no -> C' ARMS reopen_gsp_torndown=1 (diverged_recovered already 1).\nL685 [1396.565204] THE ROLL (nvidia-smi -L re-open).\nL686 [1396.572395] open scheduled to bounded worker — THE RE-OPEN. WITH C': gate fires BEFORE atomic_set(bootstrap_in_flight,1) and BEFORE queue_work -> return -EIO at ~1396.572; worker never queued; bootstrap_in_flight stays 0.\nL687 [1397.590320] AER CmpltTO (+1.018s) — WITHOUT C' the chip fell off the bus during the A13-advanced bringup. WITH C': no bringup -> no MMIO to the chip -> no CmpltTO -> no AER.\nL699-703 A13 'AER during in-flight bootstrap -> early dead-bus marker' + DISCONNECT sink-set — PREVENTED: there is no AER and bootstrap_in_flight==0, so the A13 branch is inert (correctly).\nL705-844 [1397.628-647] the STORM: tmrGetTimeEx_GH100 'Bad TimeLo ffffffff' x47, _kgspIsHeartbeatTimedOut 'timeout 5200' x46, _kgspRpcRecvPoll heartbeat-timeout x46, host wedge — PREVENTED: _kgspRpcRecvPoll is never called because kgspWaitForRmInitDone is never reached because the worker that runs kgspBootstrap_GH100 was never queued. Zero GSP printks. The foreground open returned -EIO at L686's instant and released nvl->ldata_lock immediately (no flush_work, no stuck worker to join). Host stays alive.\n\nBoth wedge SEQUENCES are prevented at the SAME structural point (the gate before queue_work), independent of WHERE the diverged chip would have fallen off the bus (lockdown poll in netcon2 vs RPC poll in netcon3) — which is exactly the property A13 lacked.","diff_sketch":["nv-linux.h (nv_linux_state_t, ~L1462 beside bootstrap_in_flight): add 'atomic_t diverged_recovered;' + 'atomic_t reopen_gsp_torndown;' with #292/C' comment. Zero-init is automatic (nvl is kzalloc'd in nv_pci_probe).","nv.c nv_bootstrap_bounded (TOP of function, BEFORE the timeout_ms==0 / !is_external feature gates, before kzalloc/atomic_set(bootstrap_in_flight,1)/queue_work): insert the C' gate -> if (nv->is_external_gpu && atomic_read(&nvl->diverged_recovered) && atomic_read(&nvl->reopen_gsp_torndown)) { nv_printf(NV_DBG_ERRORS, \"NVRM: tb_egpu [C']: refusing re-open of diverged-recovered eGPU (WPR2-torndown) BEFORE GSP bringup -> -EIO (#292); cold-recover via fix-bar1\\n\"); return -EIO; }  (nvl already computed at function top via NV_GET_NVL_FROM_NV_STATE). Placed above the feature gate so it fires even with the A12 funnel disabled.","nv-tb-egpu-close.c tb_egpu_close_diag (last-close block, ~L155): after the snapshot, when (is_last_close && atomic_read(&nvl->diverged_recovered)) do one passive WPR2 read via tb_egpu_pcie_read_wpr2(pci_resource_start(nvl->pci_dev,0), &wpr2); if cleared ((wpr2 & TB_EGPU_PCIE_WPR2_VAL_MASK)==0) atomic_set(&nvl->reopen_gsp_torndown,1) + one printk 'C': armed re-open gate'. (Reaching this post-shutdown site is itself the persistence-OFF GSP-teardown signal; WPR2==0 is the explicit design-of-record conjunct. No change to the EXPORT_SYMBOL_GPL pdev-variant signature -> ABI-safe for nvidia-uvm.ko.)","nv-tb-egpu-recover.c sysfs (~L735/L774): add write-only DEVICE_ATTR(tb_egpu_diverged_recovered, 0200, NULL, store) whose store does kstrtoul + atomic_set(&nvl->diverged_recovered, val?1:0) (mirrors tb_egpu_recover_force_trigger_store). Optional read-only DEVICE_ATTR(tb_egpu_reopen_blocked, 0444, show) returning (diverged_recovered && reopen_gsp_torndown). CRITICAL: register these in an ALWAYS-ON attr group added in nv_pci_probe (NOT the Enable-gated tb_egpu_recover_attr_group at L786), because the bits live on nvl and must work with NVreg_TbEgpuRecoverEnable=0.","tools/fix-bar1.sh (--bind block, after persistence engage ~L485, snapshot 'E-after-bind-persist-on'): add 'echo 1 > /sys/bus/pci/devices/$GPU/tb_egpu_diverged_recovered 2>/dev/null || warn ...'. Document the -EIO=cold-recover contract in the header 'Known hazards' section.","patches/addon/A14-292-reopen-failfast-gate.patch: carve edits 1-4 (source-only addon, rides next build per A-series=addon/project-local). fix-bar1.sh edit lands in the injector repo directly. Update docs/patches.md + docs/upstream-plan.md (A-series stays project-local, not upstream-bound). Add patch-intent + review files per the schema discipline."],"regression":"NO regression vs R1-R4 / A10-v2 / A13 / normal cold-init, all source-verified. (1) Normal cold-init: clean cold-plug never runs fix-bar1 -> diverged_recovered=0 (kzalloc) -> gate bypassed -> byte-identical to today. Structural false-positive guard. (2) A10-v2 lockdown arm (nv.c:1990-2004) PRESERVED byte-identical; on a C'-refused open no worker is queued so the timeout arm simply has nothing to time out; on every non-refused open it runs exactly as before. (3) A13 (bootstrap_in_flight + early os_pci marker) PRESERVED; C' sits BEFORE atomic_set(bootstrap_in_flight,1) so a refused open leaves the flag 0 and nv_pci_error_detected's A13 branch stays correct; non-refused opens unchanged. (4) R1-R4: WPR2-fast-fail substrate has diverged_recovered=0 unless fix-bar1 ran, and reopen_gsp_torndown needs a WPR2-clearing LAST-CLOSE that the fast-fail/transient path doesn't produce -> never refused; reset paths untouched (C' is pre-bringup, never enters slot_reset/resume). (5) Persistence-ON PRODUCTION: closes take rm_disable_adapter (GSP stays loaded, WPR2 NOT cleared, post-shutdown diag site not reached) -> reopen_gsp_torndown never arms -> re-opens allowed -> C' is fully inert in normal production. C' fires ONLY in the persistence-OFF re-open of a fix-bar1-recovered diverged chip = exactly #292's substrate, and nothing else. The two new atomics are additive (don't touch any existing field); the gate is additive (early-return on a new predicate). C5/C6 COND_ACQUIRE preserved (not weakened to blocking, so no F44 re-introduction).","locks_uaf":"HONEST and strictly SAFER than the status quo. GATE PATH: runs on the foreground open syscall thread holding nvl->ldata_lock (same as today's nv_bootstrap_bounded entry); does exactly 3 atomic_reads + early return -EIO; acquires NO new lock; queues NO worker -> NO flush_work -> the ENTIRE A12 sp-UAF surface (the join-guard that exists because the worker dereferences the caller's sp) is NOT exercised on the refused path. There is no detached worker, so nv_pci_remove has nothing to race. -EIO propagation is the already-proven A12 path (lockdown arm returns -EIO from this same function at nv.c:2042). ARMING WRITE (close path): atomic_set on the foreground close thread (holds ldata_lock via the close path); monotonic 0->1; atomic_t single-word -> no torn write. SYSFS WRITE (diverged_recovered): atomic_set from the sysfs store, no lock; serialized vs the gate by the device open/close lifecycle (fix-bar1 writes it after bind, strictly before any re-open) -> no read/write race. UAF: none introduced — both bits live in nvl whose lifetime brackets all opens/closes and the sysfs attr group (added in probe, removed before nvl free); no reference escapes nvl; no sp aliasing. The honest caveat: the always-on attr group must be torn down in nv_pci_remove BEFORE nvl is freed (standard sysfs_remove_group ordering) — a routine lifecycle detail, not a new hazard class. Net: C' removes worker/sp/flush_work hazards on the refused path entirely; it adds only two single-word atomics and one sysfs node.","observability":"DECISIVE advantage: C' is INDEPENDENT of the printk-storm factor AND of the contested boundedness question. By refusing before any GSP poll it produces ZERO tmrGetTimeEx 'Bad TimeLo' lines and ZERO _kgspIsHeartbeatTimedOut/_kgspRpcRecvPoll storm lines, so the synchronous netconsole/console_loglevel=8 amplifier (which VERIFIED claim 2 flags, medium-confidence, as the proximate hard-wedge converter that killed netcon3 at +1.075s) has nothing to amplify. It does NOT depend on A12's 3000ms foreground rescue (no worker to rescue), does NOT depend on osIsGpuBusDead poll coverage, does NOT depend on PDB_PROP_GPU_IS_LOST being set lock-free, and does NOT depend on whether the worker is bounded by the 5200ms heartbeat (a PTIMER red herring) or by A12's 3000ms. It therefore sidesteps the entire unresolved 'is the wedge printk-induced or a genuine F44 ldata_lock deadlock' debate — in BOTH readings, no bringup => no storm AND no stuck-worker lock-hold. The only observability footprint is benign: one synchronous '[C']: refusing re-open' printk per refused open (a single line, not a storm; safe even at loglevel 8). This makes C' robust to the observability-perturbs-the-bug constraint that has defeated active instrumentation throughout this mission.","validation":"Per reliability methodology (one variable, written hypothesis, n>=3, cheapest first). (1) BUILD apnex.32 with A14 + fix-bar1 edit; COMPILE-validate with a real `make modules` against the live kernel tree (not just git apply --check, per the compile-validation lesson). (2) PRIMARY repro = exact apnex.31 Stage5 sequence: fix-bar1 --bind a 32G-diverged chip (asserts diverged_recovered), engage then DISABLE persistence (WPR2-clearing LAST-CLOSE arms reopen_gsp_torndown), then THE ROLL (nvidia-smi -L). EXPECT: open returns -EIO within ms (no ~1s bringup, no AER CmpltTO, no storm); host stays alive; netconsole shows the single '[C']: refusing re-open' line then nothing. Verify n>=3. (3) OBSERVABILITY-MINIMISED confirm (the test the finding doc mandates): keep printk loglevel low (echo 4 4 1 7 > /proc/sys/kernel/printk) + external 100ms ping/ssh liveness + passive sysrq-w armed; confirm host NEVER loses liveness on the refused re-open (the storm path that killed netcon3 is never entered). (4) NO-REGRESSION negative controls, n>=3 each: (a) clean cold-plug at boot -> nvidia-smi -L works (gate inert, diverged_recovered=0); (b) fix-bar1 --bind FIRST open works (reopen_gsp_torndown not yet armed); (c) persistence-ON open/close/re-open works (WPR2 not cleared -> not armed); (d) WPR2-fast-fail substrate (R2) still contained, not refused. (5) USERSPACE-CONTRACT test: after -EIO, run fix-bar1 --bind (slot-cycle) -> fresh nvl -> bits clear -> re-bringup succeeds. (6) Confirm sysfs node present + writable with NVreg_TbEgpuRecoverEnable=0 (always-on attr group). Soak >=14 days before cutover.","closes_all_polls_how_note":"addressed in closes_all_polls_how","sovereignty":"L5/L6 project-local addon (A14), keeps blast radius concentrated and OUT of the NVIDIA fork core. The gate + two atomics + sysfs node touch ONLY injector-owned eGPU surfaces: nv.c nv_bootstrap_bounded (already an A12-owned funnel), nv-linux.h (one struct, beside the existing A13 bootstrap_in_flight), nv-tb-egpu-close.c (A4-owned), nv-tb-egpu-recover.c sysfs (A3-owned pattern), and tools/fix-bar1.sh (injector repo). ZERO edits to kernel_gsp.c / gpu_timeout.c / os.c (no L1 NVIDIA-core patch) — unlike the marker-coverage option-2 (teach _kgspRpcRecvPoll/timeoutCondWait osIsGpuBusDead) which is an L1 upstream-core change. A-series = addon/project-local, never upstream-bound. This honors the sovereign-modules default (L4-L6) and the concentrate-blast-radius principle.","open":["FALSE-NEGATIVE residual (the load-bearing caveat): C' detects divergence ONLY via the fix-bar1 out-of-band assertion. A diverged chip that reaches a persistence-OFF re-open WITHOUT fix-bar1 having asserted the bit (e.g. a future divergence that does NOT present the 256MB-BAR1 symptom, or a recovery done by other means) is NOT gated -> the worker is queued -> the wedge can recur. The design-of-record is explicit that divergence is driver-invisible live, so this cannot be closed structurally by C' alone. THEREFORE C' MUST SHIP WITH the lock-free marker fix (A': set PDB_PROP_GPU_IS_LOST lock-free via gpuSetDisconnectedProperties from the AER thread, NOT via the COND_ACQUIRE-deferred rm_cleanup) as phase-2 defense-in-depth, per design-2026-06-05 ('marker primary, C' probabilistic, never instead'). C' is the primary candidate for PREVENTING BOTH CAPTURED WEDGES (provable, high confidence); it is NOT a sufficient sole production fix for the un-asserted-divergence case.","CONSERVATISM residual: C' refuses re-opens that MIGHT have succeeded if the chip's divergence had healed. This is fail-SAFE (-EIO + cold-recover, never a wedge), and matches R1-R4 + both captures (the diverged-recovered chip wedged 100% of observed re-opens), but it is a behavioral change: a recovered chip now requires a cold re-plug per persistence-OFF cycle instead of a soft re-open. Acceptable given production runs persistence-ON (C' inert there).","ARMING-precision residual: I arm reopen_gsp_torndown on (is_last_close && diverged_recovered && WPR2==0). Reaching nv.c:2668 (post-shutdown) is itself the persistence-OFF GSP-teardown signal, so the WPR2 read is corroborating rather than strictly necessary; kept because the design-of-record specifies the WPR2-cleared conjunct explicitly. Confirm in the live test that the WPR2 read at the diverged chip's LAST-CLOSE is reliably 0 (capture-confirmed n=2: netcon2 L878, netcon3 L682).","PRIOR-FINDING reconciliation note (no contradiction): the captures' identical ~1.0s re-open->AER timing suggests netcon2 may have reached the RPC poll too (A13 may have UNMASKED rather than relocated the storm). C' is immune to this ambiguity — it prevents the worker regardless of which poll site it would reach — so the open question of the exact netcon2 wedge poll-site does not affect C''s correctness.","Minor: ensure the always-on attr group is registered in nv_pci_probe and removed in nv_pci_remove with correct ordering (before nvl free), and that fix-bar1's write survives the bind sequence (write AFTER modprobe+persistence, before the first close)."],"confidence":"high"},{"candidate":"A13b — \"early lock-free DUAL-marker\" (extend A13's in-flight-AER block to set BOTH os_pci_is_disconnected AND PDB_PROP_GPU_IS_LOST, the latter via a NEW lock-free RM entry rm_set_gpu_lost_inflight() that resolves pGpu via NV_GET_NV_PRIV_PGPU and calls cleanupGpuLostStateAtomic() DIRECTLY, bypassing rm_cleanup_gpu_lost_state's COND_ACQUIRE API-lock gate). os_pci covers every MMIO-cond gpuTimeoutCondWait poll (lockdown/FSP/target-mask); PDB covers every _kgspRpcRecvPoll (INIT_DONE + all post-INIT_DONE control RPCs). Optional deferred defense-in-depth (separate L1 base patch, NOT load-bearing here): teach _kgspRpcRecvPoll pre-loop + _kgspRpcSanityCheck + timeoutCondWait to consult osIsGpuBusDead so the os_pci marker alone suffices everywhere.","closes_all_polls_how":"PROVEN AGAINST BOTH CAPTURES + EVERY POLL FAMILY.\n\n== NETCON3 (apnex.31 / A13 FAIL, RPC storm) line-by-line ==\nL686[1396.572395] open scheduled (3000ms); bootstrap_in_flight=1; worker runs RmInitAdapter->kgspInitRm->_kgspBootGspRm: API lock RELEASED (kernel_gsp.c:4785), kgspBootstrap_GH100 runs lockdown poll (gh100:1050) then kgspWaitForRmInitDone (gh100:1107). For ~1s the bus is ALIVE: GSP boots, INIT_DONE drains, heartbeat initialized (6162-6164), _kgspBootGspRm returns, API lock REACQUIRED (asserted 5075), worker enters post-INIT_DONE control RPCs (SET_GUEST_SYSTEM_INFO @5105).\nL687[1397.590320]=+1.018s AER CmpltTO: diverged chip falls off bus mid-control-RPC.\nL691[1397.592528] error_detected (channel state=1).\nL699[1397.593882] A13 early block (os_pci only). >>> MY FIX ADDS HERE: rm_set_gpu_lost_inflight() -> cleanupGpuLostStateAtomic -> gpuSetDisconnectedProperties sets PDB_PROP_GPU_IS_LOST=TRUE + PDB_PROP_GPU_IS_CONNECTED=FALSE, LOCK-FREE (setProperty bit-writes gpu.c:5272-5273; only deferred osQueueWorkItem).\nL700[1397.593944] DISCONNECT sink-set. [Under A13 this was rm_cleanup_gpu_lost_state, which COND_ACQUIRE-DEFERRED because the worker holds the REACQUIRED API lock -> PDB stayed CLEAR -> storm.]\nL705[1397.628161]=+34ms STORM STARTS: _kgspRpcRecvPoll for the next control RPC; pre-loop (2813) PDB-false -> for-loop -> done: heartbeat prints (2974-2980)+tmrGetTimeEx 'Bad TimeLo'. >>> MY FIX: PDB was planted at L699, ~34ms BEFORE the worker reaches this poll, so the pre-loop short-circuit (2813-2814 PDB true) returns NV_ERR_RESET_REQUIRED at 2821 DIRECTLY (NOT via done:) -> for-loop NEVER runs -> ZERO done: heartbeat prints, ZERO tmrGetTimeEx -> STORM NEVER STARTS. Caller (5106) sees status!=NV_OK -> goto done -> kgspInitRm fails -> RmInitAdapter aborts -> worker returns -> releases group lock -> A12 flush_work (nv.c:2037) joins -> foreground returns -EIO -> releases ldata_lock.\nL705-844[1397.628->1397.647] 23x _kgspRpcRecvPoll returns / 46 heartbeat prints / 47 Bad TimeLo: ALL ELIMINATED.\nHost wedged ~+1.075s (EOF L844): with the fix the worker is freed within ~1 RPC (~1ms) of the AER -> no wedge.\n\n== NETCON2 (apnex.30 / original #292, SILENT) line-by-line ==\nL881[3472.870770] open scheduled (3000ms); apnex.30 has A12 but NO A13 (no bootstrap_in_flight, no early marker).\nL882[3473.887623]=+1.017s AER CmpltTO (IDENTICAL trigger/timing to netcon3 +1.018s -> worker reached the SAME post-INIT_DONE depth).\nL886[3473.889776] error_detected (channel state=1).\nL894[3473.891113] NEED_RESET (bus reset; attempts=1/3) -- because NO early os_pci marker, A3's sink-query gate did NOT surrender -> bus-reset path on a TB-tunneled diverged chip.\nL896 DISCONNECT (rate-limited). L897 AER recovery failed. L898[3473.937751] 'external GPU detected' -> TOTAL SILENCE; host wedged ~+1.067s, ~50ms post-AER, FAR before A12's 3000ms budget (~3475.87). Zero GSP-poll printks (silent: no early os_pci -> MMIO reads waited full PCIe completion timeouts, slow not storming, while the worker held the group lock and the foreground held ldata_lock).\n>>> MY FIX prevents BOTH netcon2 mechanisms: (a) the early os_pci marker (kept from A13) makes A3's sink-query gate SURRENDER to DISCONNECT instead of NEED_RESET -> NO bus reset (exactly as netcon3 showed, attempts=0/3) -> removes the bus-reset wedge vector; (b) the dual marker frees whatever poll the worker is at -- if the inferred lockdown gpuTimeoutCondWait, os_pci -> cond-fn MMIO read 0xFFFFFFFF -> mailbox0!=0 -> cond TRUE in 1 iter -> kgspBootstrap returns NV_ERR_NOT_READY; if the more-likely post-INIT_DONE _kgspRpcRecvPoll, PDB -> pre-loop short-circuit. Silent group-lock hang broken either way -> flush_work joins -> foreground -EIO within ~1ms.\n\n== EVERY POLL FAMILY IN THE GROUND TABLE ==\n1) MMIO-cond gpuTimeoutCondWait family -- lockdown _kgspLockdownReleasedOrFmcError (gh100:1050), kfspWaitForSecureBoot GFW-boot, kfspWaitForGspTargetMaskReleased (gh100:986), kgspResetHw asserted/deasserted: timeoutCondWait honors NO marker (verified gpu_timeout.c: loop only checks API_GPU_IN_RESET + timeout), so coverage is via the cond-fn's MMIO read returning 0xFFFFFFFF under os_pci. RELIABLE for these conds (polarity 'reg != expected => done/error' is satisfied by all-1s). COVERED by os_pci.\n2) RPC-poll family -- _kgspRpcRecvPoll for INIT_DONE (gh100:1107->6151->2682) AND all post-INIT_DONE control RPCs (5105/5112, the captured storm): pre-loop 2813-2814 (PDB_PROP_GPU_IS_LOST) + per-iteration _kgspRpcSanityCheck 314-315 (PDB_PROP_GPU_IS_LOST + API_GPU_ATTACHED=PDB_PROP_GPU_IS_CONNECTED). COVERED by PDB.\n3) heartbeat done: prints (2974-2980) -- the storm SYMPTOM, not a poll: eliminated because PDB short-circuits BEFORE the for-loop (pre-loop returns directly at 2821, never reaches done:).\n4) SYSMEM-cond waits -- GspMsgQueueSendCommand tx-full (message_queue_cpu.c:544), GspStatusQueueInit link (352): immune to os_pci (sysmem reads) and not PDB-gated, BUT BOUNDED by their own 1s/4s OSTIMER + API_GPU_IN_RESET. Residual = bounded LATENCY, not a wedge; not the captured site.\n5) SPDM/CC polarity-trap (_kgspFalconMailbox0Cleared waits ==0; 0xFFFFFFFF != 0 never satisfies): NOT reachable on consumer 5090 (SPDM/CC disabled; absent from both captures). Routed to the deferred L1 defense-in-depth.\n6) Reset-path waits: NOT entered -- early os_pci -> A3 DISCONNECT surrender suppresses the bus reset.\nNet: every poll reachable from a re-open that can WEDGE on this substrate is covered by exactly one of the two markers; the uncovered ones are bounded (sysmem) or unreachable (SPDM).","diff_sketch":["osapi.c: NEW lock-free RM entry. void NV_API_CALL rm_set_gpu_lost_inflight(nvidia_stack_t *sp, nv_state_t *pNv, NvU32 detector_class){ THREAD_STATE_NODE ts; void *fp; OBJGPU *pGpu; if(!pNv) return; NV_ENTER_RM_RUNTIME(sp,fp); threadStateInit(&ts,THREAD_STATE_FLAGS_NONE); pGpu=NV_GET_NV_PRIV_PGPU(pNv); if(pGpu) cleanupGpuLostStateAtomic(pGpu,(nv_gpu_lost_detector_t)detector_class); threadStateFree(&ts,THREAD_STATE_FLAGS_NONE); NV_EXIT_RM_RUNTIME(sp,fp);} -- identical to rm_cleanup_gpu_lost_state MINUS the rmapiLockAcquire(API_LOCK_FLAGS_COND_ACQUIRE) gate. cleanupGpuLostStateAtomic is already called lock-free by osinit.c:442/os.c:2090/kernel_gsp.c:2930, so this is consistent with existing direct callers.","nv-linux.h / rm exports: declare NV_STATUS-less void NV_API_CALL rm_set_gpu_lost_inflight(nvidia_stack_t *, nv_state_t *, NvU32); reuse NV_GPU_LOST_DETECTOR_AER_FATAL.","nv-pci.c (A13 early block ~2974, INSIDE the existing `if (nvl && atomic_read(&nvl->bootstrap_in_flight))`): keep `os_pci_set_disconnected(lost_nv->handle)` FIRST (earliest coverage for MMIO-cond polls), THEN add: `nvidia_stack_t *aer_sp=NULL; if(nv_kmem_cache_alloc_stack(&aer_sp)==0){ rm_set_gpu_lost_inflight(aer_sp, lost_nv, NV_GPU_LOST_DETECTOR_AER_FATAL); nv_kmem_cache_free_stack(aer_sp);}` Update log to 'early DUAL-marker (os_pci + PDB) to free stuck open worker (#292)'.","nv-pci.c DISCONNECT branch (default case): LEAVE the existing rm_cleanup_gpu_lost_state(AER_FATAL) dispatch unchanged -- it is now a harmless idempotent no-op for the in-flight case (cleanupGpuLostStateAtomic early-returns on PDB already set) and still load-bearing for the NON-in-flight DISCONNECT path. Do NOT weaken its COND_ACQUIRE (that is the C5/C6 F44 fix).","nv.c / A12 funnel + A10-v2 lockdown arm (nv.c:1990-2004, 2037): UNCHANGED. Worker now frees faster so flush_work joins sooner -- strictly better.","(DEFERRED, separate L1 base/C-series patch, defense-in-depth ONLY -- not load-bearing for this substrate, do NOT ship to fix #292): add `|| osIsGpuBusDead(pGpu)` to _kgspRpcRecvPoll pre-loop (2813) + _kgspRpcSanityCheck (314) and an osIsGpuBusDead short-circuit inside timeoutCondWait's loop (gpu_timeout.c:~591). This makes the lock-free os_pci marker sufficient at EVERY poll (closing the SPDM polarity-trap + future-poll whack-a-mole). Sovereignty cost: NVIDIA-core. Keep separate."],"regression":"NO regression vs R1-R4 / A10-v2 / C5-C6 / normal cold-init, verified against source. R1-R4 (WPR2-fast-fail contained, resets contain-only): the WPR2-up fast-fail returns NV_ERR_INVALID_STATE at kernel_gsp.c:4763-4768 WITHOUT an uncorrectable AER, so error_detected is never entered, bootstrap_in_flight is cleared on worker return, and no marker is set -- fast-fail path byte-untouched. A10-v2 lockdown arm (nv.c:1990-2004) and the A12 funnel (1888-2043) are unchanged; the only behavioral delta is the worker returns sooner, making the existing flush_work join FASTER (strictly better). C5/C6 F44 fix is PRESERVED: rm_cleanup_gpu_lost_state keeps its COND_ACQUIRE (NOT weakened to blocking -- that would reintroduce the original hard wedge); the new rm_set_gpu_lost_inflight is a SEPARATE entry used ONLY in the in-flight-AER window where the chip is known dead. Normal cold-init: gated on bootstrap_in_flight AND an uncorrectable AER (error_detected is only invoked for uncorrectables); a healthy cold-init has neither -> marker never fires. The dual marker is idempotent (cleanupGpuLostStateAtomic early-returns on PDB already set) so it cannot double-sink or race the A10-v2 +3000ms arm. The deferred L1 osIsGpuBusDead short-circuit (if ever shipped) is true ONLY when os_pci OR PDB is set, which healthy/fast-fail paths never have.","locks_uaf":"VIABLE -- the AER thread sets PDB_PROP_GPU_IS_LOST WITHOUT blocking on the GPU group lock the worker holds. PROOF (ground:markers-locks, source-verified): the PDB set routes cleanupGpuLostStateAtomic (os.c:1921, lock-free, no rmapiLockAcquire) -> gpuSetDisconnectedProperties_IMPL (gpu.c:5266). gpuSetDisconnectedProperties sets PDB_PROP_GPU_IS_LOST=TRUE (5272) and PDB_PROP_GPU_IS_CONNECTED=FALSE (5273) via pure in-memory setProperty bit-writes -- NO group-lock, NO API-lock acquisition. Its ONLY lock-touching side effect is a DEFERRED osQueueWorkItem(_gpuSetDisconnectedPropertiesWorker, bLockGpuGroupDevice=TRUE) at 5285; osQueueWorkItem -> os_queue_work_item (os-interface.c:1175) is NV_KMALLOC_ATOMIC + nv_kthread_q_schedule_q_item + return -- enqueue-only, explicitly atomic-context-safe, NEVER blocks. The deferred worker that DOES want the group lock runs async and queues BEHIND the bootstrap worker, executing only after the worker releases the group lock. gpu.c:5280-5283 documents the function as safe \"at raised (device) IRQL\". So NO self-deadlock on the group lock. CRITICAL DISTINCTION: the contention that killed A13's sink dispatch was the API LOCK (not the group lock) -- the worker REACQUIRED the API lock post-INIT_DONE (asserted kernel_gsp.c:5075), so rm_cleanup_gpu_lost_state's COND_ACQUIRE deferred; bypassing that wrapper avoids the API-lock contention entirely while never touching the group lock inline. UAF: pGpu via NV_GET_NV_PRIV_PGPU(nv) is a lockless deref (nv-priv.h:354) that osAttachGpu links at osinit.c:311 (early, before bootstrap) and unlinks only at detach (1674); during the bootstrap_in_flight window the worker is running -> pGpu non-NULL and alive, no free in flight (same deref rm_cleanup already does; add a NULL guard). Concurrency/torn-write: single effective writer (AER thread) of the LOST/CONNECTED bits during the storm -- the worker only READS PDB in the poll loop, not writes; the reader race is benign (monotonic false->true, OR-checked at 2814/315, worst case one extra loop iter). cleanupGpuLostStateAtomic's header explicitly designs for \"concurrent detector firing, including re-entrant AER callbacks\" and is idempotent-gated on getProperty(LOST), so this is exactly the concurrency the primitive was built for (the MMIO_DEAD detector at os.c:2090 already calls it from arbitrary MMIO-read context). Residual torn-write risk only if the worker RMW's a PDB prop co-located in the LOST/CONNECTED word between the AER write and poll re-entry -- same risk class as the existing MMIO/heartbeat detectors, not new.","observability":"Does NOT depend on the printk-storm factor and is strictly better than masking it. The fix ELIMINATES the storm at its source: with PDB planted at AER time (netcon3 L699, ~34ms before the worker reaches the poll), _kgspRpcRecvPoll's pre-loop short-circuit (2813-2814) returns NV_ERR_RESET_REQUIRED DIRECTLY at 2821 -- it never enters the for-loop and never reaches the done: label, so the unconditional heartbeat prints (2974-2980) and tmrGetTimeEx 'Bad TimeLo' (timer_gh100.c) never execute. No console_loglevel change required; the synchronous-netconsole amplifier becomes moot because there is nothing to print. It also does NOT rely on A12's +3000ms rescue (which is unreliable here: A10-v2's lockdown arm calls rm_cleanup_gpu_lost_state whose COND_ACQUIRE DEFERS because the worker holds the reacquired API lock) -- the worker is freed deterministically within ~1 RPC (~1ms). Interaction: this RESOLVES the finding's open question about how much of the wedge was printk-induced -- it is the only field where my source read (heartbeat-initialized => post-INIT_DONE => API lock reacquired => COND_ACQUIRE defers => A12 would NOT have recovered the worker absent printk either) implies the hard wedge was NOT merely observability-induced, so eliminating the storm at its marker source is necessary, not cosmetic. The console-minimised re-test is recommended as a VALIDATION instrument (clean blocked-task/sysrq capture if anything still spins), not a dependency.","validation":"Live n>=3 on apnex.32 (the gating test, per project policy n>=3 to resolve): persistence-OFF Path-B re-open of the #979-EQ-diverged, fix-bar1-recovered 32G chip (same repro as netcon2/netcon3). PASS = host SURVIVES the re-open: open returns -EIO within ~+budget, ldata_lock released, GPU sunk via DETECTOR_AER_FATAL, NO heartbeat/Bad-TimeLo storm in the capture, GPU re-engages on a subsequent cold-plug. Instrument PASSIVELY (memory: observability perturbs this bug; IBT blocks kprobe on closed-RM; kdump/drgn cannot capture the live wedge): netconsole armed + console_loglevel lowered to 4 (so the storm, if any, does not self-amplify) + async /dev/kmsg + external 100ms ping/ssh liveness probe + fsync'd heartbeat-file + armed sysrq-w/t/l for a blocked-task/all-CPU backtrace if it still wedges. Expected log signature with the fix: 'early DUAL-marker (os_pci + PDB)' then 'GPU N lost via detector_class=3' (the cleanupGpuLostStateAtomic latch firing -- the line that was ABSENT in netcon3 because the sink deferred) then 'open completed/-EIO' and ldata_lock release, with ZERO _kgspRpcRecvPoll lines. Build gate: real `make modules` against the running kernel (apply-check is insufficient per project memory), confirm rm_set_gpu_lost_inflight links (new NV_API_CALL export). Pre-live source gates already cleared: pre-loop honors PDB (kernel_gsp.c:2813), sanity honors PDB/CONNECTED (314), gpuSetDisconnectedProperties lock-free (5272-5290), os_queue_work_item non-blocking (os-interface.c:1175), pGpu lockless+alive (nv-priv.h:354 / osinit.c:311). fake-5090 #290 path is deferred per user (test live).","sovereignty":"Primary candidate stays in the ADDON blast radius (A-series): the new rm_set_gpu_lost_inflight lives in osapi.c (unix layer, already the project-owned C5/A-sink plumbing site) + a 6-line addition to the existing A13 nv-pci.c block + a header export. NO new NVIDIA-core GSP/timeout change is required -- cleanupGpuLostStateAtomic and gpuSetDisconnectedProperties already exist and are invoked as-is; the new entry merely calls the existing lock-free sink from a context that omits the (mistaken) API-lock wrapper. Blast radius is concentrated in 2 files already touched by the C5/A13 cohort. The deferred defense-in-depth osIsGpuBusDead short-circuit (in _kgspRpcRecvPoll/_kgspRpcSanityCheck/timeoutCondWait) WOULD be a base/C-series L1 NVIDIA-core patch -- kept SEPARATE and deferred so the load-bearing #292 fix does not require an L1 change. C' fail-fast re-open gate (reject the diverged post-WPR2-cleared re-open before queue_work via the fix-bar1 sticky sysfs bit) remains phase-2 defense-in-depth that ships WITH the marker fix, never instead -- it is the only zero-poll exit but depends on out-of-band divergence detection (driver-invisible live).","open":["RmInitAdapter abort-promptness: I argue NV_ERR_RESET_REQUIRED from the pre-loop is treated as FATAL (NV_RM_RPC_* caller at 5106 does goto done on status!=NV_OK; the v4 guard G8 comment 2799-2806 designed it to fast-fail), NOT soldier-on like NV_ERR_TIMEOUT (2862). High-confidence from source but the exact unwind across the full post-INIT_DONE control-RPC sequence is best confirmed live (worker returns within ~1 RPC).","Sysmem-cond waits (GspMsgQueueSendCommand tx-full @message_queue_cpu.c:544; GspStatusQueueInit @352) are marker-immune but bounded (1s/4s OSTIMER). Not the captured site and not a wedge, but if a future re-open stalls there the dual marker will not short-circuit it -- only the deferred L1 fix or a hard timeout would. Flag for the bounded-latency residual.","SPDM/CC polarity-trap (_kgspFalconMailbox0Cleared waits ==0) is unreachable on this consumer 5090 (CC off, absent from both captures) -- if CC is ever enabled it becomes a real os_pci-immune poll; covered only by the deferred L1 osIsGpuBusDead fix.","netcon2's exact proximate killer (silent group-lock worker hang vs the NEED_RESET bus reset) is not directly evidenced (zero GSP-poll printks); my fix demonstrably removes BOTH vectors, but the minimised-observability live re-test should confirm which dominated.","Recommend pairing with C' (fix-bar1 sticky-bit fail-fast gate) as phase-2 defense-in-depth so a divergent re-open ideally never enters ANY GSP poll -- ships with, not instead of, A13b."],"confidence":"high"},{"candidate":"A13-keep + A14 \"poll-honors-the-marker\" L1 GSP-core patch. KEEP A13 unchanged as the lock-free SETTER (bootstrap_in_flight + early os_pci_set_disconnected in nv_pci_error_detected, nv-pci.c:2974). ADD A14: make every GSP-bootstrap poll honor the lock-free os_pci marker via one new exported predicate osIsGpuBusLost(pGpu) (a thin wrapper over the existing static-inline osIsGpuBusDead = os_pci_is_disconnected||PDB_PROP_GPU_IS_LOST, os.c:1884), called at TWO chokepoints that cover both proven wedge engines: (1) timeoutCondWait (gpu_timeout.c:591 — the shared primitive behind EVERY gpuTimeoutCondWait site) and (2) the _kgspRpcRecvPoll family (pre-loop gate kernel_gsp.c:2813, in-loop _kgspRpcSanityCheck:314, and the done: heartbeat-print block:2974). This is the design-of-record's \"marker primary, lock-free dual coverage\" done right: A13's os_pci is set lock-free from the AER thread (no API/group lock), and the polls now READ it lock-free — no new lock, no dependence on the COND_ACQUIRE-gated rm_cleanup path that A13 silently relied on and that deferred under the worker's reacquired API lock.","closes_all_polls_how":"UNIFYING MECHANISM: A13 already sets os_pci lock-free at AER (+1.0s, before A12's 3000ms budget). The gap is the READ side: no GSP poll consults os_pci. osIsGpuBusDead (os.c:1884, os_pci||PDB) is referenced ONLY in the os.c MMIO readers, so today a poll aborts on a dead bus only if its cond-fn's 0xFFFFFFFF MMIO read happens to satisfy it (the \"polarity accident\"). A14 makes the poll loops themselves honor osIsGpuBusLost, ending the accident.\n\nPOLL-SITE-BY-POLL-SITE COVERAGE (consolidated ground table; every row reachable from a re-open RmInitAdapter->kgspBootstrap):\n- kfspWaitForSecureBoot/GFW-boot (kern_fsp_gh100:828 / gb202:80) — gpuTimeoutCondWait cond -> COVERED by Edit-6 (timeoutCondWait abort), no longer reliant on the 0xFF==SUCCESS accident.\n- kfspWaitForGspTargetMaskReleased (kern_fsp_gh100:1158) — gpuTimeoutCondWait -> COVERED by Edit-6.\n- kfspWaitForResponse / kfspPollForResponse (kern_fsp.c:411) — FSP EMEM own-loop; chip got PAST FSP in both captures; bounded by GPU_TIMEOUT_DEFAULT. RESIDUAL (bounded, not a wedge site); optional 2-line osIsGpuBusLost check.\n- kgspResetHw asserted/deasserted (kernel_gsp_gh100:133/144) — gpuTimeoutCondWait, polarity-trap on _kgspWaitForDeasserted -> COVERED by Edit-6 (this is the one Class-A poll os_pci could NOT cover via the accident).\n- _kgspEstablishSpdmSession (gh100:620/662, incl. _kgspFalconMailbox0Cleared waits ==0, the polarity trap) — gpuTimeoutCondWait -> COVERED by Edit-6 (not reachable on this CC-off 5090 anyway).\n- gpuTimeoutCondWait(_kgspLockdownReleasedOrFmcError) (gh100:1050) — the ORIGINAL apnex.30 silent wedge -> COVERED DIRECTLY by Edit-6 (no longer relies on mailbox0!=0 accident).\n- GspStatusQueueInit link (message_queue_cpu.c:352) + GspMsgQueueSendCommand tx-full (message_queue_cpu.c:544) — SYSMEM own-loops with gpuCheckTimeout, bounded 4s/1s, never the wedge. RESIDUAL (bounded); optional osIsGpuBusLost check.\n- kgspWaitForRmInitDone->_kgspRpcRecvPoll(GSP_INIT_DONE) (kernel_gsp.c:6151->2682) -> COVERED by Edits 3/4/5.\n- POST-INIT_DONE control RPCs (_issueRpcAndWait->_kgspRpcRecvPoll; SET_GUEST_SYSTEM_INFO @5105, GET_GSP_STATIC_INFO @5112) — the LIVE A13 STORM SITE -> COVERED by Edits 3/4/5.\nThe two chokepoints (Edit-6 timeoutCondWait; Edits 3/4/5 _kgspRpcRecvPoll) are SHARED primitives, not per-cond-fn patches — that is why this is not whack-a-mole.\n\nnetcon2 WALK (apnex.30, no A13, SILENT wedge) — redesign PREVENTS:\nL881 [3472.870770] open scheduled to bounded worker(3000ms): redesign arms bootstrap_in_flight=1 here. Worker enters kgspBootstrap; on a no-A13 build it stalls SILENTLY at the lockdown gpuTimeoutCondWait (and/or, per identical-timing reconciliation, the RPC poll) because no marker is set and the MMIO cond reads block on the PCIe completion timeout. L882 [3473.887623] AER CmpltTO (+1.017s): chip off bus. L893/894 [3473.891113] actual=NEED_RESET (bus reset) because bootstrap_in_flight was absent. REDESIGN DELTA: bootstrap_in_flight=1 -> A13 early branch fires os_pci_set_disconnected (lock-free) at the L889 [3473.889776] error_detected, taking DISCONNECT not NEED_RESET (matches netcon3). osIsGpuBusLost now TRUE. On the worker's very next spin iteration: Edit-6 aborts the lockdown timeoutCondWait (osIsGpuBusLost->NV_ERR_GPU_IS_LOST,break) OR Edits 3/4 abort the RPC poll -> worker returns error -> kgspBootstrap/RmInitAdapter unwind via the already-proven NV_ERR_RESET_REQUIRED fast-drain (the v4 G8 path at 2806) -> A12 wait_for_completion returns WELL before the 3000ms expiry (3475.87) -> foreground rc=-EIO, ldata_lock released. The actual L898 [3473.937751] \"external GPU detected\" -> silence (host wedged) is ELIMINATED: the worker self-aborts ~microseconds after the +1.017s marker instead of hanging.\n\nnetcon3 WALK (apnex.31, A13, RPC STORM wedge) — redesign PREVENTS:\nL686 [1396.572395] open scheduled(3000ms): bootstrap_in_flight=1. Worker advances past lockdown (A13's os_pci made the lockdown cond resolve via 0xFFFFFFFF) into _kgspRpcRecvPoll + post-INIT_DONE control RPCs. L687 [1397.590320] AER CmpltTO(+1.018s). L699 [1397.593882] A13 fires early dead-bus marker (#292) -> os_pci set; osIsGpuBusLost now TRUE. L700 [1397.593944] DISCONNECT sink-set, attempts=0/3 (no bus reset). ACTUAL: L705 [1397.628161] storm begins (+34ms after marker) -> _kgspRpcRecvPoll re-invoked, done:-label heartbeat prints + tmrGetTimeEx \"Bad TimeLo ffffffff\", 46 lines through EOF/wedge L844 [1397.647496]. REDESIGN DELTA: the FIRST poll touch after L699 sees osIsGpuBusLost==TRUE: a worker parked in the for-loop hits _kgspRpcSanityCheck (Edit-4)->NV_ERR_GPU_IS_LOST->goto done, and the done: heartbeat prints are GATED OFF (Edit-5) -> ZERO storm lines; every re-invocation short-circuits at the pre-loop (Edit-3) BEFORE the for-loop/done: -> ZERO prints. NONE of L705..L844 are emitted, so console_loglevel=8 netconsole has nothing to storm on and the CPU is not livelocked. Worker returns error -> RmInitAdapter unwinds (each soldier-on RPC also returns in microseconds via the pre-loop, silent) -> A12 completes within budget -> -EIO, ldata_lock released. The wedge at L844 is ELIMINATED. A candidate covering only the RPC poll (Edits 3/4/5) would close netcon3 but NOT a netcon2 stuck at the lockdown poll — Edit-6 closes that arm, which is why both are required (A13's exact failure was covering one site).","diff_sketch":["os.c (after the static-inline osIsGpuBusDead, ~1896): add exported thin wrapper to preserve hot-path inlining of the inline in osDevReadReg008/016/032 -- `NvBool osIsGpuBusLost(OBJGPU *pGpu){ return osIsGpuBusDead(pGpu); }`  (NULL-safe via the inline's pGpu==NULL guard).","inc/kernel/gpu/nv-gpu-lost.h (project-owned header, already included by kernel_gsp.c:27): add `NvBool osIsGpuBusLost(struct OBJGPU *pGpu);` next to cleanupGpuLostStateAtomic. Declare in the PROJECT header, NOT generated g_os_nvoc.h, to keep blast radius in project-owned territory. (NvBool resolves via nvtypes already pulled in ahead of this header in both TUs.)","kernel_gsp.c:2813-2814 (Edit-3, _kgspRpcRecvPoll pre-loop gate): OR-in `|| osIsGpuBusLost(pGpu)` into the existing `bFatalError || PDB_PROP_GPU_IS_LOST` short-circuit -> returns NV_ERR_RESET_REQUIRED. Reuses the proven v4-G8 fast-drain unwind; covers RPC re-invocation storm.","kernel_gsp.c:314-318 (Edit-4, _kgspRpcSanityCheck, called every loop iter at 2851): OR-in `|| osIsGpuBusLost(pGpu)` into the existing `!API_GPU_ATTACHED || PDB_PROP_GPU_IS_LOST` clause -> returns NV_ERR_GPU_IS_LOST. Covers a worker parked inside the for-loop.","kernel_gsp.c:2974-2981 (Edit-5, done: heartbeat prints): wrap the two _kgspHeartbeatIsGspRm/Libos timed-out NV_PRINTF(LEVEL_ERROR) blocks in `if (!osIsGpuBusLost(pGpu)) { ... }` -> suppresses the false 'heartbeat timed out' storm on a dead PTIMER (0xFFFFFFFF). Belt-and-suspenders; Edit-3 already prevents re-entry from reaching here.","gpu_timeout.c: add `#include \"gpu/nv-gpu-lost.h\"`; in timeoutCondWait (591) insert at loop top before osSpinLoop: `if (osIsGpuBusLost(pGpu)) { status = NV_ERR_GPU_IS_LOST; break; }`. ONE chokepoint covers ALL gpuTimeoutCondWait sites (lockdown, SPDM, mailbox, reset asserted/deasserted, FSP-via-cond, CC-cleanup) polarity-independently -- fixes the _kgspFalconMailbox0Cleared waits-==0 trap. pGpu==NULL-safe; healthy waits unaffected (predicate FALSE).","RELABEL/PACKAGING: A13 stays addon (workaround setter). A14 is L1 GSP-core robustness -> carve as C-series (C7) upstream-bound on the fork, documented in docs/patches.md + docs/upstream-plan.md; candidate for the NVIDIA PR (generalizes: any surprise-removed GPU mid-init benefits)."],"regression":"NO regression vs R1-R4 / A10-v2 / A12 / normal cold-init, because osIsGpuBusLost is TRUE only when os_pci_is_disconnected OR PDB_PROP_GPU_IS_LOST is set -- a condition a healthy bus never has. NORMAL COLD-INIT: bus alive, both markers clear -> all 4 new checks are pure-FALSE no-ops (one cheap predicate per poll iteration; osIsGpuBusDead kept static-inline so the per-register hot read path is NOT de-inlined). Identical bring-up; BAR1 32G, params unchanged. R1-R4 (WPR2 fast-fail contained; resets contain-only): the WPR2-stuck fast-fail re-open has a LIVE bus (no os_pci, no PDB) -> osIsGpuBusLost FALSE -> no short-circuit -> identical fast-fail; A14 never touches the fast-fail path. A10-v2 lockdown arm (nv.c:2002-2003 os_pci+rm_cleanup): PRESERVED byte-identical; A14 only makes its flush_work join FASTER (worker aborts sooner) -> strictly better; surprise-removal SURVIVAL (the 06-05 clean A2->C5->A7 result) is on the close/teardown path, untouched. A12 funnel (3000ms budget + A10-v2 grace discriminator): PRESERVED; A14 makes the worker self-abort BEFORE the budget in the AER case, so A12's timeout arm fires LESS often; the grace fast-fail keys on completion-state not markers -> unchanged. Edit-3/4 reuse the EXISTING NV_ERR_RESET_REQUIRED/NV_ERR_GPU_IS_LOST return values (no new error codes); Edit-6 returns NV_ERR_GPU_IS_LOST instead of NV_ERR_TIMEOUT on a dead bus -- callers (gh100:1052, SPDM, reset) test `status != NV_OK` generically; a caller-audit for any NV_ERR_TIMEOUT special-case is the one listed validation item.","locks_uaf":"LOCK-FREE on BOTH ends -> no new lock, no inversion, no UAF. SETTER (unchanged A13): os_pci_set_disconnected is a WRITE_ONCE on the Linux pci_dev disconnect flag from the AER thread -- no API lock, no GPU group lock. READER (A14): osIsGpuBusLost -> osIsGpuBusDead -> READ_ONCE of os_pci_is_disconnected(nv->handle) + a PDB bit read; the worker already owns the GPU group lock during the poll and needs NO additional lock to read these. This deliberately AVOIDS the trap that sank A13 live: the AER thread's rm_cleanup_gpu_lost_state uses rmapiLockAcquire(COND_ACQUIRE) which DEFERS because the worker REACQUIRED the API lock at kernel_gsp.c:4820 for the post-INIT_DONE RPC phase (confirmed: heartbeat-initialized => storm is post-INIT_DONE => API lock held). We never route the marker through that COND_ACQUIRE path; we make the lock-free os_pci marker (already set) sufficient. UAF: osIsGpuBusLost resolves nv via NV_GET_NV_STATE(pGpu) then reads nv->handle -- the EXACT deref the os.c MMIO readers already perform on every read during this identical bootstrap window, so the access pattern is proven-safe here; pGpu/nv/pci_dev are all live (worker mid-init, flush_work not yet joined). pGpu==NULL guarded (returns FALSE), covering timeoutCondWait's non-GPU callers. No torn-write concern: os_pci is a single Linux flag, monotonic; PDB read is unaffected. Edit-6 returning early does NOT skip any unlock (timeoutCondWait takes no lock).","observability":"The fix does NOT depend on the printk-storm factor; it REMOVES the storm at its source and is robust to the unresolved F44-vs-printk attribution. Edit-3 (pre-loop) makes every _kgspRpcRecvPoll re-invocation return BEFORE the for-loop, the timeout branch, AND the done: heartbeat prints -> zero of the 46 netcon3 storm lines (tmrGetTimeEx 'Bad TimeLo'/'heartbeat timed out 5200') are emitted; Edit-5 additionally gates the done: prints on !osIsGpuBusLost as belt-and-suspenders. So whichever VERIFIED reading is correct -- (a) the hard wedge was the synchronous console_loglevel=8 netconsole storm livelocking the CPU, or (b) it was the F44 ldata_lock hold across an unbounded worker -- A14 closes it: (a) no prints to storm on; (b) the worker self-aborts in microseconds at +1.0s (os_pci set at AER) so flush_work joins and ldata_lock is released. The marker-coverage gap is the NECESSARY upstream cause of BOTH the storm and the lock-hold, so removing it dominates either branch. Caveat honored: A13 alone was COUNTERPRODUCTIVE (its os.c:2050 short-circuit pre-empts the DETECTOR_MMIO_DEAD self-heal and converted the silent hang into a fast storm); A14 supplies the missing reader so the lock-free marker self-terminates the poll the way A13 intended. Re-test must use PASSIVE observability only (loglevel<=4, async kmsg, external 100ms ping/ssh liveness, passive sysrq-w/t/l) per project policy -- no bpftrace/kprobe (perturbs; IBT blocks kprobe on closed RM).","validation":"1) COMPILE not apply-check: real `make modules` of the composed C+E+A+C7 tree against the 7.0.9-fc44 kernel (apply != validated; P5 lesson). Audit no caller branches on NV_ERR_TIMEOUT-vs-NV_ERR_GPU_IS_LOST from timeoutCondWait (gh100:1052/SPDM/reset all test !=NV_OK -> expected clean). 2) LIVE repro on apnex.32 (redesign build): exact #292 substrate -- persistence-OFF re-open of the #979-EQ-diverged, fix-bar1-recovered 32G chip (deauth->fix-bar1->pm0->roll). One variable, n>=3 (reliability methodology). EXPECTED: at AER CmpltTO (~+1.0s) A13 sets os_pci, poll aborts within one iteration, worker returns NV_ERR_RESET_REQUIRED, RmInitAdapter unwinds, foreground returns -EIO, ldata_lock released, NO storm, host SURVIVES (re-open fails cleanly; chip recoverable by a subsequent cold-plug). 3) MINIMISED-OBSERVABILITY confirm: rerun with `echo 4 4 1 7 > /proc/sys/kernel/printk` + external liveness -> host survives identically with/without storm-loglevel, proving the fix is printk-independent. 4) NO-REGRESSION matrix: R2 WPR2 fast-fail still fast-fails (n>=3); normal cold-init healthy (BAR1 32G, persistence engages, soak); A10-v2 surprise-removal SURVIVAL still clean; A12 budget still bounds a synthetic stuck worker. 5) On any residual wedge, passive sysrq blocked-task + all-CPU backtrace to identify the new stuck site (would implicate a Class-C/D SYSMEM/FSP residual poll). Then 14-day soak before cutover.","sovereignty":"L1 GSP-core patch -- requires explicit justification per the sovereign-modules policy, and it is justified: the poll-abort predicate lives intrinsically inside NVIDIA's poll loops (_kgspRpcRecvPoll's gate, timeoutCondWait's while-cond); there is NO L4-L6 out-of-fork way to make those loops honor a disconnect marker, so L1 is unavoidable for the READER half (A13's setter half stays in kernel-open/nv*.c). Blast radius is concentrated by design: ONE new predicate osIsGpuBusLost (a thin wrapper over the existing osIsGpuBusDead -- no new semantics), declared in the PROJECT-owned nv-gpu-lost.h (NOT generated g_os_nvoc.h, so no NVOC-gen edits), called at exactly 4 sites (3 kernel_gsp.c + 1 gpu_timeout.c). Cost: widens our owned surface in kernel_gsp.c/gpu_timeout.c -- files we would rather not carry. Mitigation: it is upstream-aligned (GSP polls SHOULD honor pci_dev_is_disconnected for ANY surprise-removed GPU, not just eGPU) -> carve as C7 (Core, upstream-bound) on the fork, document in docs/patches.md + upstream-plan.md, and queue for the NVIDIA PR; this turns an L1 workaround into an upstream contribution that eventually leaves our tree. Distinct senses both satisfied: L1 (unavoidable, justified) + cohesion (concentrated, one predicate, project header).","observability_note":"n/a","confidence":"medium","open":["Class-C SYSMEM polls (GspMsgQueueSendCommand tx-full message_queue_cpu.c:544; GspStatusQueueInit link:352) and Class-D kfspWaitForResponse (kern_fsp.c:411) are BOUNDED (1s/4s/GPU_TIMEOUT_DEFAULT) and never observed as wedge sites, so left uncovered by the two chokepoints; cheap optional osIsGpuBusLost checks would close them for defense-in-depth. Honest residual to the 'every poll' mandate -- bounded, not infinite.","Whether RmInitAdapter 'soldiers on' issuing many post-INIT_DONE control RPCs after the first NV_ERR_RESET_REQUIRED is not pinned from the capture; even if it does, each now returns in microseconds via the pre-loop with no prints, so it cannot storm or hold the lock long -- but the live re-test should confirm the unwind is prompt.","The F44-ldata_lock-vs-printk-storm split (VERIFIED claim-2 refuted-medium) is still unresolved; A14 is robust to both, and the minimised-observability re-test (step 3) will finally separate them.","netcon2's exact stuck poll is inference (passive netconsole, no stack); Edit-6 + Edits-3/4/5 cover BOTH candidate sites (lockdown and RPC), so the ambiguity does not gate the fix -- but a passive sysrq stack on any future repro would retire the open question.","Ship C' fail-fast re-open gate as phase-2 defense-in-depth alongside A14 (design-of-record: marker primary, C' probabilistic since divergence is driver-invisible live via the fix-bar1 sticky bit) -- complementary, out of scope for THIS candidate."]},{"candidate":"CANDIDATE = \"C7 in-flight dead-bus poll coverage (load-bearing) + A13 AER marker-source (kept) + A14 fix-bar1 fail-fast gate (defense-in-depth)\". The live A13 FAIL proved the in-flight backstop — not a fail-fast gate — is what actually CLOSES the polls, so this composition makes the dead-bus poll-coverage the load-bearing layer and demotes the gate to a deterministic-but-non-complete first line.\n\nWHY NOT A13b/dual-marker-from-AER (REJECTED, source-proven): the storm is the POST-INIT_DONE control RPCs (SET_GUEST_SYSTEM_INFO@5105 / GET_GSP_STATIC_INFO@5112), issued AFTER _kgspBootGspRm REACQUIRES the RM API lock (kernel_gsp.c:4818-4820). Proof from netcon3: it fires _kgspIsHeartbeatTimedOut, which is only meaningful after _kgspHeartbeatInit (kernel_gsp.c:6164), which runs only after INIT_DONE drains. So during the storm the worker HOLDS the reacquired API lock → rm_cleanup_gpu_lost_state's rmapiLockAcquire(COND_ACQUIRE) (osapi.c:1913) DEFERS → PDB_PROP_GPU_IS_LOST is never set by the AER thread, and the SAME deferral disables A12's +timeout LOCKDOWN-arm rescue (nv.c:2003, same COND_ACQUIRE). Making COND_ACQUIRE blocking would re-introduce the F44 wedge C1/C6 fixed. Setting PDB lock-free via gpuSetDisconnectedProperties direct from the AER thread is viable but (a) torn-writes a shared PDB bitfield concurrently with the worker and (b) still misses the polarity-trap timeoutCondWait conds. So the ONLY reliable lever is to make the poll SITES honor the lock-free os_pci marker A13 already sets — that is C7.\n\nCOMPOSITION / load-bearing: A13's os_pci_set_disconnected (lock-free WRITE_ONCE, nv-pci.c:2981, gated on bootstrap_in_flight alone) is the MARKER SOURCE and is kept verbatim. C7 makes BOTH GSP poll-engines READ that marker via osIsGpuBusDead (os.c:1884 = os_pci OR PDB). C7 is load-bearing because it is what closes every poll-site regardless of lock state, divergence-detectability, or loglevel. A14 (fail-fast gate, fix-bar1 sticky bit) is the preferred first line — for the known #292 production substrate (a fix-bar1-recovered EQ-diverged chip) it deterministically refuses the re-open before any GSP poll is entered — but because a NOVEL in-flight divergence is driver-invisible live, A14 cannot be the completeness guarantee; it ships WITH C7, never instead. Minimal total change is C7 alone (it closes both captured wedges); A14 is separable phase-2.","closes_all_polls_how":"TWO chokepoints close EVERY ground-table poll-site by engine; A13's lock-free os_pci is the marker both now honor.\n\nENGINE-1 = timeoutCondWait (gpu_timeout.c:591), the shared primitive behind EVERY gpuTimeoutCondWait. C7 adds `if (osIsGpuBusDead(pGpu)) { status = NV_ERR_TIMEOUT; break; }` inside the while(!cond) loop. ONE edit covers, uniformly and lock-model-independently: (1) _kgspLockdownReleasedOrFmcError [netcon2 origin], (2) _kgspFalconMailbox0Cleared [POLARITY TRAP: waits ==0, 0xFFFFFFFF never satisfies it — os_pci dead-value alone CANNOT close this; C7 does], (3) _kgspSpdmBootedOrFmcError, (4) _kgspWaitForAsserted/_kgspWaitForDeasserted [kgspResetHw; ALSO fixes the TMR/PTIMER-dead edge where the dead PTIMER stalls the timeout so it never advances — C7 breaks on the marker regardless of timer], (5) _kgspHasCcCleanupFinished, (6) kfspWaitForGspTargetMaskReleased / kfspWaitForSecureBoot where timeoutCondWait-based. ENGINE-2 = _kgspRpcRecvPoll (kernel_gsp.c:2682). C7 adds `|| osIsGpuBusDead(pGpu)` to the pre-loop short-circuit (2813) AND to _kgspRpcSanityCheck's connectivity gate (314), and gates the done-block heartbeat prints (2974-2980) on `!osIsGpuBusDead(pGpu)`. Covers kgspWaitForRmInitDone INIT_DONE poll AND every post-INIT_DONE control RPC [netcon3 STORM]. ENGINE-3 = message_queue_cpu.c sysmem waits (tx-full @544, status-queue @352): bounded at 1s/4s (NOT the observed wedge), covered by ENGINE-1 if timeoutCondWait-based, else by the kgspHealthCheck MMIO sub-condition (os_pci→RESET_REQUIRED); a one-line osIsGpuBusDead add closes them for whack-a-mole completeness (flagged in open).\n\nWALK netcon2 (apnex.30 SILENT lockdown wedge): L860 chip BAR1=32G diverged; L881 [3472.870770] open scheduled, bootstrap_in_flight=1. [A14 first line: if fix-bar1 set the sticky bit this re-open returns -EIO here, before queue_work — no poll, no AER, prevented. Assume A14 missed, to prove the backstop.] L882 [3473.887623] AER CmpltTO at +1.017s. My candidate's A13 AER half fires (original netcon2 LACKED it → its MMIO reads incurred full hardware CmpltTO each iteration → slow-SILENT hang) → os_pci set lock-free. The worker is at the lockdown gpuTimeoutCondWait (or has advanced to the RPC poll): ENGINE-1's osIsGpuBusDead break (or ENGINE-2's pre-loop) fires on the next iteration → wait returns NV_ERR_TIMEOUT → kgspBootstrap_GH100 returns NV_ERR_NOT_READY → bootstrap unwinds → A12 flush_work joins in ~ms → open returns -EIO → ldata_lock released. No silent hang; the +1.067s death (L898 then silence) never occurs.\n\nWALK netcon3 (apnex.31 A13-FAIL RPC STORM): L686 [1396.572395] open scheduled, in_flight=1. [A14 first line as above.] L687 [1397.590320] AER CmpltTO +1.018s. L699 [1397.593882] A13 fires → os_pci set. L700 [1397.593944] DISCONNECT, rm_cleanup COND_ACQUIRE DEFERS (worker holds reacquired API lock) → PDB stays clear → THIS is why A13 alone stormed. L705-L844 [1397.628161→1397.647496] the ~23-pair heartbeat/Bad-TimeLo storm to EOF (host died). WITH C7: the first _kgspRpcRecvPoll re-entry after L699 hits the pre-loop (2813) `|| osIsGpuBusDead` → os_pci is set → returns NV_ERR_RESET_REQUIRED at 2821, BEFORE the for-loop and BEFORE the done: label → NO heartbeat print. SET_GUEST_SYSTEM_INFO fails → kgspInitRm goto done (5109) → API lock released → unwind → flush_work joins → -EIO → ldata_lock released, all within ~ms of L699 (~1397.594), i.e. ~34ms BEFORE the storm's first line (L705 @1397.628). A worker already inside a for-loop iteration when os_pci landed exits via the sanity gate (314→2859→done), but the done-block prints are gated on !osIsGpuBusDead → at most zero heartbeat lines. The storm L705-L844 is eliminated at source; the printk amplifier has nothing to amplify.","diff_sketch":["C7-e1 os.c:1884 — un-`static`-`inline` osIsGpuBusDead; declare `NvBool osIsGpuBusDead(OBJGPU*)` in the OS-interface header alongside osIsGpuShutdown (precedent: osIsGpuShutdown is os.c-defined and already called from kernel_gsp.c:320). Pure visibility change; body unchanged.","C7-e2 gpu_timeout.c timeoutCondWait (~593, inside `while(!pCondFunc)` after osSpinLoop): `if (osIsGpuBusDead(pGpu)) { status = NV_ERR_TIMEOUT; break; }`. ONE site covers all gpuTimeoutCondWait conds incl. polarity-trap + TMR-dead reset waits. NV_ERR_TIMEOUT = existing caller-handled return; zero new error paths.","C7-e3 kernel_gsp.c _kgspRpcRecvPoll pre-loop 2813-2814: append `|| osIsGpuBusDead(pGpu)` to the `bFatalError || PDB_PROP_GPU_IS_LOST` short-circuit. Each RPC re-entry returns NV_ERR_RESET_REQUIRED at 2821 before for-loop/done: → silences storm + fast-unwinds the control-RPC sequence.","C7-e4 kernel_gsp.c _kgspRpcSanityCheck 314-315: append `|| osIsGpuBusDead(pGpu)` to the `!API_GPU_ATTACHED_SANITY_CHECK(pGpu) || PDB_PROP_GPU_IS_LOST` gate → frees a worker already parked inside the for-loop on its next iteration (goto done 2859).","C7-e5 kernel_gsp.c done-block 2974-2980: gate the two heartbeat NV_PRINTFs on `!osIsGpuBusDead(pGpu) && _kgspHeartbeatIs...TimedOut(...)` → even the one parked-worker exit emits zero storm lines (observability-amplifier removed at source).","C7-e6 (completeness, optional) message_queue_cpu.c:352 + :544 — same osIsGpuBusDead short-circuit in the two sysmem wait loops IF hand-rolled (bounded 1s/4s regardless).","KEEP VERBATIM (no regression): A13 nv-pci.c:2974-2982 (os_pci marker source, bootstrap_in_flight gate); A10-v2 LOCKDOWN arm nv.c:2002-2003; C1 osapi.c:1913 COND_ACQUIRE (MUST stay non-blocking — blocking = F44 regression); C5 cleanupGpuLostStateAtomic; C6 primitive. Only edit: refresh A12 JOIN-COST comment nv.c:2027-2036 to note C7 now bounds the RPC poll too, not just the lockdown MMIO-cond.","A14 (defense-in-depth, A-series addon, separable): fix-bar1.sh writes a 0200 per-device sticky sysfs `diverged_recovered` bit (precedent tb_egpu_recover_force_trigger_store nv-tb-egpu-recover.c:735); new nv_state_t bool (cleared on fresh enumeration since nvl is re-created); nv_bootstrap_bounded nv.c:~1904 (after is_external_gpu gate, before queue_work): `if (diverged_recovered && nv->is_external_gpu && last_close_wpr2_cleared) return -EIO;` — enters ZERO GSP polls. Conjunctive to bar false-positives."],"regression":"NO regression vs any prior result; C7 is additive short-circuits that fire ONLY when osIsGpuBusDead is already TRUE. NORMAL COLD-INIT: on a healthy chip os_pci is clear and PDB is clear, so every added `|| osIsGpuBusDead()` evaluates FALSE — the bringup path is behaviorally identical (one cheap READ_ONCE flag-read per poll iteration / per RPC call; sub-ns, no allocation, no lock). Healthy BAR1-32G init, R1-R4 cold opens, and the measured ~1.3-1.9s full cold init are untouched. R1-R4 / WPR2-FAST-FAIL: the fast-fail worker RETURNS (never enters the storm); A10-v2's grace arm explicitly SKIPS the marker on fast-fail (nv.c:1981-1988, chip NOT sunk) so os_pci/PDB stay clear → C7 never fires on that substrate → R2 recoverable-in-driver behavior preserved; resets stay contain-only. A10-v2 LOCKDOWN ARM (nv.c:2002-2003): unchanged; C7 makes its own JOIN-COST promise (\"self-terminate within one iteration\") finally TRUE for the RPC poll, not just the lockdown MMIO-cond — strictly better (faster flush_work join, shorter ldata_lock hold). A12 funnel, C1 COND_ACQUIRE, C5/C6 sink: byte-identical. The one prior CLAIM corrected (not a regression in actuation): finding-3's \"+3000ms A12 recovery / 5200ms-bounded worker\" optimism — source shows the post-INIT_DONE storm holds the reacquired API lock so A12's LOCKDOWN-arm rm_cleanup ALSO defers; C7 makes recovery independent of any lock-gated PDB set, so this correction strengthens, not weakens, the deployed funnel.","locks_uaf":"LOCK-CLEAN by construction. C7 acquires NO lock: osIsGpuBusDead is a READ_ONCE of the Linux pci_dev disconnected flag + a PDB getProperty bit-read — no API lock, no GPU group lock, no ldata_lock, no new ordering, no inversion. It only READS markers; it NEVER writes PDB or os_pci, so there is ZERO torn-write surface (the A13b-direct-gpuSetDisconnectedProperties alternative WOULD torn-write the PDB bitfield concurrently with the worker — C7 avoids that entirely by reading the marker A13 already set lock-free). The decisive deadlock that sank A13 (worker holds reacquired API lock post-INIT_DONE → rm_cleanup COND_ACQUIRE defers → PDB unset) is SIDESTEPPED: C7 needs no marker the lock-holder must publish; the lock-free os_pci marker is sufficient. We deliberately KEEP C1's COND_ACQUIRE non-blocking — converting it to blocking is the F44 wedge and is explicitly not done. UAF: the A12 JOIN GUARD (flush_work before the caller frees its sp; refcount-2 on the work struct; nvlfp write kept on the syscall thread) is PRESERVED unchanged — C7 makes the worker RETURN faster, which shrinks the ldata_lock-hold window and makes the join strictly safer; no worker outlives its caller. osIsGpuBusDead derefs pGpu→NV_GET_NV_STATE→handle, all stable for the device lifetime and valid while the worker holds the GPU group lock across the poll. A14's fail-fast returns -EIO before queue_work, so it creates no worker, no sp lifetime, no UAF.","observability":"Decouples the fix from the printk-storm factor entirely — this is the key honesty point. The netcon3 hard wedge was co-caused by the synchronous netconsole printk-storm at console_loglevel=8 livelocking the storming CPU while the foreground held ldata_lock; the storm is the done-block heartbeat NV_PRINTFs (kernel_gsp.c:2974-2980) emitted on every _kgspRpcRecvPoll return. C7-e3 makes each post-marker RPC re-entry return at the pre-loop (2821) BEFORE the done: label, and C7-e5 gates the done-block prints on !osIsGpuBusDead — so the storm is eliminated AT SOURCE, not merely hidden. The fix therefore does NOT depend on lowering loglevel: it survives at console_loglevel=8 with netconsole armed because there is no longer a storm to amplify. This is strictly stronger than the finding-doc's \"minimised-observability re-test,\" which only confirms the diagnosis. I keep that re-test as a VALIDATION cross-check (run the repro at BOTH loglevel=8 and loglevel=4; SURVIVAL at both proves observability-independence) and honor project policy by using only PASSIVE channels (async kmsg, external 100ms ping/ssh liveness, passive sysrq-w/t/l blocked-task dump) — no bpftrace/kprobe (perturbs the bug; IBT blocks kprobe on the closed-RM path); kdump/drgn cannot capture the live eGPU wedge.","validation":"Build apnex.32 = deployed set + C7 (A13 retained); A14 as a second build for the gate test. (1) PRIMARY live repro, n>=3: persistence-OFF re-open of the EQ-diverged, fix-bar1-recovered 32G chip (exact #292 A13 stage-5 roll). PASS = open returns -EIO within ~ms of the AER CmpltTO, ldata_lock released, ZERO _kgspRpcRecvPoll/_kgspIsHeartbeatTimedOut/Bad-TimeLo lines in netconsole, GPU sunk (DETECTOR_AER_FATAL or via unwind), host alive, injector un-drains and re-engages — survival like the 06-05 surprise-removal clean run. (2) OBSERVABILITY cross-check: same repro at console_loglevel=8 AND loglevel=4 → SURVIVAL at both (proves C7-e3/e5 removed the storm structurally). (3) NEGATIVE CONTROL normal cold-init (non-diverged cold-plug): healthy bringup, BAR1 32G, params 3000/2000, no -EIO, no new log lines (osIsGpuBusDead FALSE throughout). (4) NEGATIVE CONTROL R2 WPR2-fast-fail re-open: fast-fail arm, chip NOT sunk, recoverable — confirms C7 doesn't fire when the marker is unset. (5) A14 gate: verify fix-bar1 sets the sticky bit; a re-open of a recovered chip returns -EIO before queue_work (no AER, no poll); a fresh cold-plug clears the bit (no false-positive on clean init). Passive sysrq blocked-task + all-CPU backtrace armed as backstop. Per methodology: one variable per test, written hypothesis, n>=3 to resolve.","confidence":"medium","sovereignty":"C7 lands in NVIDIA-core files (gpu_timeout.c, kernel_gsp.c, os.c) so it is an L1/base-C-series patch — justified because it is the symmetric completion of C5/v4's own os.c dead-bus-reader pattern (the readers already honor osIsGpuBusDead; C7 makes the two poll-engines honor the same predicate) and is a genuine core-RM surprise-removal-robustness fix that benefits any GPU falling off the bus during GSP bootstrap, hence upstream-bound (C-series, same destination as C1-C6). Blast radius is concentrated: 1 visibility change (os.c) + 2 short-circuit chokepoints (gpu_timeout.c timeoutCondWait, kernel_gsp.c _kgspRpcRecvPoll) that subsume the entire ground table, no per-cond-fn whack-a-mole. The marker SOURCE stays in the sovereign addon layer (A13 nv-pci.c, A10-v2/A12 nv.c). A14 (fail-fast gate + fix-bar1 sticky bit) is pure A-series addon (project-local, retire-able). Net new core surface is ~5 lines across 3 files; everything else is addon or unchanged.","open":["Storm re-caller not fully pinned: why ~23 _kgspRpcRecvPoll returns rather than one control-RPC abort (long post-INIT_DONE RPC fan-out vs stuck-IRQ re-drive). Does not affect coverage — C7-e3's pre-loop catches EVERY re-entry — but flag for the minimised-observability re-test.","Missing LEVEL_WARNING 'API lock contended, deferring' line in netcon3: source strongly implies COND_ACQUIRE deferral (worker holds reacquired API lock post-INIT_DONE, proven by heartbeat-init firing), but the warning is absent from the capture (dropped amid storm, AER-thread preemption, or unlikely NULL pGpu). Only the OUTCOME (PDB clear) is proven. Confirm with non-storm capture.","ENGINE-3 message_queue_cpu.c sysmem waits (tx-full@544, status-queue@352): bounded 1s/4s, not the observed wedge, covered by ENGINE-1 only if timeoutCondWait-based — verify their loop form; add the C7-e6 one-liner if hand-rolled for whack-a-mole completeness.","A14 divergence detection is deterministic only for the fix-bar1-recovered substrate (sticky bit set); a NOVEL in-flight #979 EQ-divergence with no prior fix-bar1 run leaves the bit clear → A14 misses → relies entirely on C7. This is exactly why C7 is load-bearing and A14 ships-with-never-instead; do not over-trust the gate.","FSP GFW/secure-boot polls (kfspWaitForSecureBoot, kfspPollForResponse kern_fsp.c:411): confirm whether they route through timeoutCondWait (then ENGINE-1 covers) or a hand-rolled MMIO loop (covered incidentally by os_pci dead-value; empirically passed in both captures). If hand-rolled and any cond is value-specific, add the same short-circuit.","Live n=0 so far: confidence is medium pending the n>=3 apnex.32 repro; design-correctness against source + both captures is high, but live behavior of the unwind/join timing and the A12 comment refresh wants empirical confirmation."]}]

## REVIEWS
[{"candidate":"C-prime-failfast-gate (proposed addon A14 / apnex.32) — re-open fail-fast gate at top of nv_bootstrap_bounded","closes_wedge":true,"poll_coverage_complete":false,"regresses":false,"uaf_or_lock":false,"verdict":"adopt-with-changes","strongest_objection":"FALSE-NEGATIVE RESIDUAL + UN-GATED SECOND FUNNEL. C' is coverage-complete BY CONSTRUCTION (gate returns -EIO before queue_work at nv.c:1925, so the worker that runs RmInitAdapter->kgspBootstrap->any of the 13 polls is never created) — but ONLY when its predicate fires, and the predicate depends on diverged_recovered, an OUT-OF-BAND fix-bar1 assertion. The ground table is emphatic that #979 divergence is NOT reliably detectable in-driver live (PMC_BOOT_0/WPR2/LnkSta all read healthy on the wedging chip). So any diverged re-open that did NOT get the fix-bar1 256MB-BAR1 recovery (a future divergence with a different symptom, or recovery by other means) leaves diverged_recovered=0 -> gate bypassed -> worker queued -> the EXACT netcon3 _kgspRpcRecvPoll heartbeat storm recurs. This is not hypothetical; it is the same poll site the live FAIL hit. Separately, the design's structural claim that 'every GSP-bootstrap poll is reachable ONLY from the [nv_bootstrap_bounded] worker' is overstated: nv_dynpower_bounded (runtime-PM GC6/RTD3 exit, nv.c:5726) reaches rm_transition_dynamic_power -> GSP bringup via its OWN queue_work (nv.c:2117), entirely un-gated by C'. Therefore C' provably prevents BOTH CAPTURED wedges but is not a sufficient sole production fix — it MUST ship with the lock-free dual-marker backstop A' (set PDB_PROP_GPU_IS_LOST + clear PDB_PROP_GPU_IS_CONNECTED via gpuSetDisconnectedProperties from the AER thread, NOT via the COND_ACQUIRE-deferred rm_cleanup_gpu_lost_state that demonstrably deferred in netcon3). Net: C' is the right PRIMARY fix for the captured substrate (out-of-band detection is genuinely the most reliable signal for the chip that actually wedges), but coverage-completeness across every reachable poll-site requires A' + gating the dynpower funnel.","must_fix":["SHIP-BLOCKER: C' must ship WITH the lock-free dual-marker fix A' (gpuSetDisconnectedProperties from the AER thread, bypassing the COND_ACQUIRE-gated rm_cleanup_gpu_lost_state which deferred in netcon3 because the worker reacquired the API lock at kernel_gsp.c:4818 before the RPC phase). Reason: divergence is driver-invisible live, so C''s diverged_recovered predicate is a no-op for any un-asserted/future divergence -> worker queued -> netcon3 RPC-poll storm recurs. The design lists this as an 'open' item; it must be promoted to a ship gate, not optional defense-in-depth.","Gate (or explicitly suppress GSP bringup for) the nv_dynpower_bounded runtime-PM funnel (nv.c:2094/5726). It reaches kgspBootstrap via its own queue_work, un-covered by the gate placed only in nv_bootstrap_bounded. A diverged chip waking from GC6/RTD3 reproduces the wedge. The structural 'reachable ONLY from the bootstrap worker' claim is false for this path. (System-resume __nv_pm_resume_locked is fine — it routes through nv_bootstrap_bounded at nv.c:5100.)","Register the new tb_egpu_diverged_recovered (0200) + tb_egpu_reopen_blocked (0444) nodes and the two nvl atomics' surface in an ALWAYS-ON attr group created in nv_pci_probe and removed in nv_pci_remove BEFORE nvl is freed — NOT the existing tb_egpu_recover_attr_group, which recover.c:803-810 only creates when NVreg_TbEgpuRecoverEnable != 0 (live default is 0 at recover.c:70). The design specifies this; verify it in code review + a real `make modules` compile (apply != validated).","Live-validate (n>=3, observability-minimised harness: printk 4 4 1 7 + external 100ms liveness + passive sysrq) that fix-bar1 --bind actually asserts diverged_recovered BEFORE the persistence-OFF re-open and that reopen_gsp_torndown arms on the WPR2=0 LAST-CLOSE. The two captures CANNOT confirm the new out-of-band bit (it did not exist at capture time); prevention is provable-by-construction but the ARMING must be empirically confirmed. WPR2==0 at LAST-CLOSE is capture-confirmed n=2 (netcon2 L878, netcon3 L682).","Document the conservatism/behavioral change: a fix-bar1-recovered chip now requires a cold re-plug (fix-bar1 --bind, slot-cycle -> fresh nvl -> bits clear) per persistence-OFF cycle instead of a soft re-open, and add the optional read-only tb_egpu_reopen_blocked companion so orchestration can distinguish a C' refusal (-EIO=cold-recover) from other -EIO. Fail-safe, but it is a contract change for the injector bind path."]},{"candidate":"A13b-full-sink","closes_wedge":true,"poll_coverage_complete":false,"regresses":false,"uaf_or_lock":false,"verdict":"adopt-with-changes","strongest_objection":"NV_GET_NV_PRIV_PGPU API-lock contract violation. The new lock-free rm_set_gpu_lost_inflight resolves pGpu via NV_GET_NV_PRIV_PGPU(nv), whose own definition (src/nvidia/arch/nvalloc/unix/include/nv-priv.h:352) carries the explicit precondition comment \"Make sure that your stack has taken API Lock before using this macro.\" Bypassing the API lock is the entire point of the new entry (to dodge rm_cleanup's COND_ACQUIRE deferral), so the violation is structural, not incidental. This is the FIRST lock-free nv->pGpu resolution in the lost-state code: the existing lock-free caller (os.c:2090 DETECTOR_MMIO_DEAD) already holds pGpu in hand and never touches the macro, and the only other nv->pGpu resolver (rm_cleanup_gpu_lost_state, osapi.c:1913-1915) honors the convention by acquiring the API lock first. In the bootstrap_in_flight window the deref is safe by lifecycle (pGpu is set at osAttachGpu osinit.c:311 before bootstrap and cleared only at detach osinit.c:1674, and error_detected runs synchronously while the worker still holds the device), but the design does not address the documented contract at all. This is precisely the category of unreviewed precondition gap (cf. the channel-state gate bug) that the mandate flags as having caused the A13 live FAIL, and it must be justified in-code or replaced with a lock-free-sanctioned pGpu resolution before ship.","must_fix":["API-lock contract: rm_set_gpu_lost_inflight resolves pGpu via NV_GET_NV_PRIV_PGPU(nv) without the API lock, violating the macro's documented precondition (nv-priv.h:352). Justify in-code why the bootstrap_in_flight lifecycle (pGpu linked osinit.c:311 at attach, cleared only at detach osinit.c:1674) makes the lock-free deref safe, or resolve pGpu by a sanctioned lock-free path. Same class of unreviewed precondition gap that shipped A13.","Poll coverage is NOT complete. Sysmem-cond waits (message_queue_cpu.c:544 GspMsgQueueSendCommand tx-full, :352 GspStatusQueueInit) are reachable from a re-open and immune to BOTH markers (sysmem reads, not PDB-gated) - bounded only by their own 1s/4s OSTIMER. Worst edge: worker parked in the 4s GspStatusQueueInit wait at AER time -> the foreground +3000ms arm flush_work (nv.c:2037) BLOCKS holding ldata_lock until the worker exits the sysmem wait (~1s residual). Ship the deferred L1 osIsGpuBusDead short-circuit (kernel_gsp.c pre-loop/sanity + gpu_timeout.c loop) so os_pci suffices everywhere, OR pair mandatorily with C' fail-fast so a divergent re-open never enters these polls. Do not rely on 'bounded latency'/'unreachable' as the answer to a mandate that demands EVERY poll be covered.","The 'ZERO done: heartbeat prints' claim is overstated. If the worker is already mid-for-loop inside a _kgspRpcRecvPoll call that began before PDB was planted, it exits via _kgspRpcSanityCheck (kernel_gsp.c:2851->314) which falls through to the done: label (2971-2981) and emits up to ~1 heartbeat print-pair before returning NV_ERR_GPU_IS_LOST. Only the pre-loop path (2813-2821) is print-free. Net is still storm-elimination, but the design's 'ZERO' is wrong; validate live with console_loglevel=4 that residual prints are a handful, not a storm.","Live n>=3 on the real persistence-OFF Path-B substrate remains the gate. netcon2's wedge point is silent/inferential (zero GSP-poll printks), so the silent-wedge vector can only be confirmed live, not from source. Also confirm RmInitAdapter abort-promptness (open question #1: NV_ERR_RESET_REQUIRED treated as fatal vs soldier-on) - even soldier-on yields print-free pre-loop returns over a bounded RMInitAdapter RPC sequence, but confirm the worker returns and releases the GPU group lock within budget so flush_work joins and open returns -EIO."]},{"candidate":"A13-keep + A14 \"poll-honors-the-marker\" (kernel_gsp-deadbus-shortcircuit)","closes_wedge":true,"poll_coverage_complete":false,"regresses":false,"uaf_or_lock":false,"verdict":"adopt-with-changes","strongest_objection":"The candidate's central selling point — \"two SHARED-primitive chokepoints, not whack-a-mole\" — is TRUE for the two proven wedge engines (timeoutCondWait covers every gpuTimeoutCondWait site incl. lockdown; Edits 3/4/5 cover the _kgspRpcRecvPoll family) and I verified both against source and both captures: netcon3's storm (L705-L844, each group = one _kgspRpcRecvPoll return through done:) is eliminated because the pre-loop short-circuit is a DIRECT return (not goto done) so re-invocations never reach the prints (Edit-3), the in-flight invocation exits via the per-iteration sanity check before the timeout/heartbeat block (Edit-4), and Edit-5 wraps the FULL done: predicate `if` blocks so the tmrGetTimeEx/_kgspIsHeartbeatTimedOut side-effect prints are also gated (all 3 lines/group suppressed); netcon2's silent wedge (whether lockdown or, per identical +1.0s timing, the RPC poll) is covered by Edit-6 returning NV_ERR_GPU_IS_LOST, which the lockdown caller at gh100:1050 handles via a generic `if(status!=NV_OK)`. BUT the claim is INCOMPLETE: the Class-C SYSMEM own-loops (message_queue_cpu.c GspMsgQueueSendCommand tx-full :544 and GspStatusQueueInit :352) and the Class-D FSP own-loop (kern_fsp.c kfspWaitForResponse :411) are hand-rolled poll loops that route through NEITHER chokepoint and remain UNcovered (bounded 1s/4s, never observed as wedge sites, but not short-circuited). \"Covers every reachable poll-site\" is therefore literally false — which is precisely the incomplete-per-poll-coverage class that let A13 ship and wedge live. The candidate honestly flags these as residuals, so this is an acknowledged gap, not a hidden one; it bounds the verdict to adopt-with-changes rather than a clean adopt.","must_fix":["Close (or provably risk-accept with C') the two own-loops OUTSIDE both chokepoints to satisfy the literal 'every poll' mandate: add the cheap osIsGpuBusLost short-circuit to message_queue_cpu.c GspMsgQueueSendCommand tx-full wait (:544) + GspStatusQueueInit link wait (:352) and kern_fsp.c kfspWaitForResponse (:411). These are bounded but are the exact 'bounded-but-uncovered' residual that the A13 live-FAIL teaches must not be hand-waved.","Caller-audit ALL gpuTimeoutCondWait callers for any NV_ERR_TIMEOUT special-case, since Edit-6 now returns NV_ERR_GPU_IS_LOST on a dead bus. Lockdown caller (gh100:1050) VERIFIED generic (!=NV_OK); SPDM (gh100:620/662), kgspResetHw asserted/deasserted (gh100:133/144), and CC-cleanup callers must each be confirmed to test !=NV_OK and not branch on NV_ERR_TIMEOUT. (Healthy paths are unaffected — marker is FALSE — so any change is dead-bus-only and benign-or-better, but confirm explicitly.)","Implement Edit-5 by wrapping the FULL `if(_kgspHeartbeatIsGspRm/LibosHeartbeatTimedOut(...)){NV_PRINTF}` blocks (NOT just the NV_PRINTF) inside `if(!osIsGpuBusLost(pGpu))`, so the predicate call — which is what emits the tmrGetTimeEx 'Bad TimeLo' and _kgspIsHeartbeatTimedOut lines — is short-circuited. As worded this holds; the implementation must preserve it.","Preserve the bPollingForRpcResponse=NV_FALSE clear on Edit-3's new exit (OR-in osIsGpuBusLost into the EXISTING pre-loop condition rather than adding a separate early return) so the next entrant does not trip NV_ASSERT_OR_RETURN(!bPollingForRpcResponse). Edit-4's path already clears it at done:.","COMPILE (real `make modules` against 7.0.9-fc44), not `git apply --check`, of the composed C+E+A+C7 tree (P5 lesson) — the new exported osIsGpuBusLost wrapper + nv-gpu-lost.h declaration + gpu_timeout.c include must build clean across all TUs.","LIVE n>=3 on apnex.32 with the exact #292 substrate (persistence-OFF re-open of the EQ-diverged fix-bar1-recovered 32G chip), PLUS a minimised-observability run (printk 4 4 1 7 + external 100ms liveness) to confirm host SURVIVES identically with/without storm-loglevel — proving the fix is printk-independent. A13 passed source-review and FAILED live; this candidate must clear the live gate before promotion to clean 'adopt'."]},{"candidate":"C7 in-flight dead-bus poll coverage (load-bearing) + A13 AER marker-source (kept) + A14 fix-bar1 fail-fast gate (defense-in-depth)","closes_wedge":true,"poll_coverage_complete":false,"regresses":false,"uaf_or_lock":false,"verdict":"adopt-with-changes","strongest_objection":"The design's thesis is \"close EVERY poll-site regardless of lock state,\" yet its two load-bearing chokepoints (C7-e2 timeoutCondWait + C7-e3/e4 _kgspRpcRecvPoll) provably do NOT cover the hand-rolled message-queue sysmem waits, and the fix for them (C7-e6) is self-relegated to \"optional.\" Verified at message_queue_cpu.c:544: the tx-full wait is `while(NV_TRUE)` whose ONLY escape is gpuCheckTimeout, reset to 1s PER ELEMENT (gpuSetTimeout @541 inside the per-element for-loop) — NO MMIO health-check escape at all (unlike GspStatusQueueInit @382). If the diverged chip falls off the bus while the worker is sending a multi-element init RPC, the worker spins ~1s*elemCount holding the GPU group lock while the foreground holds ldata_lock across flush_work — multi-second, the same F44 blast-radius substrate, and exactly the \"incomplete per-poll marker coverage\" class that produced the A13 live FAIL. The captured wedges (lockdown, RPC poll) ARE closed at source and loglevel-independently, so this is not a reject — but shipping with a known-uncovered-merely-bounded poll-site repeats the precise methodological error the mandate calls out, so poll_coverage_complete cannot be asserted until C7-e6 is mandatory and the FSP polls are audited.","must_fix":["Promote C7-e6 from optional to REQUIRED: add the osIsGpuBusDead short-circuit to the message_queue_cpu.c sysmem waits, ESPECIALLY the tx-full wait at line 544 (verified: while(NV_TRUE), no MMIO escape, bounds only on gpuCheckTimeout 1s-per-element => up to 1s*elemCount of GPU-group-lock hold on a dead bus). This is a genuine uncovered poll-site of the same class that sank A13; leaving it bounded-but-not-short-circuited is the error the mandate forbids.","Complete the FSP poll-routing audit (open item 5) BEFORE claiming completeness: confirm kfspWaitForSecureBoot / kfspPollForResponse / kfspWaitForGspTargetMaskReleased route through timeoutCondWait (then ENGINE-1/C7-e2 covers) or are hand-rolled MMIO loops; if hand-rolled with any value-specific cond (polarity trap), add the same osIsGpuBusDead short-circuit. They only 'passed' incidentally in both captures (chip died downstream), not by proven coverage.","GspStatusQueueInit loop (message_queue_cpu.c:352): it has a kgspHealthCheck_HAL MMIO escape at 382, but that is MMIO-dependent (accidental, like the lockdown-poll coverage A13 relied on). Add an explicit osIsGpuBusDead short-circuit for determinism rather than relying on the health check happening to catch the dead bus.","Live validation n>=3 at BOTH console_loglevel=8 AND loglevel=4 with netconsole armed is a hard adoption gate, not a nice-to-have: survival at loglevel=8 is the load-bearing proof that the storm is removed AT SOURCE (C7-e3 pre-loop return + C7-e5 done-block gate) rather than merely hidden. This explicit log-replay/observability-independence check is what the A13 workflow skipped.","Blast-radius review of the GPU-wide timeoutCondWait edit (C7-e2): confirm no timeoutCondWait caller anywhere in the driver relies on cond completion via a NON-MMIO (e.g. sysmem-flag) path AFTER the GPU is marked lost, since C7-e2 will now break such a wait early with NV_ERR_TIMEOUT. Low risk (lost-GPU MMIO conds never complete anyway) but must be confirmed because the primitive is driver-wide, not GSP-bootstrap-scoped.","Keep A14 strictly defense-in-depth and never a completeness substitute (the candidate already states this, but it must be enforced): A14's fix-bar1 sticky bit is blind to a NOVEL #979 EQ-divergence with no prior fix-bar1 run, so C7 must remain the load-bearing layer; do not let A14's determinism tempt a future reviewer into trusting the gate as the guarantee."]}]

## CRITIC
Evaluated against the leading composition the reviews + VERIFIED claim 3 converge on: **C7 in-flight dead-bus poll-reader (load-bearing) + A13 os_pci marker-source (kept) + A14 fix-bar1 fail-fast gate (defense-in-depth)**, candidate 4. All four designs got "adopt-with-changes / poll_coverage_complete:false." I verified every load-bearing claim below against source. Ranked gaps that MUST be closed before the next live test:

---

**GAP-1 [HIGH — actuation (d) + regression (b)]: The runtime-PM `nv_dynpower_bounded` funnel is a SECOND GSP-bringup path that arms NEITHER A13 nor C7.**
Verified nv.c:2094-2148: `nv_dynpower_bounded` has its own `queue_work` (2117) → `rm_transition_dynamic_power` → GSP bringup, but it never sets `nvl->bootstrap_in_flight` (the flag lives ONLY at nv.c:1924/1937/2040 in `nv_bootstrap_bounded`). Consequence chain: on a GC6/RTD3 resume the AER early-free block (nv-pci.c:2974, gated on `bootstrap_in_flight`) does NOT fire → os_pci is NOT set at AER time → C7's poll-readers have no EARLY marker to honor. os_pci is only set later by the grace arm (nv.c:2137, after `TbEgpuOpenTimeoutMs + GraceMs`), which is far past the ~+34ms storm onset / +1.075s death window — so the wedge reproduces. On the persistence-OFF #292 substrate, runtime-PM (RTD3/GC6) is exactly the regime that becomes active when the device idles, so this path IS reachable. The C' gate (placed only in `nv_bootstrap_bounded`) also misses it. This is the precise "incomplete coverage caught only live" class the mandate warns about, and it defeats the entire composition on the dynpower path, not just C'.
RESOLVE: set `bootstrap_in_flight` around the dynpower `queue_work` (nv.c:2110-2117) and clear on both worker-return arms; place the C' gate before it too. Add a runtime-PM repro to the test matrix (idle→GC6→touch→resume of the diverged chip), n>=3.

**GAP-2 [HIGH — uncovered poll-sites (a) + actuation (d)]: Three hand-rolled poll loops reachable from a re-open are covered by NEITHER C7 chokepoint.**
C7's two chokepoints are `timeoutCondWait` (gpu_timeout.c) and `_kgspRpcRecvPoll`. These three loops route through neither:
- `message_queue_cpu.c:544` tx-full SEND wait — verified `while (NV_TRUE)`, escapes ONLY on `msgqTxGetWriteBuffer!=NULL` (sysmem) or `gpuCheckTimeout` reset to **1s PER ELEMENT** inside the per-element for-loop (gpuSetTimeout @541). NO MMIO read, NO marker check. On a dead GSP this spins ~1s×elemCount holding the GPU group lock while the foreground blocks in `flush_work` holding `ldata_lock` → a multi-second, **silent (no storm)** F44 wedge substrate. Worst of the three.
- `message_queue_cpu.c:352` GspStatusQueueInit link wait — sysmem + a `kgspHealthCheck_HAL` MMIO escape (accidental/polarity-dependent, like the lockdown poll), 4s.
- `kern_fsp.c:411` `kfspWaitForResponse` — verified hand-rolled `while(!kfspIsResponseAvailable_HAL(...))`, accidental MMIO coverage only, bounded by `kfspCheckResponseTimeout`.
(Note: `kfspWaitForSecureBoot`@gh100:828 and `kfspWaitForGspTargetMaskReleased`@gh100:1158 DO use `gpuTimeoutCondWait` → genuinely covered by C7-e2.)
RESOLVE: promote C7-e6 from "optional" to REQUIRED and add an `if (osIsGpuBusDead(pGpu)) break;` short-circuit to all three loops (msgq:544, msgq:352, kern_fsp.c:411). Until closed, "every poll covered" is false and a chip death during init-RPC send reproduces a silent F44 hold that no marker terminates.

**GAP-3 [HIGH — unproven assumption (c): the lock-safety question, sharpened]: setting PDB lock-free clobbers live PM-state, and violates a documented API-lock precondition — this REJECTS the A13b dual-marker variant and positively favors the C7 read-only approach.**
Verified gpu.c:5266-5291: `gpuSetDisconnectedProperties` writes SEVEN properties + `bInD3Cold` + GC6 state, not just LOST — including `PDB_PROP_GPU_IN_PM_CODEPATH=FALSE`, `PDB_PROP_GPU_IN_STANDBY=FALSE`, `bInD3Cold=FALSE`, `SET_GPU_GC6_STATE(POWERED_ON)`. Called lock-free from the AER thread CONCURRENTLY with a dynpower (RTD3/GC6) worker that is actively managing those exact bits = state-corruption race, not the benign monotonic LOST-flip the reviews assumed. The existing `rm_cleanup_gpu_lost_state` COND_ACQUIRE is what currently serializes this; A13b's lock-free bypass removes that protection. Separately, `NV_GET_NV_PRIV_PGPU` (nv-priv.h:352) documents "Make sure that your stack has taken API Lock before using this macro" — A13b's lock-free pGpu resolution violates it (this is the same unreviewed-precondition class that shipped A13's channel-state assumption).
RESOLVE: make the C7 READ-ONLY marker layer load-bearing (it reads os_pci/PDB, never writes — no lock, no PM-state clobber, no precondition violation). If any PDB-write path is retained, confine it to the `bootstrap_in_flight` cold-init window (no PM transition in flight) AND justify the lock-free `NV_GET_NV_PRIV_PGPU` deref in-code. Do not ship A13b's lock-free `gpuSetDisconnectedProperties`/`cleanupGpuLostStateAtomic` from the AER thread as the primary fix.

**GAP-4 [HIGH — unproven assumption (c): "does the wedge survive with netconsole off?"]: still untested, and GAP-1/GAP-2 leave reachable storm-free F44 wedges that a loglevel change cannot reveal.**
VERIFIED claim 2 is refuted-medium; both captures died at ~+1.07s, before any A12/grace bound could be tested, so the absent-storm outcome is genuinely unresolved. C7's correctness proof for the bootstrap path ("storm eliminated at source") holds ONLY once the early os_pci marker fires + readers short-circuit — but GAP-1 (dynpower, no early marker) and GAP-2 (msgq:544/352, kfsp:411, no marker reader) are reachable paths where the F44 group-lock hold persists for seconds WITHOUT a storm, so netconsole-off would not make them recover.
RESOLVE: the next live test MUST run the repro at BOTH `console_loglevel=8` (netconsole armed) AND minimised observability (`echo 4 4 1 7 > /proc/sys/kernel/printk` + external 100ms ping/ssh liveness + passive sysrq-w/t/l blocked-task dump), n>=3; SURVIVAL at both is the load-bearing proof. Capture a blocked-task stack so any residual wedge is attributed to a specific poll (closes GAP-2/GAP-9).

**GAP-5 [MEDIUM — regression in understanding (b)]: the A12 lock-model comment (nv.c:1959-1968) is now known-incomplete and must be sharpened, or the source perpetuates the exact error the mandate forbids.**
Verified nv.c:1959-1968 states "across the poll the worker holds the GPU GROUP lock, NOT the API lock." Source shows this is true ONLY for the lockdown window; `kgspInitRm` REACQUIRES the API lock at kernel_gsp.c:4818-4820 before the post-INIT_DONE control RPCs — the netcon3 storm site (proven post-INIT_DONE because `_kgspIsHeartbeatTimedOut` only returns non-trivially after `_kgspHeartbeatInit`@6162). The stale comment silently underwrites the false assumption that "A12's lockdown-arm `rm_cleanup` COND_ACQUIRE will succeed" — it DEFERS during the storm because the API lock is held.
RESOLVE: extend the comment to "API lock released across the lockdown cond, REACQUIRED across the post-INIT_DONE control-RPC phase; therefore `rm_cleanup` COND_ACQUIRE may DEFER during the RPC storm, and the lock-free os_pci marker + C7 poll-readers (not `rm_cleanup`'s PDB set) free that poll." Update the A12 JOIN-COST comment (nv.c:2027-2036) consistently.

**GAP-6 [MEDIUM — actuation (d): blast radius + build]: C7-e2 edits `timeoutCondWait`, a DRIVER-WIDE primitive, and changes the dead-bus return status.**
Verified gpu_timeout.c: `timeoutCondWait` takes `pGpu = pTD->pGpu` (available, NULL-safe via osIsGpuBusDead), used by EVERY `gpuTimeoutCondWait` driver-wide, not just GSP bootstrap.
RESOLVE: (i) audit all `timeoutCondWait` callers for any cond that legitimately completes via a NON-MMIO (sysmem/host-written) path AFTER os_pci/PDB is set (low risk — a lost GPU's MMIO conds never complete — but confirm); (ii) audit all `gpuTimeoutCondWait` callers (lockdown gh100:1050 verified generic `!=NV_OK`; SPDM gh100:620/662, kgspResetHw gh100:133/144, CC-cleanup, FSP) for any `NV_ERR_TIMEOUT`-specific branch, since C7-e2 now returns a lost/timeout status on a dead bus; (iii) **real `make modules`** of the composed C+E+A tree (apply != validated — the P5 lesson), since the exported `osIsGpuBusDead` wrapper + header decl + new gpu_timeout.c include must build across all TUs.

**GAP-7 [MEDIUM — actuation (d): C7 recovers WITHOUT ever setting PDB].**
Verified os.c:2050 short-circuits BEFORE the DETECTOR_MMIO_DEAD funnel at os.c:2081, and the heartbeat self-sink at kernel_gsp.c:2928-2930 needs `timeoutCount==threshold` which the fast exits never reach; with the worker freed via the os_pci READ short-circuit and A12's timeout-arm `rm_cleanup` deferring (GAP-5), `PDB_PROP_GPU_IS_LOST` may end up never set. The chip is left os_pci-dead but RM-side PDB-unset until teardown.
RESOLVE: confirm the close/teardown path (`nv_shutdown_adapter`, `rm_cleanup`, device free) and any subsequent re-open do NOT assert/depend on `PDB_PROP_GPU_IS_LOST==TRUE` for correct lost-state cleanup. If they do, have the worker's own `NV_ERR_RESET_REQUIRED` unwind set PDB under its already-held locks (safe — worker owns them). Cheap to confirm; do it before the live test so a teardown anomaly isn't misread as a new wedge.

**GAP-8 [MEDIUM — coverage of the defense-in-depth layer (a)]: A14/C' fix-bar1 sticky-bit gate is structurally blind to a novel divergence; C7 must stay load-bearing.**
Divergence is driver-invisible live (PMC_BOOT_0/WPR2/LnkSta read healthy; Phy16Sta.EquComplete is endpoint-only under TB virtualization, n=2). A divergence that doesn't present the 256MB-BAR1 symptom, or is recovered by other means, leaves `diverged_recovered=0` → gate bypassed → worker queued → wedge. C' is therefore a deterministic-but-partial first line, never the completeness guarantee.
RESOLVE: keep C7 (poll-readers) as the required layer that ships WITH A14, never instead; document the false-negative in-code so no future reviewer elevates the gate to "the fix." (Matches VERIFIED claim 3 synthesis: marker/poll-reader primary, C' probabilistic.)

**GAP-9 [LOW — residual, does not block but must be captured in the test (d)]: storm re-caller multiplier and netcon2's actual stuck poll are unpinned.**
Why ~23 `_kgspRpcRecvPoll` returns vs one control-RPC abort, the absent "API lock contended, deferring" LEVEL_WARNING in netcon3, and netcon2's stuck poll (inferred — zero GSP-poll printks, no stack) are not pinned. C7's pre-loop catches every re-entry so coverage is unaffected, but the minimised-observability re-test (GAP-4) should capture a clean blocked-task stack to retire these.

---

Bottom line for next live test: GAP-1 (dynpower funnel arms no marker) and GAP-2 (three hand-rolled sysmem/FSP polls outside both chokepoints) are reachable wedge paths the leading candidate does NOT close and MUST be patched first; GAP-3 (PDB-write lock-safety) rejects the A13b variant and confirms C7-read-only as the safe load-bearing layer; GAP-4 (dual-loglevel survival, n>=3) and GAP-5/6/7 (lock-model comment, primitive blast-radius+compile, PDB-unset teardown) gate a trustworthy test. Source-verified anchors: nv.c:2094-2148, 1924/2137, 1959-1968; nv-pci.c:2974-2982/3018-3025; message_queue_cpu.c:544 & :352; kern_fsp.c:411; kern_fsp_gh100.c:828/1158; os.c:1884/2050/2081; kernel_gsp.c:2813-2822/2851/2928-2933/2971-2981; gpu.c:5266-5291; gpu_timeout.c timeoutCondWait; nv-priv.h:352.
