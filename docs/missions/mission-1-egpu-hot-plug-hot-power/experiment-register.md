# MISSION-1 — current experiment register (single source of truth)

**Updated:** 2026-06-05 (E27 pinned; #292 captured+re-root-caused; #304/#305 filed). **This doc supersedes the per-series indices for CURRENT status + priority.** The older docs (`mission.md`, `matrix.md`, `experiments/README.md`) are kept as historical provenance + execution detail; where they disagree with this register on *status*, this register wins.

**Why this exists:** the investigation grew four parallel series (BAR1 archaeology, open-arm wedge, shutdown, recovery integration) plus a 43-entry failure-mode catalog. The per-series indices each went stale as findings landed. This is the one place that reflects what is actually answered, open, or retired today.

---

## 1. Failure-mode map (the active ones)

Full catalog (43 modes) lives in `fake-5090/failure-modes/`. The modes this mission is actively working:

| Mode | What | Defense / status |
|---|---|---|
| **F41** | Chip ReBAR CTRL resets `0xF→0x8` on TB hot-add → broken-BAR1 (256 MB) | **SOLVED in userspace** by `tools/fix-bar1.sh` (chip CTRL write + pciehp slot-cycle), verified n≥2. **E27** = the in-kernel version (the destination). |
| **F40-open** | `RmInitAdapter` GSP lockdown-release stall on a userspace-recovered chip → host wedge | **Bounded** by A6 (`-EIO`); first-open hole **closed by A9** (apnex.24). Chip-side cause is #979 territory (contain, not cure). |
| **F42** | Leaked F40b bounded-wait worker → UAF if `rmmod`/rebind races it | A7 has the SH-3 `flush_work` guard; **A6 = gap** (Phase 0 is verifying whether this needs closing before the destructive ladder). |
| **F43** | `rm_shutdown_adapter` ~600 ms GSP-unload RPC latency (NOT a hang) | A7 budget `NVreg_TbEgpuShutdownTimeoutMs=1200`. Possibly deletable (v5). |

Sub-mission C (unexpected disconnect / Xid 154 / surprise-removal cascade — F33/F39) is a **separate parallel track** (A2/A3/C5 + the v4 cascade work), not part of the recovery-validation campaign below.

---

## 2. Investigation series — status

| Series | Question | Status |
|---|---|---|
| **BAR1 archaeology** (`matrix.md`, E01–E27; hypothesis **H10**) | Does any software path recover BAR1=32 GB on a re-plugged eGPU? | **ANSWERED 2026-05-28.** `fix-bar1.sh` is the trigger — it *is* **E16 (chip RBAR-CTRL write) + E2 (pciehp slot-cycle)** chained. H10 **CONFIRMED**; exit-criterion (a) met. **E2–E24 SUPERSEDED** (they hunted for a trigger we found). **E27** (in-kernel) stays live. |
| **Shutdown** (SH-0..3, #278–281) | Why does `rm_shutdown_adapter` "hang"? | **DONE.** It's a ~600 ms RPC, not a hang → produced the **F43**/**F42** split (2026-05-31). Closed. |
| **Open-arm** (OA Lanes 1–3, #282; `open-arm-forensics-ledger.md`) | F40-open root cause | **Lane 1+2 DONE** — site pinned (GSP lockdown-release, `kgspBootstrap_GH100 → gpuTimeoutCondWait`). **Lane 3 (destructive) + Rung-9 cure-vs-contain OPEN** (#285). |
| **Reset-efficacy ladder** (#286) | Does a runtime reset *cure* the open-arm wedge? | **RETIRED** — confounded/unsafe (the 2026-05-31 R0.5 reboot); subsumed into the recovery-validation campaign below (the cure question becomes the operational determinism question). |

---

## 3. Current prioritised work

1. **[ACTIVE] Deterministic userspace recovery validation** — prove a re-plugged eGPU recovers deterministically in userspace (`fix-bar1 --bind` + persistence + A9 net), until E27 retires it; the deterministic recipe *is* E27's acceptance spec. **Phase 0 (chip-free) COMPLETE 2026-05-31** — verdict: A9-bounded `-EIO` is **NOT clean** (source-confirmed F42 UAF on A6's open path, widened by A9), so the destructive ladder is **hard-gated on R0 = the A6 leak→join hardening** (chip-free). Plan (R0 + harness gates + R1–R3 + E27 spec + the R0 stop-rule): [`../../superpowers/plans/2026-05-31-deterministic-recovery-validation.md`](../../superpowers/plans/2026-05-31-deterministic-recovery-validation.md). The capstone that composes F41-recovery + F40-open containment + F42 + persistence. **UPDATE 2026-06-01: R0 deployed (apnex.25, A6 flush→join closing the F42 UAF) + R1 baseline-determinism PASSED 5/5, ZERO reboots** (`r1-baseline-determinism-20260601T044533Z`) — userspace recovery is **deterministic for the non-adversarial path** (broken-BAR1 → `fix-bar1 --bind` → 32768/Gen3/persistence/CUDA-ready, H2D 2.71–2.75 GB/s, A6 never armed). **Recipe sharpened:** complete recovery = `fix-bar1 --bind` **+ `nvidia_uvm` bringup** (`modprobe nvidia_uvm` + `nvidia-modprobe -u -c 0`) — fix-bar1-alone is nvidia-smi-ready but NOT CUDA/vLLM-ready; E27 acceptance must mean *CUDA-ready*. **R2 + R3 COMPLETE 2026-06-01 (zero reboots):** **R2 CONTAINED 10/10** (`rung2.sh` — every divergent fire bounded to `-EIO` ~210 ms, every worker JOINED, 0 KFENCE UAF, 0 wedge) and **R3 RACE-SAFE 10/10** (`rung3.sh` — immediate post-`-EIO` `rmmod` bounded 53–86 ms, no F42 double-UAF) with **CUDA-functional** post-fire recovery (nvbandwidth H2D 2.72–2.76 GB/s, 10/10). **Session-wide: 0 real KFENCE use-after-free across ~21 deliberate fires** ⇒ R0's `flush_work` empirically closes F42 (lifecycle/leak invariant, substrate-independent). **CAVEAT (corrected 2026-06-01 post adversarial review):** the precond reproduces only the **WPR2-already-up fast-fail** substrate (`_kgspBootGspRm: unexpected WPR2 already up`), where the worker fails at ~200 ms and joins in ~10 ms — the `gpuTimeoutCondWait` GSP-lockdown busy-poll worst case was NOT exercised live. **SOURCE-SETTLED 2026-06-02 (workflow `lockdown-poll-substrate-experiment-design` + source verification; NO fire — the lockdown substrate is not userspace-reproducible on demand, WPR2-already-up is an H-OA1 sequela):** on a true lockdown substrate the open-timeout foreground blocks up to RM `gpuTimeout` (~4 s graphics / ~30 s compute) at `nv.c:1905` `rm_cleanup_gpu_lost_state`→`rmapiLockAcquire(API_LOCK_FLAGS_NONE)` (BLOCKING; NONE lacks the `COND_ACQUIRE` non-block bit) **before** `flush_work` (`nv.c:1943`), because the worker holds that RM API lock across the lockdown poll. ⇒ A6's effective bound is `gpuTimeout` NOT 200 ms on that substrate; the C5 sink is ineffective there; size E27/A9 against `gpuTimeout`. **⚠️ CORRECTED 2026-06-02 (same day, post-wedge): the "not userspace-reproducible" clause above is FALSIFIED.** The lockdown substrate IS reproducible — **clean shutdown (WPR2→0) then re-open** — and it **HARD-WEDGED the host (2 reboots)**: the source-settled worst case is now empirically triggered. A6/R0/C5 do NOT contain it. ⇒ the path is the **in-driver containment fix (task #292), NOT a synthetic**; fake-5090 stays deferred to a later failure-modes-validation project. Forensics: [`wedge-2026-06-02-lockdown-reopen-forensics.md`](./wedge-2026-06-02-lockdown-reopen-forensics.md). The #292 fix unblocks the live #2 cure-isolation matrix. The userspace recovery is now validated on the **clean path (R1) AND the adversarial bad-chip-open path (R2/R3)**. **NOT covered:** physical surprise-removal / live chassis power-cycle with the driver active (separate unrecovered wedge class = sub-mission B). **R4 COMPLETE 2026-06-01 (cure-vs-contain, `rung4-cure.sh`, zero reboots across all 5 variants):** ALL FIVE runtime resets — none/rebind/flr/sbr/slot — are **CONTAIN-ONLY** (1/1 smoke each, every fire divergence-confirmed `timed≥1`, bounded `-EIO` ~210 ms, 0 KFENCE UAF, recovered CUDA-functional). **No PCIe-level reset (FLR, SBR=A3's `pci_reset_bus`, pciehp slot-cycle) cures the F40-open GSP-lockdown divergence** ⇒ it is **sticky = #979 territory**; A6 containment + the TB-reenum recovery (R1) is the right-and-only fix. Refinement: the slot-cycle re-enumerated the device (BAR1→32768) yet didn't cure, while the recovery path (which adds a **TB deauth/reauth**) does — so the cure boundary is the **TB-tunnel level, not PCIe** (hypothesis: pciehp slot-cycle re-enumerates the tunnel endpoint but doesn't power-cycle the eGPU silicon). The retired #286 reset-ladder's host-wedge danger was the A6 first-open hole; **A9 (probe-time `is_external_gpu`) closes it — empirically proven here**: `rebind` (the exact 2026-05-31 wedge variant) and the device-drop-risk `sbr`/`slot` all ran contained, zero reboots, gated by the `tb_egpu_is_external==1` per-fire assert. **R1–R4 ALL COMPLETE.** Design+safety-review via workflow `r4-cure-vs-contain-design`.
2. **[NEXT] E27** — broken-BAR1 recovery on the TB hot-add path; retire `fix-bar1`. **⮕ E2 CYCLE-2 FAILED + PLATFORM RESET 2026-06-13 — re-scoped, NOT dead: same `flr=1` PASSED cycle-1 (seconds between FLR and bind) but FAILED cycle-2 (`kbusVerifyBar2` garbage, `RmInitAdapter 0x24:0x72:1307`) — the difference was TIMING (cycle-2 ran FLR+modprobe back-to-back in ms). ⇒ FLR-WITHOUT-SETTLE/VERIFY is unreliable; likely a post-reset SETTLE-TIME variable (fix-bar1's slot-cycle has settle sleeps). The failed-relatch chip, then repeatedly accessed (11 opens; my retry nvidia-smi → WPR2/crashcat), escalated to a FATAL HW error → firmware/platform RAS RESET (no kdump panic; next-boot `BERT: 1 record`) — aggravated by experiment retries, not purely inherent. **NEXT (creative): add post-FLR settle + relatch-VERIFY-before-bind to the module — tests settle-time AND is fail-safe (no bind on unrelatched chip → no platform-reset). Re-test n≥3.** slot-cycle/PERST = proven FALLBACK if settle+verify still fails. See finding's E2 CYCLE-2 FORENSICS. (Superseded "VIABLE" claim below = re-scoped to "viable WITH settle+verify".)** **⮕ E2 LIVE PASS 2026-06-13 (n=1): MECHANISM VIABLE — `flr=1` (resize + `pci_reset_function`/FLR) + `recover=0` on the REAL broken-256 M tree → `RmInitAdapter` rc=0, GPU healthy (RTX 5090, 32 G, P8), zero errors. The BAR-aware FLR re-latches the size on a fresh chip → NO slot-cycle/PERST needed; the source A3-CHECK prediction is REFUTED (its honest residual — clean FLR-before-init — was the truth; the don't-give-up live check flipped it). `recover=0` is a safety belt (failed recovery can't trigger A3's wedge); on success A3 stays dormant. OPEN: n≥3 determinism; A3 hardening (BAR-aware gate) so recover can stay on; merge. See finding's E2 LIVE RESULT.** (Superseded A3-CHECK pessimism below.) **⮕ A3-CHECK 2026-06-13 (source, med-confidence): the 256 M→32 G ReBAR SIZE relatch needs a PERST — A3 DID restore ReBAR=0xF after its SBR and the retry still wedged ⇒ FLR (weaker reset) likely FAILS E2. REVISED MECHANISM = automate fix-bar1's slot-cycle/PERST (not a light reset); near-term = auto-trigger fix-bar1 + A3 hardening (A3's RECOVERED gate is BAR0-only/size-blind = false-positive → must be BAR-aware + signature-gated). One contained `flr=1`+`recover=0` run closes FLR. See finding's A3 RESET-PATH CHECK.** Prior: **⮕ E1 LIVE RESULT 2026-06-13: `flr=0` (resize, no reset) on the REAL broken-256 M tree FAILED + WEDGED THE HOST** (capture `/root/netconsole-stage01-new.log`; finding's E1 LIVE RESULT). The 256 M→32 G *size* change without a reset desynced the GPU memory aperture → `kbusVerifyBar2 BAR2 garbage 0xffffffff` → `RmInitAdapter failed 0x24:0x72:1307` (CONTAINED) → then the **A3 recover module (recover=1) fired a non-ReBAR-aware `pci_reset_bus` + retry, and the retry's MMIO on the re-desynced aperture WEDGED**. ⇒ (1) a reset IS required (`flr=0` dead); (2) experiments MUST run `recover=0`; (3) size-relatch now leans toward needing fix-bar1's slot-cycle/PERST. **NEXT = E2: `flr=1` (FLR, BAR-preserving) WITH `recover=0`** — if a pre-init BAR-aware FLR re-latches the size, mechanism lives; if not, verdict → "automate fix-bar1's slot-cycle." (Prior optimistic status below kept for provenance.) **⮕ STATUS 2026-06-13 (live test + salvage investigation): MECHANISM VIABLE WITH A 1-LINE FIX.** Stage-1 wet run refuted the NAIVE no-reset call (it re-placed BAR1 at `0x6000000000` and left the GPU un-initable, `RmInitAdapter` `0x22:0x38:859`) — but the cause was **DEVICE STATE, not the address** (the GPU is healthy at `0x6000000000` right now; the recovery kept that base and only reset). I'd moved BAR1 of an already-RM-initialized chip without a reset → internal-aperture/config desync. **Fix (implemented):** add `pci_reset_function(gpu_dev)` (FLR, preserves the resized BAR) after the resize — module param `flr=Y`. **Pending live re-test on the REAL broken-256 M substrate** (Stage-1 used the wrong healthy substrate): E1 `flr=0` (likely PASS without reset — fresh chip), E2 `flr=1` else. Honest residual: the real fix changes BAR1 *size* not just base; if Blackwell needs a true PERST to re-latch size, FLR could be insufficient → falls back to "automate fix-bar1's slot-cycle." Module/harness `tools/e27-bar1-rearm/`. See finding's INVESTIGATION RESULT. (Superseded analysis below, kept for provenance.) **⮕ CONCLUSIVELY RE-SCOPED 2026-06-13 (real-7.0.9-source determinism analysis) → `finding-2026-06-13-E27-halfb-determinism-verdict.md`. NOT a kernel patch — a deterministic OUT-OF-TREE MODULE retires `fix-bar1` whole (both halves), no kernel rebuild/cmdline. Confidence high; conditional on one n≥3 aged-tree experiment.** Root cause (resolved): the **root hotplug port `00:07.0`'s own prefetch window freezes at a 32 G-MISALIGNED base** on aged trees (06-05: `0x4810000000`, 256 MiB off-grid); the `resource_assigned()` skip (`setup-bus.c:1293`) then refuses to re-size it, so the only 32 G-aligned interior slot is `0x5000000000` (~256 MiB room) → squish → off-bus. 32 G alignment is NATURAL (from the GPU BAR via `calculate_head_align`); `resource_alignment=35@03:00.0` is **redundant/not causal**. Mechanism: decode-off → `pci_release_resource()` the empty downstream-port sibling prefetch windows (`03:01/02/03`, the cascade-blockers) → `pci_resize_resource(gpu,1,15,0)` cascades release `03:00.0→02:00.0→00:07.0` + re-sizes/re-places `00:07.0` at `0x4000000000`; this ALSO calls `pci_rebar_set_size(...,15)` = chip-CTRL `0x8→0xF` (half (a) subsumed). All primitives `EXPORT_SYMBOL` in the real `Module.symvers`. Decisive experiment + fallback in the finding. **STALE (superseded by the finding):** the old "intermediate `02:00.0`=256 MiB bottleneck / grow it at `pci_reassign_bridge_resources`" framing — that 256 MiB window was a `fix-bar1` slot-cycle artifact, not the native-path cause. Old refs: `finding-2026-06-05-…` (correction-bannered), `experiments/E27-pci-core-patch.md`. **Iterate LIVE on an aged tree (operator-gated); fake-5090 CANNOT repro (no TB tunnel layer).**

3. **[OPEN — load-bearing] #292 — re-open RM GSP-lockdown bring-up deadlock (in-driver containment).** **CAPTURED live + re-root-caused 2026-06-05** (Path-B persistence-OFF roll, netconsole, 3/3 adversarial-verified): after a clean LAST-CLOSE clears WPR2→0 on a **#979-EQ-diverged** chip, the next `open()` re-enters `RmInitAdapter → kgspBootstrap_GH100 → gpuTimeoutCondWait(_kgspLockdownReleasedOrFmcError)` (the WPR2-clear lockdown busy-poll) and **hard-wedges the host**. Mechanism (**source-true**, per nv.c:1946-1959/2023-2027 — **supersedes the 06-02 doc's "API-lock inversion"**, which the source comment falsified the same day): the foreground re-open holds **`nvl->ldata_lock`** across an unbounded `flush_work` of the stuck worker; the worker holds only the **GPU group lock** (it RELEASES the RM API lock at `kernel_gsp.c:4785` under relaxed GSP init locking — so it is **not** an API-lock inversion). The deployed **A10-v2** lockdown arm already bounds this at **timeout+grace** via the lock-free `os_pci_set_disconnected` marker — but the 06-05 capture wedged at **~1.07 s, INSIDE the in-flight window before the 3000 ms timeout**, when the re-open's MMIO raised an **AER CmpltTO** and `nv_pci_error_detected` became the **second `ldata_lock` contender** (exactly the 06-02 doc's line-35 prediction — now live-confirmed). **NOT a close-path, NOT an AER/bus-reset wedge** — the AER is a downstream symptom; `tb_egpu recover` is exonerated; all 5 resets are contain-only (R4). **Fix candidates (target the in-flight-AER window):** (A') `nv_pci_error_detected` sets the lock-free dead-bus marker EARLY when a bootstrap worker is in-flight (frees the stuck re-open before any `ldata_lock` contention); (C') fail-fast the diverged re-open at the A12 funnel before the chip-touch; (B', heavier) drop `ldata_lock` across the bounded wait. MUST NOT regress the WPR2-fast-fail path (R2–R4) or the A10-v2 discriminator. **Repro:** diverged 32 G chip, persistence-OFF LAST-CLOSE → re-open, near-deterministic roll-1 (live-wedging → fake-5090 #290 or armed kernel-side capture only). Forensics: `finding-2026-06-05-recovery-bringup-wedge-forensics.md` §CAPTURED v3. Unblocks live #2 / the #291 TB-tunnel cure-isolation matrix. **⮕ A13 (=(A')) BUILT + DEPLOYED apnex.31 + LIVE-TESTED 2026-06-06 → FAILED** (`finding-2026-06-06-A13-live-FAIL-rpcpoll-wedge.md`, capture `netcon3-…`): A13 fired correctly but the wedge MOVED past the lockdown poll into `_kgspRpcRecvPoll` (heartbeat storm); A13 covered the lockdown poll only **by accident** (`0xFFFFFFFF≠0`) and is **counterproductive** (its early `os_pci` marker pre-empts the `DETECTOR_MMIO_DEAD` self-heal funnel). **REDESIGN (19-agent, dual-capture, no-regression) → `design-2026-06-06-292-redesign-C7-A13prime-A14.md`:** the fix is **C7** (read-only dead-bus poll-reader `osIsGpuBusLost` at the 2 engine chokepoints `timeoutCondWait`/`_kgspRpcRecvPoll` + 3 hand-rolled loops — covers all 13 reachable GSP poll-sites) **+ A13′** (keep marker source; arm `bootstrap_in_flight` on the 2nd funnel `nv_dynpower_bounded` = GAP-1; fix the lock-model comment = GAP-5) **+ A14** (fix-bar1 sticky fail-fast gate, defense-in-depth). REJECTED: A13b lock-free PDB-write (clobbers 7 PM bits + `NV_GET_NV_PRIV_PGPU` API-lock precondition); C′ alone (divergence-invisible live). Build = **apnex.32**; validate **dual-loglevel** (storm may be a netconsole amplifier — unresolved), n≥3, both funnels, every poll-site short-circuited. GAP-7 (PDB-unset teardown) + GAP-6 (timeoutCondWait blast-radius caller audit + real `make modules`) are pre-build checks. **⮕ BUILT + LIVE-VALIDATED 2026-06-13 (apnex.32):** GAP-6/7 audit GATE-GO (all 25 callers safe; +C7-e7 REQUIRED guard-widenings incl. load-bearing `rpcRmApiFree_GSP`); **A14 gate PASS** (13 ms refusal of the apnex.30/31-killer roll); **C7 PASS ×2** (min-obs + loglevel 8 — AER→A13 marker→C7-e4 one-iteration exit→bounded `rc=-5`, host alive, **zero storm** ⇒ **GAP-4 ANSWERED: storm removed at source**); clean-chip no-regression matrix green + accidental R2 fast-fail regression PASS. **Residual:** recover-disabled control incomplete (udev race) → redo via modprobe.d; **F48 discovered** (`finding-2026-06-13-F48-pbi-capwalk-spin.md`: probe-time PBI cap-walk infinite spin on a stale disconnected pci_dev, `pci_pbi.c:88`, vendor latent, host-survivable but unkillable-modprobe → reboot; fix = TTL-bound walk + probe gate); C7 n≥3 top-up; dynpower repro (likely unreachable: `pcie_port_pm=off`); 14-day soak. Handover §NEXT-STEPS item 3 has the full ledger. **⮕ C8 (F48 fix) BUILT+DEPLOYED apnex.33 (silent on clean probe). ⮕ RECOVER-DISABLED CONTROL + C7 n=3 TOP-UP DONE 2026-06-13 — n=3 ALL PASS:** with `NVreg_TbEgpuRecoverEnable=0` (modprobe.d drop-in, lexically-last over the injector's `=1`, verified `modprobe -c`) and A14 disarmed, all 3 #292 rolls contained identically (A12 bounded → AER → A13 DISCONNECT explicitly `disabled (NVreg_TbEgpuRecoverEnable=0)` → C7 single-pass `SET_GUEST_SYSTEM_INFO failed 0xf` → `RmInitAdapter failed 0x62:0xf:2131` → `rc=-5` ~1 s, host alive; storm quant/cycle: ~30 NVRM lines, `_kgspRpcRecvPoll`=1, heartbeat=0). **C7/A13 containment is independent of the recover module — lives on `nvl`.** Host restored to soaking baseline (recover=1, injector re-adopted via "already loaded — skipping", 0 restarts; capture disarmed). **#292 live work CLOSED; only the 14-day soak (from 2026-06-13) gates cutover/upstream.**

4. **[DONE 2026-06-13] fix-bar1 hardening — #304 / #305.** **#304 (P1):** BAR1=0 false-success — awk float `(e-s+1)/1024/1024` on an unset BAR yields `9.53674e-07`, the `-lt 32768` bash test errors, the "RECOVERY FAILED" fatal is skipped, and it prints "✓ recovered" then proceeds to a doomed modprobe — **this masks the E27 off-bus failure**. Fix = integer math + treat `s==0 && e==0` as size 0 → hard fail. **#305 (P2):** when "no driver bound" is already proven, auto-clear the `COMMAND` decode bits (safe self-heal) instead of refusing. **⮕ IMPLEMENTED + reviewed 2026-06-13 (`tools/fix-bar1.sh`, +50/−7):** #304 = new pure-bash 64-bit-integer `bar1_size_mib()` (0 for unset/off-bus) at both PRE/POST size gates + a distinct **off-bus (`==0`) fatal** that names the E27 bridge-window failure instead of "✓ recovered". #305 = COMMAND guard **relaxed from exact-`0000` to memory-decode-bit-only** (`& 0x2`, Memory Space Enable — the real ReBAR-resize safety predicate; the old guard wrongly refused `0x0404`/`0x0407` hit live 2026-06-13) + **dry-run-safe self-heal** (clears COMMAND only when no driver bound, which is already proven above). **Validated WITHOUT live device** (soak undisturbed): `bash -n` + shellcheck (no new findings) + synthetic unit tests of both logic units (unset→0/256→256/32768→32768/malformed→0; COMMAND 0x0000/0404/0407 classify correctly) + **3-reviewer adversarial workflow** (opus) verdict **ship-with-fixes, zero must-fix**; the one should-fix (a dry-run log that printed a false "memory decode off ✓") + 2 reinforcing guards folded in. **Live integration verified on next real recovery** (not run against the live soak). fix-bar1 remains a stopgap pending E27 in-driver recovery.

5. **[OPEN, lower] OA Lane 3 / cure-vs-contain** (#285) — the open-arm *science* (does any reset clear the divergence, or is it #979 territory). Can fold into #1's adversarial rung or run separately, reboot-loop.
6. **[RETIRED] #286** — subsumed by #1.
7. **[SUPERSEDED] E2–E24** — BAR1 archaeology answered. Keep **E27** (kernel target); **E25/E26** remain as kernel-alternative fallbacks.
8. **[PARALLEL] Sub-mission C** — surprise-removal / Xid-154 resilience (E8/E9 + cascade-class v4). Separate track; not gated on the above.

---

## 4. Terminology — two different "Phase 3"s

- **`matrix.md` "Phase 3"** = upstream kernel work (E25–E27). Still valid usage there.
- **The A9 plan's "Phase 3"** = the destructive validation phase of the deterministic-recovery campaign (item 1 above).

When ambiguous, say "**E27 / kernel work**" vs "**recovery-validation destructive phase**."

---

## 5. Sub-mission status (de-staled from mission.md's 2026-05-25 snapshot)

| Sub-mission | mission.md (2026-05-25) | Current (2026-05-31) |
|---|---|---|
| **A — hot-plug** (cable insert) | "probably works on idle GPU" | Re-plug → broken-BAR1 (F41) is **recoverable in userspace** via `fix-bar1.sh`; the determinism of that recovery is what item-1 validates. |
| **B — hot-power** (chassis power-cycle) | "definitively broken, multi-month" | **De-stale:** the broken-BAR1 outcome is **recoverable** via `fix-bar1.sh` (software slot-cycle = the bridge re-enum trigger H10 sought). E27 is the in-kernel fix. Not "definitively broken" — recoverable-with-a-userspace-step. |
| **C — unexpected disconnect** (Xid 154) | "partially handled, gap" | Still the surprise-removal wedge class; separate track (item 6). |

---

## 6. Cross-references

- `mission.md` — root doc + H1–H10 registry (2026-05-25 snapshot; see banner there).
- `matrix.md` — Phase-2 BAR1 archaeology matrix (H10 answered; see banner there).
- `experiments/README.md` + `experiments/E*.md` — per-experiment execution detail.
- `open-arm-forensics-ledger.md` — the live OA (F40-open) investigation (Lanes/Rungs).
- `shutdown-hang-ledger.md` — the closed SH investigation (→ F43/F42).
- `experiments/h1-userspace-recovery-2026-05-28.md` — the verified `fix-bar1` recovery (answers H10).
- `tools/fix-bar1.sh` — the recovery procedure under validation.
- `fake-5090/failure-modes/` — the full 43-mode catalog (F40 / F41 / F42 / F43).
