# GENERATED by tools/regen-base-patches.sh -- DO NOT EDIT.
# Patch:  A5-version-and-toggles
# Source: fork branch a5-version-and-toggles @ 5fab257398d8b11b2b129eac472d108964000a03
# Base:   a4-close-path-telemetry @ cddf8b9ad3cc999ae3ede135d46b0c7258985cdc
# Regenerate with: tools/regen-base-patches.sh
#
diff --git a/kernel-open/Kbuild b/kernel-open/Kbuild
index 1af79599..2157e13e 100644
--- a/kernel-open/Kbuild
+++ b/kernel-open/Kbuild
@@ -85,6 +85,19 @@ ccflags-y += -D__KERNEL__ -DMODULE -DNVRM
 include $(src)/../version.mk
 ccflags-y += -DNV_VERSION_STRING=\"$(NVIDIA_VERSION)\"
 
+# tb-egpu (cluster A5): Kconfig-style master toggle.
+#
+# CONFIG_NV_TB_EGPU      master gate. Default y. Currently reserved as
+#                        a documentation-only symbol — gating P1-P5 out
+#                        would require wrapping all eGPU additions
+#                        across multiple clusters in #ifdef, out of
+#                        scope for the refactor. Build is "always y"
+#                        today; the symbol is defined for future use.
+CONFIG_NV_TB_EGPU       ?= y
+ifeq ($(CONFIG_NV_TB_EGPU),y)
+ccflags-y += -DCONFIG_NV_TB_EGPU
+endif
+
 # Include and link Tegra out-of-tree modules.
 ifneq ($(wildcard /usr/src/nvidia/nvidia-public),)
  SYSSRCNVOOT ?= /usr/src/nvidia/nvidia-public
diff --git a/version.mk b/version.mk
index ea15e650..7a71d5c1 100644
--- a/version.mk
+++ b/version.mk
@@ -1,4 +1,4 @@
-NVIDIA_VERSION = 595.71.05
+NVIDIA_VERSION = 595.71.05-aorus.14
 NVIDIA_NVID_VERSION = 595.71.05
 NVIDIA_NVID_EXTRA = 
 
